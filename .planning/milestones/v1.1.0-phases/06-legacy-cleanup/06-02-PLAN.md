---
phase: 06-legacy-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
autonomous: true
requirements: [LC-02, LC-03, LC-05]

must_haves:
  truths:
    - "EntitiesCatalog не использует ModelFactory и legacy-модели для создания entity"
    - "OnDeadEntity определяет тип entity через EntityType enum, не через TryFindModel"
    - "SpawnNewEnemy читает позицию корабля из ShipPositionData ECS singleton"
    - "GameScreen читает Score из ECS ScoreData singleton через GameScreenData.Score"
    - "Legacy callbacks (OnUserGunShooting, OnUserLaserShooting, OnShipCollided, OnEntityDestroyed) удалены"
  artifacts:
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "Каталог без ModelFactory, без legacy-моделей, с EntityType tracking"
      contains: "enum EntityType"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "OnDeadEntity с EntityType вместо TryFindModel"
      contains: "EntityType.Asteroid"
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "SpawnNewEnemy через ShipPositionData, без _shipModel.Move.Position"
      contains: "ShipPositionData"
    - path: "Assets/Scripts/Application/Screens/GameScreen.cs"
      provides: "Score из GameScreenData.Score, без _data.Model.Score"
      contains: "_data.Score"
  key_links:
    - from: "EntitiesCatalog.cs"
      to: "Application.OnDeadEntity"
      via: "TryGetEntityType(go, out EntityType) callback"
      pattern: "TryGetEntityType"
    - from: "Game.cs"
      to: "ShipPositionData singleton"
      via: "EntityManager query"
      pattern: "ShipPositionData"
---

<objective>
Убрать зависимость от legacy-моделей в EntitiesCatalog, Application, Game и GameScreen. EntitiesCatalog создает entity без ModelFactory, отслеживает EntityType через enum. OnDeadEntity определяет тип через EntityType. SpawnNewEnemy читает позицию корабля из ECS. GameScreen читает Score из ECS.

Purpose: Полное устранение зависимостей на legacy-модели, что позволит в Plan 03 безопасно удалить файлы.
Output: Код без ссылок на ShipModel, AsteroidModel, BulletModel, UfoBigModel, IGameEntityModel, Model.Score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-legacy-cleanup/06-RESEARCH.md
@.planning/phases/06-legacy-cleanup/06-01-SUMMARY.md
</context>

<interfaces>
<!-- ECS Tags (уже существуют) -->
From Assets/Scripts/ECS/Components/Tags/:
```csharp
public struct ShipTag : IComponentData { }
public struct AsteroidTag : IComponentData { }
public struct BulletTag : IComponentData { }
public struct UfoTag : IComponentData { }
public struct UfoBigTag : IComponentData { }
public struct PlayerBulletTag : IComponentData { }
public struct EnemyBulletTag : IComponentData { }
public struct DeadTag : IComponentData { }
```

From Assets/Scripts/ECS/Components/AgeData.cs:
```csharp
public struct AgeData : IComponentData { public int Age; }
```

From Assets/Scripts/ECS/Components/MoveData.cs:
```csharp
public struct MoveData : IComponentData
{
    public float2 Position;
    public float Speed;
    public float2 Direction;
}
```

From Assets/Scripts/ECS/Components/ShipPositionData.cs:
```csharp
public struct ShipPositionData : IComponentData { public float2 Position; }
```

From Assets/Scripts/ECS/Components/ScoreData.cs:
```csharp
public struct ScoreData : IComponentData { public int Value; }
```

From Assets/Scripts/ECS/EntityFactory.cs:
```csharp
public static Entity CreateShip(EntityManager em, float2 position, float moveSpeed, ...);
public static Entity CreateAsteroid(EntityManager em, float2 position, float speed, float2 direction, int age, int scoreValue);
public static Entity CreateBullet(EntityManager em, float2 position, float speed, float2 direction, float lifeTime, bool isPlayer);
public static Entity CreateUfoBig(EntityManager em, float2 position, float speed, float2 direction, int maxShoots, float reloadDuration, float shootToEvery, int scoreValue);
public static Entity CreateUfo(EntityManager em, float2 position, float speed, float2 direction, int maxShoots, float reloadDuration, float shootToEvery, float moveToEvery, int scoreValue);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: EntitiesCatalog -- убрать ModelFactory, добавить EntityType tracking, упростить Create* методы</name>
  <files>Assets/Scripts/Application/EntitiesCatalog.cs</files>
  <read_first>
    - Assets/Scripts/Application/EntitiesCatalog.cs
    - Assets/Scripts/Application/ModelFactory.cs
    - Assets/Scripts/ECS/EntityFactory.cs
    - Assets/Scripts/View/ShipVisual.cs
    - Assets/Scripts/View/AsteroidVisual.cs
    - Assets/Scripts/View/BulletVisual.cs
    - Assets/Scripts/View/UfoVisual.cs
  </read_first>
  <action>
Полный рефакторинг EntitiesCatalog.cs:

1. **Добавить enum EntityType** (внутри namespace SelStrom.Asteroids, вне класса или внутри):
   ```csharp
   public enum EntityType
   {
       Ship,
       Asteroid,
       Bullet,
       UfoBig,
       Ufo
   }
   ```

2. **Заменить словари на ECS-ориентированные:**
   - Убрать: `Dictionary<IGameEntityModel, IEntityView> _modelToVisual`
   - Убрать: `Dictionary<IEntityView, IGameEntityModel> _visualToModel`
   - Убрать: `Dictionary<GameObject, IGameEntityModel> _gameObjectToModel`
   - Убрать: `Dictionary<IGameEntityModel, EventBindingContext> _modelToBindings`
   - Оставить: `Dictionary<GameObject, Entity> _gameObjectToEntity`
   - Добавить: `Dictionary<GameObject, EntityType> _gameObjectToEntityType`
   - Добавить: `Dictionary<GameObject, EventBindingContext> _gameObjectToBindings`

3. **Убрать поле `_modelFactory`** и `_useEcs`.

4. **Изменить Connect():** убрать параметр `ModelFactory modelFactory`. Сигнатура: `Connect(GameData configs, ViewFactory viewFactory, EntityManager entityManager, CollisionBridge collisionBridge)`. Убрать отдельный ConnectEcs().

5. **Убрать TryFindModel<T>()** -- заменить на:
   ```csharp
   public bool TryGetEntityType(GameObject go, out EntityType entityType)
   {
       return _gameObjectToEntityType.TryGetValue(go, out entityType);
   }
   ```

6. **Рефакторить CreateShip():**
   - Убрать `_modelFactory.Get<ShipModel>()`, model.SetData и все настройки model.*
   - Создать entity напрямую через EntityFactory.CreateShip(...)
   - ViewModel и bindings привязать к ECS данным через ObservableBridgeSystem (как уже работает)
   - Bindings: создать EventBindingContext, НО не привязывать к model -- ObservableBridgeSystem уже синхронизирует position/rotation/thrust через ShipViewModel
   - Коллизия: привязать через CollisionBridge.ReportCollision (уже работает)
   - Зарегистрировать в `_gameObjectToEntity`, `_gameObjectToEntityType[go] = EntityType.Ship`, `_gameObjectToBindings`
   - Вернуть `void` вместо `ShipModel` (или оставить ViewFactory reference для view)

   **ВАЖНО:** CreateShip сейчас возвращает ShipModel. Нужно изменить на void. Все места, где используется ShipModel из CreateShip (Game.Start(), передача в CreateUfo/CreateBigUfo для ShootTo.Ship), нужно заменить:
   - `_shipModel = _catalog.CreateShip(OnShipCollided)` -- OnShipCollided callback заменить на callback через CollisionBridge (уже настроен)
   - `_shipModel.Gun.OnShooting = OnUserGunShooting` -- убрать (ECS GunShootEvent buffer уже обрабатывает)
   - `_shipModel.Laser.OnShooting = OnUserLaserShooting` -- убрать (ECS LaserShootEvent buffer уже обрабатывает)

   Новая сигнатура: `public void CreateShip()`

7. **Рефакторить CreateBullet():**
   - Убрать `_modelFactory.Get<BulletModel>()`, model.SetData
   - Создать entity через EntityFactory.CreateBullet(...)
   - ViewModel + bindings: BulletViewModel с position. Position будет синхронизирована через GameObjectSyncSystem (уже работает -- Transform обновляется из ECS). НО BulletVisual.Connect(viewModel) все еще нужен для collision callback.
   - Коллизия: CollisionBridge.ReportCollision
   - Убрать параметр `Action<BulletModel, Collision2D> onRegisterCollision` -- заменить на callback без модели
   - Новая сигнатура: `public void CreateBullet(GameData.BulletData data, GameObject prefab, Vector2 position, Vector2 direction)`

8. **Рефакторить CreateAsteroid():**
   - Убрать `_modelFactory.Get<AsteroidModel>()`, model.SetData
   - Entity через EntityFactory.CreateAsteroid(...)
   - ViewModel: AsteroidViewModel с sprite. Position sync через GameObjectSyncSystem.
   - Убрать bindings.From(model.Move.Position) -- не нужен, position обновляется через ECS -> Transform.

9. **Рефакторить CreateBigUfo() и CreateUfo():**
   - Убрать `_modelFactory.Get<UfoBigModel/UfoModel>()`, model.SetData
   - Убрать параметры `ShipModel ship`, `Action<UfoBigModel> onRegisterCollision`, `Action<GunComponent> onGunShooting`
   - Новые сигнатуры: `CreateBigUfo(Vector2 position, Vector2 direction)`, `CreateUfo(Vector2 position, Vector2 direction)`
   - Entity через EntityFactory

10. **ReleaseByGameObject():** убрать все обращения к _modelToVisual, _visualToModel, _gameObjectToModel, _modelFactory.Release. Использовать _gameObjectToBindings, _gameObjectToEntity. Вызвать _viewFactory.Release(go) для возврата GameObject в пул.

11. **Release(IGameEntityModel):** удалить целиком (больше не нужен -- ReleaseByGameObject покрывает).

12. **CleanUp():** рефакторить -- очистить _gameObjectToBindings, _gameObjectToEntity, _gameObjectToEntityType, уничтожить ECS entities, очистить CollisionBridge.

13. **AddToCatalog:** переписать:
    ```csharp
    private void AddToCatalog(GameObject go, Entity entity, EntityType type, EventBindingContext bindings)
    {
        _gameObjectToEntity[go] = entity;
        _gameObjectToEntityType[go] = type;
        _gameObjectToBindings[go] = bindings;
    }
    ```

14. **Добавить метод `public void ReleaseAllGameEntities()`** -- замена Model.CleanUp() для уничтожения всех entity и возврата GO в пул:
    ```csharp
    public void ReleaseAllGameEntities()
    {
        var gameObjects = new List<GameObject>(_gameObjectToEntity.Keys);
        foreach (var go in gameObjects)
        {
            ReleaseByGameObject(go);
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "ModelFactory\|IGameEntityModel\|_modelFactory\|TryFindModel" Assets/Scripts/Application/EntitiesCatalog.cs | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - EntitiesCatalog.cs не содержит `ModelFactory`, `IGameEntityModel`, `_modelFactory`, `TryFindModel`
    - EntitiesCatalog.cs содержит `enum EntityType`
    - EntitiesCatalog.cs содержит `Dictionary<GameObject, EntityType> _gameObjectToEntityType`
    - EntitiesCatalog.cs содержит `public bool TryGetEntityType`
    - CreateShip() возвращает void
    - CreateBullet() не принимает Action<BulletModel, Collision2D>
    - CreateUfo/CreateBigUfo не принимают ShipModel
    - Release(IGameEntityModel) удален
  </acceptance_criteria>
  <done>EntitiesCatalog полностью рефакторен -- без legacy-моделей, с EntityType tracking</done>
</task>

<task type="auto">
  <name>Task 2: Application.cs и Game.cs -- убрать legacy-модели, рефакторить OnDeadEntity и спавн</name>
  <files>Assets/Scripts/Application/Application.cs, Assets/Scripts/Application/Game.cs, Assets/Scripts/Application/Screens/GameScreen.cs</files>
  <read_first>
    - Assets/Scripts/Application/Application.cs (после Plan 01)
    - Assets/Scripts/Application/Game.cs (после Plan 01)
    - Assets/Scripts/Application/Screens/GameScreen.cs
    - Assets/Scripts/Application/EntitiesCatalog.cs (после Task 1)
    - Assets/Scripts/Bridge/ObservableBridgeSystem.cs
    - Assets/Scripts/ECS/Components/ShipPositionData.cs
    - Assets/Scripts/ECS/Components/ScoreData.cs
    - Assets/Scripts/ECS/Components/AgeData.cs
    - Assets/Scripts/ECS/Components/MoveData.cs
  </read_first>
  <action>
Рефакторинг Application.cs, Game.cs, GameScreen.cs для устранения зависимостей от legacy-моделей:

**Application.cs:**

1. **Убрать `_model` поле.** Заменить:
   - `_model = new Model { GameArea = new Vector2(sceneWidth, sceneHeight) };` -- убрать
   - `new ModelFactory(_model)` -- убрать (EntitiesCatalog больше не принимает ModelFactory)
   - `_catalog.Connect(_configs, new ModelFactory(_model), new ViewFactory(...))` заменить на `_catalog.Connect(_configs, new ViewFactory(...), em, _collisionBridge)`
   - `bridgeSystem.SetModel(_model)` -- убрать (ObservableBridgeSystem больше не нужен _model для score sync; Score будет читаться напрямую из ECS в GameScreen)
   - Убрать `_model = null` из Dispose()

2. **OnDeadEntity(GameObject go):** заменить TryFindModel на TryGetEntityType + ECS queries:
   ```csharp
   private void OnDeadEntity(GameObject go)
   {
       _collisionBridge.UnregisterMapping(go);
       var position = (Vector2)go.transform.position;

       if (_catalog.TryGetEntityType(go, out var entityType))
       {
           if (entityType == EntityType.Asteroid)
           {
               _game.PlayEffect(_configs.VfxBlowPrefab, position);
               // Получить age и speed из ECS entity (если ещё существует)
               if (_catalog.TryGetEntity(go, out var entity) && _entityManager.Exists(entity))
               {
                   var ageData = _entityManager.GetComponentData<AgeData>(entity);
                   var moveData = _entityManager.GetComponentData<MoveData>(entity);
                   var age = ageData.Age - 1;
                   if (age > 0)
                   {
                       var speed = Math.Min(moveData.Speed * 2, 10f);
                       _catalog.CreateAsteroid(age, position, speed);
                       _catalog.CreateAsteroid(age, position, speed);
                   }
               }
           }
           else if (entityType == EntityType.Ship)
           {
               _game.PlayEffect(_configs.VfxBlowPrefab, position);
               _game.StopFromEcs();
           }
           else if (entityType == EntityType.UfoBig || entityType == EntityType.Ufo)
           {
               _game.PlayEffect(_configs.VfxBlowPrefab, position);
           }
       }

       _catalog.ReleaseByGameObject(go);
   }
   ```

   **ВАЖНО:** Нужно сохранить `_entityManager` как поле Application (добавить `private EntityManager _entityManager;` и присвоить в Start()).

3. **Убрать ссылку на `using Model.Components;`** если есть.

**Game.cs:**

1. **Убрать `_shipModel` поле** и `_model` поле.

2. **Добавить `private EntityManager _entityManager;`** (принять через ConnectEcs или конструктор).

3. **Start():**
   - `_catalog.CreateShip()` -- void, убрать присвоение `_shipModel`
   - Убрать `_shipModel.Gun.OnShooting = ...` и `_shipModel.Laser.OnShooting = ...` -- ECS event buffers уже обрабатывают
   - Для начального спавна астероидов: `SpawnAsteroid(Vector2.zero)` вместо `SpawnAsteroid(_shipModel.Move.Position.Value)` -- корабль начинает в (0,0)
   - `_actionScheduler.ScheduleAction(SpawnNewEnemy, ...)` -- уже работает после Plan 01
   - GameScreenData: убрать `ShipModel = _shipModel`, `Model = _model`. Добавить `EntityManager = _entityManager` или функцию получения Score.

4. **SpawnNewEnemy():** заменить `_shipModel.Move.Position.Value` на чтение из ShipPositionData singleton:
   ```csharp
   private Vector2 GetShipPosition()
   {
       var query = _entityManager.CreateEntityQuery(typeof(ShipPositionData));
       if (query.CalculateEntityCount() > 0)
       {
           var data = query.GetSingleton<ShipPositionData>();
           return new Vector2(data.Position.x, data.Position.y);
       }
       return Vector2.zero;
   }
   ```
   Использовать `GetShipPosition()` в SpawnNewEnemy, SpawnUfo, SpawnBigUfo, SpawnAsteroid.

5. **SpawnUfo/SpawnBigUfo:** убрать параметр `_shipModel` -- каталог больше не принимает ShipModel. Вызов: `_catalog.CreateUfo(position, direction)`, `_catalog.CreateBigUfo(position, direction)`.

6. **Убрать legacy callbacks:**
   - `OnUfoCollided(UfoBigModel model)` -- удалить
   - `OnShipCollided(Collision2D obj)` -- удалить (корабль обрабатывается через DeadEntityCleanupSystem)
   - `OnUserBulletCollided(BulletModel, Collision2D)` -- удалить (обрабатывается через ECS CollisionHandler)
   - `OnEnemyBulletCollided(BulletModel, Collision2D)` -- удалить
   - `OnEntityDestroyed(IGameEntityModel)` -- удалить
   - `OnUserGunShooting(GunComponent)` -- удалить
   - `OnUserLaserShooting(LaserComponent)` -- удалить
   - `OnEnemyGunShooting(GunComponent)` -- удалить
   - `Kill(IGameEntityModel)` -- удалить

7. **Stop():** убрать `_model.ActionScheduler.ResetSchedule()` -- уже `_actionScheduler.ResetSchedule()` после Plan 01.

8. **StopFromEcs():** упростить -- Score sync теперь читается из GameScreenData. Но StopFromEcs по-прежнему вызывает Stop(). Убрать SyncEcsScoreToModel() если Score читается из ECS напрямую. Переименовать StopFromEcs() в StopGame() (уже нет разделения ECS/legacy).

9. **Restart():** заменить `_model.CleanUp()` на `_catalog.ReleaseAllGameEntities()` + `_actionScheduler.ResetSchedule()` + ClearEcsEventBuffers().

10. **ProcessShootEvents() -- лазерная часть:** заменить `_model.ReceiveScore(model)` на ничего -- ECS CollisionHandler уже начисляет score через DeadTag путь. Заменить `_catalog.TryFindModel<IGameEntityModel>(gameObject, out var model)` на проверку наличия entity через `_catalog.TryGetEntity(gameObject, out var entity)`.

11. **Убрать `_model.OnEntityDestroyed += OnEntityDestroyed;`** из конструктора.

**GameScreen.cs:**

1. **GameScreenData:** убрать `ShipModel` и `Model` поля. Добавить:
   ```csharp
   public int Score;
   public EntityManager EntityManager;
   ```

2. **ShowEndGame():** заменить `_data.Model.Score` на `_data.Score`. Score будет установлен перед вызовом ToggleState(EndGame) -- через StopFromEcs/Stop flow.

3. **OnSubmitClicked():** заменить `_data.Model.Score` на `_data.Score`.

4. **ActivateHud():** убрать `if (_data.UseEcs)` / `else` -- оставить только ECS path (ObservableBridgeSystem).

5. **DeactivateHud():** убрать `if (_data.UseEcs)` -- оставить только ECS path.

6. **Убрать `using Model.Components;`** если есть.

**Где берется Score для GameScreen:**
В Game.StopGame() (бывший StopFromEcs): перед вызовом Stop() прочитать ScoreData из ECS и записать в `_data.Score`:
```csharp
public void StopGame()
{
    var scoreQuery = _entityManager.CreateEntityQuery(typeof(ScoreData));
    if (scoreQuery.CalculateEntityCount() > 0)
    {
        var entity = scoreQuery.GetSingletonEntity();
        _data.Score = _entityManager.GetComponentData<ScoreData>(entity).Value;
    }
    Stop();
}
```
Для этого `_data` в Game нужно хранить как поле (не `readonly` struct) и GameScreen.Connect принимает ref или Game обновляет _gameScreen._data.Score через метод.

**Альтернативный подход:** GameScreen.ShowEndGame() сама читает ScoreData:
```csharp
private void ShowEndGame()
{
    var score = 0;
    var world = World.DefaultGameObjectInjectionWorld;
    if (world != null && world.IsCreated)
    {
        var em = world.EntityManager;
        var query = em.CreateEntityQuery(typeof(ScoreData));
        if (query.CalculateEntityCount() > 0)
        {
            score = em.GetComponentData<ScoreData>(query.GetSingletonEntity()).Value;
        }
    }
    var scoreVm = new ScoreViewModel();
    scoreVm.Score.Value = $"score: {score}";
    // ...
}
```
Этот подход проще -- GameScreen напрямую читает ECS singleton. Использовать этот вариант. Тогда GameScreenData не нужен Score/EntityManager -- просто убрать Model/ShipModel/UseEcs.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "ShipModel\|AsteroidModel\|BulletModel\|UfoBigModel\|UfoModel\|IGameEntityModel\|IGroupVisitor\|ModelFactory\|_shipModel\|_model" Assets/Scripts/Application/Application.cs Assets/Scripts/Application/Game.cs Assets/Scripts/Application/Screens/GameScreen.cs | grep ":0$" | wc -l | grep "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - Application.cs не содержит `_model`, `ShipModel`, `AsteroidModel`, `TryFindModel`, `ModelFactory`
    - Application.cs содержит `TryGetEntityType` и `EntityType.Asteroid`
    - Application.cs содержит `private EntityManager _entityManager`
    - Game.cs не содержит `_shipModel`, `_model`, `ShipModel`, `BulletModel`, `UfoBigModel`, `IGameEntityModel`, `GunComponent`, `LaserComponent`
    - Game.cs содержит `GetShipPosition()` с `ShipPositionData`
    - Game.cs метод `Kill(IGameEntityModel)` удален
    - Game.cs legacy callbacks (OnUfoCollided, OnShipCollided, OnUserBulletCollided, OnEnemyBulletCollided, OnEntityDestroyed, OnUserGunShooting, OnUserLaserShooting, OnEnemyGunShooting) удалены
    - GameScreen.cs не содержит `_data.Model`, `_data.ShipModel`, `_data.UseEcs`
    - GameScreenData struct не содержит `ShipModel`, `Model`, `UseEcs`
  </acceptance_criteria>
  <done>Application, Game и GameScreen полностью свободны от legacy-моделей. Весь доступ к данным через ECS.</done>
</task>

</tasks>

<verification>
- `grep -rn "ShipModel\|AsteroidModel\|BulletModel\|UfoBigModel\|UfoModel" Assets/Scripts/Application/` возвращает 0 результатов
- `grep -rn "IGameEntityModel\|IGroupVisitor\|IModelSystem" Assets/Scripts/Application/` возвращает 0 результатов
- `grep -rn "ModelFactory" Assets/Scripts/Application/` возвращает 0 результатов (кроме файла ModelFactory.cs который будет удален в Plan 03)
- `grep -rn "_data.Model\|_data.ShipModel\|_data.UseEcs" Assets/Scripts/Application/Screens/GameScreen.cs` возвращает 0 результатов
- `grep -rn "using Model.Components" Assets/Scripts/Application/` возвращает 0 результатов
</verification>

<success_criteria>
- EntitiesCatalog использует EntityType enum и прямые ECS entity для tracking
- OnDeadEntity работает через EntityType + ECS component queries
- SpawnNewEnemy читает позицию корабля из ShipPositionData ECS singleton
- GameScreen читает Score напрямую из ECS ScoreData singleton
- Все legacy callbacks удалены из Game.cs
- Код компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/06-legacy-cleanup/06-02-SUMMARY.md`
</output>
