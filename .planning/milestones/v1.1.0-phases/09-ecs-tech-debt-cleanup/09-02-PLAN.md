---
phase: 09-ecs-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/View/AsteroidVisual.cs
  - Assets/Scripts/View/BulletVisual.cs
  - Assets/Scripts/View/UfoVisual.cs
  - Assets/Scripts/View/ShipVisual.cs
  - Assets/Scripts/Bridge/ObservableBridgeSystem.cs
  - Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
autonomous: true
requirements:
  - TD-04
  - TD-05

must_haves:
  truths:
    - "Non-ship ViewModel классы не содержат dead Position binding"
    - "Ship Transform пишется одним путём -- через GameObjectSyncSystem"
    - "Переключение спрайтов thrust продолжает работать через ObservableBridgeSystem"
    - "HUD-данные продолжают обновляться через ObservableBridgeSystem"
  artifacts:
    - path: "Assets/Scripts/View/AsteroidVisual.cs"
      provides: "AsteroidViewModel без Position"
    - path: "Assets/Scripts/View/BulletVisual.cs"
      provides: "BulletViewModel без Position"
    - path: "Assets/Scripts/View/UfoVisual.cs"
      provides: "UfoViewModel без Position"
    - path: "Assets/Scripts/View/ShipVisual.cs"
      provides: "ShipViewModel без Position/Rotation, сохраняя Sprite и OnCollision"
    - path: "Assets/Scripts/Bridge/ObservableBridgeSystem.cs"
      provides: "Bridge без двойной записи Position/Rotation в ShipViewModel"
  key_links:
    - from: "GameObjectSyncSystem"
      to: "Transform (все entities)"
      via: "MoveData -> Transform.position, RotateData -> Transform.rotation"
      pattern: "goRef\\.Transform\\.position"
    - from: "ObservableBridgeSystem"
      to: "ShipViewModel.Sprite"
      via: "ThrustData.IsActive -> Sprite switch"
      pattern: "_shipViewModel\\.Sprite\\.Value"
---

<objective>
Удалить dead MVVM Position bindings из non-ship ViewModel и устранить двойную запись Ship Transform.

Purpose: Убрать мёртвый код -- Position никогда не устанавливается через MVVM, Transform пишется GameObjectSyncSystem.
Output: Чистые ViewModel без dead bindings, единый путь записи Ship Transform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ecs-tech-debt-cleanup/09-RESEARCH.md

<interfaces>
<!-- GameObjectSyncSystem уже пишет Position и Rotation в Transform для всех entities -->

From Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs:
```csharp
[UpdateInGroup(typeof(PresentationSystemGroup))]
public partial class GameObjectSyncSystem : SystemBase
{
    // Entities с MoveData + RotateData + GameObjectRef -> position + rotation
    // Entities с MoveData + GameObjectRef, без RotateData -> position only
}
```

From Assets/Scripts/Bridge/ObservableBridgeSystem.cs:
```csharp
[UpdateInGroup(typeof(PresentationSystemGroup))]
public partial class ObservableBridgeSystem : SystemBase
{
    public void SetShipViewModel(ShipViewModel viewModel, Sprite mainSprite, Sprite thrustSprite);
    public void SetHudData(HudData hudData);
    // OnUpdate: пишет HUD-данные + ShipViewModel.Position/Rotation/Sprite
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить dead Position binding из AsteroidViewModel, BulletViewModel, UfoViewModel</name>
  <files>
    Assets/Scripts/View/AsteroidVisual.cs,
    Assets/Scripts/View/BulletVisual.cs,
    Assets/Scripts/View/UfoVisual.cs
  </files>
  <read_first>
    Assets/Scripts/View/AsteroidVisual.cs,
    Assets/Scripts/View/BulletVisual.cs,
    Assets/Scripts/View/UfoVisual.cs
  </read_first>
  <action>
Из каждого файла удалить ReactiveValue Position и привязку Bind.From(ViewModel.Position).To(transform).

**AsteroidVisual.cs:**
- Удалить из AsteroidViewModel: `public readonly ReactiveValue<Vector2> Position = new();`
- Удалить из AsteroidVisual.OnConnected: `Bind.From(ViewModel.Position).To(transform);`
- Удалить `using SelStrom.Asteroids.Bindings;` -- больше не нужен (Bind из Bindings)
- Оставить: `ReactiveValue<Sprite> Sprite`, `ReactiveValue<Action<Collision2D>> OnCollision`, привязку Sprite

**BulletVisual.cs:**
- Удалить из BulletViewModel: `public readonly ReactiveValue<Vector2> Position = new();`
- Удалить из BulletVisual.OnConnected: `Bind.From(ViewModel.Position).To(transform);`
- Удалить `using SelStrom.Asteroids.Bindings;` -- больше не нужен
- Оставить: `ReactiveValue<Action<Collision2D>> OnCollision`, `_collider.enabled = true;`

**UfoVisual.cs:**
- Удалить из UfoViewModel: `public readonly ReactiveValue<Vector2> Position = new();`
- Удалить из UfoVisual.OnConnected: `Bind.From(ViewModel.Position).To(transform);` -- OnConnected станет пустым, оставить пустой метод
- Удалить `using SelStrom.Asteroids.Bindings;`
- Удалить `using UnityEngine;` если Vector2 больше не используется (проверить: Action<Collision2D> -- System.Action, Collision2D -- UnityEngine). UnityEngine нужен для Collision2D -- оставить.
- Оставить: `ReactiveValue<Action<Collision2D>> OnCollision`

Все три файла: `using Shtl.Mvvm;` оставить (ReactiveValue), `using UnityEngine;` оставить (Collision2D).
  </action>
  <verify>
    <automated>grep -c "Position" Assets/Scripts/View/AsteroidVisual.cs Assets/Scripts/View/BulletVisual.cs Assets/Scripts/View/UfoVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - AsteroidVisual.cs: 0 вхождений "Position"
    - BulletVisual.cs: 0 вхождений "Position"
    - UfoVisual.cs: 0 вхождений "Position"
    - AsteroidVisual.cs содержит `ReactiveValue<Sprite> Sprite`
    - BulletVisual.cs содержит `_collider.enabled = true`
    - UfoVisual.cs содержит `ReactiveValue<Action<Collision2D>> OnCollision`
    - Ни один файл не содержит `using SelStrom.Asteroids.Bindings`
  </acceptance_criteria>
  <done>Dead Position binding удалён из всех non-ship ViewModel. TD-04 закрыт.</done>
</task>

<task type="auto">
  <name>Task 2: Устранить двойную запись Ship Transform и обновить тесты</name>
  <files>
    Assets/Scripts/View/ShipVisual.cs,
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
  </files>
  <read_first>
    Assets/Scripts/View/ShipVisual.cs,
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs,
    Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
  </read_first>
  <action>
GameObjectSyncSystem уже пишет Position (из MoveData) и Rotation (из RotateData, через atan2->Quaternion.Euler) в Transform для всех entities с GameObjectRef, включая Ship. Двойная запись через ObservableBridgeSystem -> ShipViewModel -> ShipVisual не нужна.

**1. ShipViewModel (в ShipVisual.cs):**
- Удалить: `public readonly ReactiveValue<Vector2> Position = new();`
- Удалить: `public readonly ReactiveValue<Vector2> Rotation = new();`
- Оставить: `public readonly ReactiveValue<Sprite> Sprite = new();`
- Оставить: `public readonly ReactiveValue<Action<Collision2D>> OnCollision = new();`

**2. ShipVisual (в ShipVisual.cs):**
- Удалить из OnConnected: `Bind.From(ViewModel.Position).To(transform);`
- Удалить из OnConnected: `ViewModel.Rotation.Connect(OnRotationChanged);`
- Оставить: `ViewModel.Sprite.Connect(sprite => _spriteRenderer.sprite = sprite);`
- Удалить метод `OnRotationChanged` целиком (строки 27-31)
- Удалить `using SelStrom.Asteroids.Bindings;` -- Bind больше не используется
- `using UnityEngine;` оставить (SpriteRenderer, Sprite, Collision2D, SerializeField, Quaternion)

**3. ObservableBridgeSystem.cs:**
- Удалить строку 79: `_shipViewModel.Position.Value = new Vector2(pos.x, pos.y);`
- Удалить строки 81-82: `var rot = rotate.ValueRO.Rotation;` и `_shipViewModel.Rotation.Value = new Vector2(rot.x, rot.y);`
- Оставить блок sprite switch (строки 84-88) без изменений
- Переменная `pos` в блоке `_shipViewModel != null` больше не нужна -- удалить `var pos = move.ValueRO.Position;` из этого блока (строка 78)
- ВНИМАНИЕ: в блоке `_hudData != null` (строки 55-73) тоже есть `var pos` и `var rot` -- их НЕ трогать, они нужны для HUD

Результат блока `if (_shipViewModel != null)` после правки:
```csharp
if (_shipViewModel != null)
{
    if (_mainSprite != null && _thrustSprite != null)
    {
        _shipViewModel.Sprite.Value =
            thrust.ValueRO.IsActive ? _thrustSprite : _mainSprite;
    }
}
```

**4. ObservableBridgeSystemTests.cs:**
- Удалить тест `PushesPosition_ToShipViewModel` (строки 153-173)
- Удалить тест `PushesRotation_ToShipViewModel` (строки 175-195)
- Оставить все остальные тесты (HUD тесты, Sprite тесты, DoesNotCrash)
  </action>
  <verify>
    <automated>grep -c "Position\|Rotation" Assets/Scripts/View/ShipVisual.cs && grep -c "_shipViewModel.Position\|_shipViewModel.Rotation" Assets/Scripts/Bridge/ObservableBridgeSystem.cs</automated>
  </verify>
  <acceptance_criteria>
    - ShipVisual.cs: 0 вхождений "Position" (кроме как часть других слов, но ReactiveValue<Vector2> Position точно отсутствует)
    - ShipVisual.cs: не содержит `OnRotationChanged`
    - ShipVisual.cs: не содержит `Bind.From`
    - ShipVisual.cs: содержит `ViewModel.Sprite.Connect`
    - ObservableBridgeSystem.cs: 0 вхождений `_shipViewModel.Position` и `_shipViewModel.Rotation`
    - ObservableBridgeSystem.cs: содержит `_shipViewModel.Sprite.Value`
    - ObservableBridgeSystemTests.cs: не содержит `PushesPosition_ToShipViewModel`
    - ObservableBridgeSystemTests.cs: не содержит `PushesRotation_ToShipViewModel`
    - ObservableBridgeSystemTests.cs: содержит `ThrustActive_SetsThrustSprite`
  </acceptance_criteria>
  <done>Ship Transform пишется одним путём -- GameObjectSyncSystem. ObservableBridgeSystem обновляет только Sprite и HUD. TD-05 закрыт.</done>
</task>

</tasks>

<verification>
1. `grep -c "Position" Assets/Scripts/View/AsteroidVisual.cs Assets/Scripts/View/BulletVisual.cs Assets/Scripts/View/UfoVisual.cs` -- 0 в каждом файле
2. `grep "Bind.From" Assets/Scripts/View/ShipVisual.cs` -- 0 совпадений
3. `grep "_shipViewModel.Position\|_shipViewModel.Rotation" Assets/Scripts/Bridge/ObservableBridgeSystem.cs` -- 0 совпадений
4. `grep "_shipViewModel.Sprite" Assets/Scripts/Bridge/ObservableBridgeSystem.cs` -- 1 совпадение (sprite switch сохранён)
5. Все EditMode тесты проходят зелёным
</verification>

<success_criteria>
- TD-04: Non-ship ViewModel классы не содержат dead Position binding
- TD-05: Ship Transform пишется одним путём через GameObjectSyncSystem
- ObservableBridgeSystem сохраняет функциональность HUD и Sprite switch
- Все существующие тесты проходят зелёным
</success_criteria>

<output>
After completion, create `.planning/phases/09-ecs-tech-debt-cleanup/09-02-SUMMARY.md`
</output>
