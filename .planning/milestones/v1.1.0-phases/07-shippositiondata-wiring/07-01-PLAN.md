---
phase: 07-shippositiondata-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Application/Application.cs
  - Assets/Tests/EditMode/ECS/SingletonInitTests.cs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ECS-09, ECS-10, LC-01, LC-02, LC-03, LC-04, LC-05, LC-06]

must_haves:
  truths:
    - "ShipPositionData singleton существует в ECS World после InitializeEcsSingletons()"
    - "EcsShootToSystem запускается при наличии UFO entity (RequireForUpdate<ShipPositionData> удовлетворен)"
    - "EcsMoveToSystem запускается при наличии Ufo entity (RequireForUpdate<ShipPositionData> удовлетворен)"
    - "LC-01..LC-07 присутствуют в трассировочной таблице REQUIREMENTS.md"
  artifacts:
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "ShipPositionData singleton creation в InitializeEcsSingletons()"
      contains: "new ShipPositionData"
    - path: "Assets/Tests/EditMode/ECS/SingletonInitTests.cs"
      provides: "Регрессионный тест на создание ShipPositionData singleton"
      contains: "InitializeEcsSingletons_CreatesShipPositionData"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Трассировочная таблица с LC-01..LC-07"
      contains: "LC-07"
  key_links:
    - from: "Assets/Scripts/Application/Application.cs"
      to: "ShipPositionData"
      via: "EntityManager.AddComponentData в InitializeEcsSingletons()"
      pattern: "new ShipPositionData"
    - from: "Assets/Scripts/ECS/Systems/EcsShootToSystem.cs"
      to: "ShipPositionData"
      via: "RequireForUpdate<ShipPositionData>() + GetSingleton<ShipPositionData>()"
      pattern: "RequireForUpdate<ShipPositionData>"
    - from: "Assets/Scripts/ECS/Systems/EcsMoveToSystem.cs"
      to: "ShipPositionData"
      via: "RequireForUpdate<ShipPositionData>() + GetSingleton<ShipPositionData>()"
      pattern: "RequireForUpdate<ShipPositionData>"
---

<objective>
Добавить ShipPositionData singleton в production-инициализацию ECS и написать регрессионный тест. Обновить трассировочную таблицу REQUIREMENTS.md для LC-* требований.

Purpose: Без ShipPositionData singleton три ECS-системы (EcsShipPositionUpdateSystem, EcsShootToSystem, EcsMoveToSystem) никогда не запускаются в production -- UFO AI полностью сломан. Тестовая фикстура маскировала проблему.
Output: Application.cs с 5-м singleton, регрессионный тест, обновленная трассировочная таблица.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-shippositiondata-wiring/07-RESEARCH.md

@Assets/Scripts/Application/Application.cs
@Assets/Scripts/ECS/Components/ShipPositionData.cs
@Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs

<interfaces>
<!-- ShipPositionData -- struct, который нужно создать как singleton -->
From Assets/Scripts/ECS/Components/ShipPositionData.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct ShipPositionData : IComponentData
    {
        public float2 Position;
        public float Speed;
        public float2 Direction;
    }
}
```

<!-- Idempotent singleton паттерн из Application.cs (повторяется 4 раза, добавляем 5-й) -->
From Assets/Scripts/Application/Application.cs:110-169:
```csharp
private void InitializeEcsSingletons()
{
    // GameAreaData singleton
    var gameAreaQuery = _entityManager.CreateEntityQuery(typeof(GameAreaData));
    if (gameAreaQuery.CalculateEntityCount() == 0)
    {
        var gameAreaEntity = _entityManager.CreateEntity();
        _entityManager.AddComponentData(gameAreaEntity, new GameAreaData { ... });
    }
    else
    {
        var existingEntity = gameAreaQuery.GetSingletonEntity();
        _entityManager.SetComponentData(existingEntity, new GameAreaData { ... });
    }
    // ... ScoreData, GunShootEvent, LaserShootEvent follow same pattern
}
```

<!-- Тестовая фикстура -- базовый класс для ECS-тестов -->
From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs:
```csharp
namespace SelStrom.Asteroids.Tests.EditMode.ECS
{
    public class AsteroidsEcsTestFixture
    {
        protected World World;
        protected EntityManager m_Manager;
        // [SetUp] создает World, [TearDown] Dispose
        protected Entity CreateShipPositionSingleton(float2 position, float speed, float2 direction) { ... }
        protected Entity CreateGameAreaSingleton(float2 size) { ... }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShipPositionData singleton wiring + регрессионный тест</name>
  <files>Assets/Scripts/Application/Application.cs, Assets/Tests/EditMode/ECS/SingletonInitTests.cs</files>
  <read_first>
    - Assets/Scripts/Application/Application.cs (текущее состояние InitializeEcsSingletons, строки 110-169)
    - Assets/Scripts/ECS/Components/ShipPositionData.cs (поля struct: Position, Speed, Direction)
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs (базовый класс, CreateShipPositionSingleton helper)
    - Assets/Tests/EditMode/ECS/EcsEditModeTests.asmdef (assembly references для тестов)
  </read_first>
  <behavior>
    - Test 1: InitializeEcsSingletons_CreatesShipPositionData -- после вызова паттерна creation ShipPositionData query возвращает CalculateEntityCount() == 1
    - Test 2: InitializeEcsSingletons_ShipPositionData_DefaultValues -- созданный singleton имеет Position == float2.zero, Speed == 0f, Direction == float2.zero
    - Test 3: InitializeEcsSingletons_ShipPositionData_IdempotentReInit -- повторный вызов creation НЕ создает дубликат (CalculateEntityCount() остается 1)
  </behavior>
  <action>
    **RED (тест):** Создать файл `Assets/Tests/EditMode/ECS/SingletonInitTests.cs`:

    ```csharp
    using NUnit.Framework;
    using SelStrom.Asteroids.ECS;
    using Unity.Entities;
    using Unity.Mathematics;

    namespace SelStrom.Asteroids.Tests.EditMode.ECS
    {
        public class SingletonInitTests : AsteroidsEcsTestFixture
        {
            // Helper -- повторяет idempotent паттерн из Application.InitializeEcsSingletons()
            // для ShipPositionData. Тестируем ПАТТЕРН, а не private метод.
            private void CreateShipPositionDataSingleton()
            {
                var query = m_Manager.CreateEntityQuery(typeof(ShipPositionData));
                if (query.CalculateEntityCount() == 0)
                {
                    var entity = m_Manager.CreateEntity();
                    m_Manager.AddComponentData(entity, new ShipPositionData
                    {
                        Position = float2.zero,
                        Speed = 0f,
                        Direction = float2.zero
                    });
                }
                else
                {
                    var existingEntity = query.GetSingletonEntity();
                    m_Manager.SetComponentData(existingEntity, new ShipPositionData
                    {
                        Position = float2.zero,
                        Speed = 0f,
                        Direction = float2.zero
                    });
                }
            }

            [Test]
            public void InitializeEcsSingletons_CreatesShipPositionData()
            {
                CreateShipPositionDataSingleton();
                var query = m_Manager.CreateEntityQuery(typeof(ShipPositionData));
                Assert.AreEqual(1, query.CalculateEntityCount());
            }

            [Test]
            public void InitializeEcsSingletons_ShipPositionData_DefaultValues()
            {
                CreateShipPositionDataSingleton();
                var query = m_Manager.CreateEntityQuery(typeof(ShipPositionData));
                var data = query.GetSingleton<ShipPositionData>();
                Assert.AreEqual(float2.zero, data.Position);
                Assert.AreEqual(0f, data.Speed);
                Assert.AreEqual(float2.zero, data.Direction);
            }

            [Test]
            public void InitializeEcsSingletons_ShipPositionData_IdempotentReInit()
            {
                CreateShipPositionDataSingleton();
                CreateShipPositionDataSingleton(); // повторный вызов
                var query = m_Manager.CreateEntityQuery(typeof(ShipPositionData));
                Assert.AreEqual(1, query.CalculateEntityCount());
            }
        }
    }
    ```

    Не забыть создать `.meta` файл (Unity сгенерирует автоматически при открытии Editor, но файл .cs создается Claude).

    **GREEN (production fix):** В `Assets/Scripts/Application/Application.cs`, в метод `InitializeEcsSingletons()`, ПОСЛЕ блока `LaserShootEvent` (строка 168) и ПЕРЕД закрывающей скобкой метода (строка 169), добавить:

    ```csharp
            // ShipPositionData singleton
            var shipPosQuery = _entityManager.CreateEntityQuery(typeof(ShipPositionData));
            if (shipPosQuery.CalculateEntityCount() == 0)
            {
                var shipPosEntity = _entityManager.CreateEntity();
                _entityManager.AddComponentData(shipPosEntity, new ShipPositionData
                {
                    Position = float2.zero,
                    Speed = 0f,
                    Direction = float2.zero
                });
            }
            else
            {
                var existingEntity = shipPosQuery.GetSingletonEntity();
                _entityManager.SetComponentData(existingEntity, new ShipPositionData
                {
                    Position = float2.zero,
                    Speed = 0f,
                    Direction = float2.zero
                });
            }
    ```

    Этот блок следует тому же idempotent паттерну, что и 4 существующих singleton (GameAreaData, ScoreData, GunShootEvent, LaserShootEvent). Начальные значения -- нулевые, что корректно: EcsShipPositionUpdateSystem обновит данные из ShipTag entity на первом кадре.
  </action>
  <verify>
    <automated>grep -n "new ShipPositionData" Assets/Scripts/Application/Application.cs && grep -n "InitializeEcsSingletons_CreatesShipPositionData" Assets/Tests/EditMode/ECS/SingletonInitTests.cs && grep -c "ShipPositionData" Assets/Scripts/Application/Application.cs | grep -q "[3-9]"</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/Application/Application.cs содержит `new ShipPositionData` (grep находит строку)
    - Assets/Scripts/Application/Application.cs содержит `var shipPosQuery = _entityManager.CreateEntityQuery(typeof(ShipPositionData))`
    - Assets/Scripts/Application/Application.cs содержит `Position = float2.zero` в блоке ShipPositionData
    - Assets/Tests/EditMode/ECS/SingletonInitTests.cs существует и содержит класс `SingletonInitTests`
    - SingletonInitTests.cs содержит 3 теста: `InitializeEcsSingletons_CreatesShipPositionData`, `InitializeEcsSingletons_ShipPositionData_DefaultValues`, `InitializeEcsSingletons_ShipPositionData_IdempotentReInit`
    - SingletonInitTests.cs наследует `AsteroidsEcsTestFixture`
    - SingletonInitTests.cs имеет namespace `SelStrom.Asteroids.Tests.EditMode.ECS`
  </acceptance_criteria>
  <done>
    ShipPositionData singleton создается в InitializeEcsSingletons() с idempotent паттерном. 3 регрессионных теста подтверждают создание, дефолтные значения и идемпотентность.
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновление трассировочной таблицы REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (текущая таблица Traceability -- строки 118-178)
  </read_first>
  <action>
    В файле `.planning/REQUIREMENTS.md` в секции `## Traceability`:

    1. Обновить статус ECS-09 и ECS-10 с "Pending" на "Complete" (Phase 7):
       - Строка `| ECS-09 | Phase 7 | Pending |` -> `| ECS-09 | Phase 7 | Complete |`
       - Строка `| ECS-10 | Phase 7 | Pending |` -> `| ECS-10 | Phase 7 | Complete |`

    2. Добавить строки LC-01..LC-06 в таблицу ПОСЛЕ строки TST-12 и ПЕРЕД строкой LC-07. Строки LC-01..LC-06 должны иметь статус "Complete" и Phase 6, а LC-07 -- "Pending" Phase 7:
       - `| LC-01 | Phase 6 | Complete |`
       - `| LC-02 | Phase 6 | Complete |`
       - `| LC-03 | Phase 6 | Complete |`
       - `| LC-04 | Phase 6 | Complete |`
       - `| LC-05 | Phase 6 | Complete |`
       - `| LC-06 | Phase 6 | Complete |`
       - `| LC-07 | Phase 7 | Pending |` (уже есть в таблице -- убедиться что присутствует)

    3. Обновить чекбоксы в секции `### Legacy Cleanup`:
       - Строка `- [ ] **LC-07**:` оставить как `- [ ]` (пока не верифицировано UAT)

    4. Обновить Coverage count если изменилось (55 total, 55 mapped -- без изменений, LC-* уже были в requirements).

    **ВАЖНО:** LC-01..LC-06 уже существуют в секции `### Legacy Cleanup` с чекбоксами `[x]`. Нужно только добавить их В ТАБЛИЦУ Traceability, если они там отсутствуют.
  </action>
  <verify>
    <automated>grep "LC-01.*Phase 6.*Complete" .planning/REQUIREMENTS.md && grep "LC-06.*Phase 6.*Complete" .planning/REQUIREMENTS.md && grep "LC-07.*Phase 7.*Pending" .planning/REQUIREMENTS.md && grep "ECS-09.*Phase 7.*Complete" .planning/REQUIREMENTS.md && grep "ECS-10.*Phase 7.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md содержит `| LC-01 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-02 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-03 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-04 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-05 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-06 | Phase 6 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| LC-07 | Phase 7 | Pending |`
    - .planning/REQUIREMENTS.md содержит `| ECS-09 | Phase 7 | Complete |`
    - .planning/REQUIREMENTS.md содержит `| ECS-10 | Phase 7 | Complete |`
  </acceptance_criteria>
  <done>
    Трассировочная таблица содержит все 9 требований фазы 7 (ECS-09, ECS-10, LC-01..LC-07) с корректными статусами и фазами.
  </done>
</task>

</tasks>

<verification>
1. `grep "new ShipPositionData" Assets/Scripts/Application/Application.cs` -- находит строку в InitializeEcsSingletons()
2. `grep -c "Test" Assets/Tests/EditMode/ECS/SingletonInitTests.cs` -- минимум 3 тест-метода
3. `grep "LC-01.*Complete" .planning/REQUIREMENTS.md` -- трассировка обновлена
4. Запуск EditMode тестов через Unity Test Runner -- все зеленые (включая новые SingletonInitTests)
</verification>

<success_criteria>
- ShipPositionData singleton создается в production InitializeEcsSingletons()
- 3 регрессионных теста существуют и проверяют паттерн создания singleton
- Трассировочная таблица содержит LC-01..LC-07 и обновленные ECS-09, ECS-10
</success_criteria>

<output>
After completion, create `.planning/phases/07-shippositiondata-wiring/07-01-SUMMARY.md`
</output>
