---
phase: 05-bridge-layer-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/ECS/Bridge/CollisionBridge.cs
  - Assets/Scripts/ECS/Systems/ObservableBridgeSystem.cs
  - Assets/Scripts/ECS/Systems/DeadEntityCleanupSystem.cs
  - Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs
  - Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
  - Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs
autonomous: true
requirements:
  - BRG-03
  - BRG-04
  - BRG-05
  - TST-10
must_haves:
  truths:
    - "CollisionBridge принимает два GameObject из OnCollisionEnter2D, разрешает их в Entity через Dictionary маппинг и записывает CollisionEventData в DynamicBuffer"
    - "ObservableBridgeSystem пушит ECS-данные (позиция, скорость, ротация, лазер) в ReactiveValue HudData каждый кадр"
    - "DeadEntityCleanupSystem находит entities с DeadTag+GameObjectRef, вызывает callback для Release и уничтожает Entity"
    - "EditMode тесты покрывают все три bridge-системы"
  artifacts:
    - path: "Assets/Scripts/ECS/Bridge/CollisionBridge.cs"
      provides: "Utility для проброса Physics2D коллизий в ECS"
      contains: "class CollisionBridge"
    - path: "Assets/Scripts/ECS/Systems/ObservableBridgeSystem.cs"
      provides: "Managed ISystem для ECS->MVVM"
      contains: "class ObservableBridgeSystem"
    - path: "Assets/Scripts/ECS/Systems/DeadEntityCleanupSystem.cs"
      provides: "Cleanup-система для уничтожения dead entities"
      contains: "class DeadEntityCleanupSystem"
    - path: "Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs"
      provides: "EditMode тесты CollisionBridge"
      contains: "class CollisionBridgeTests"
    - path: "Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs"
      provides: "EditMode тесты ObservableBridgeSystem"
      contains: "class ObservableBridgeSystemTests"
    - path: "Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs"
      provides: "EditMode тесты DeadEntityCleanupSystem"
      contains: "class DeadEntityCleanupSystemTests"
  key_links:
    - from: "CollisionBridge.ReportCollision"
      to: "DynamicBuffer<CollisionEventData>"
      via: "EntityManager.GetBuffer + buffer.Add"
      pattern: "CollisionEventData"
    - from: "ObservableBridgeSystem"
      to: "HudData (ReactiveValue)"
      via: "SystemAPI.Query<ShipTag> -> push to ReactiveValue"
      pattern: "HudData"
    - from: "DeadEntityCleanupSystem"
      to: "EntitiesCatalog.Release (через callback)"
      via: "Query<DeadTag, GameObjectRef>"
      pattern: "DeadTag.*GameObjectRef"
---

<objective>
CollisionBridge, ObservableBridgeSystem, DeadEntityCleanupSystem и их EditMode тесты.

Purpose: Создать три оставшихся bridge-компонента: (1) CollisionBridge -- проксирует Physics2D коллизии из MonoBehaviour в ECS CollisionEventData буфер, (2) ObservableBridgeSystem -- транслирует ECS-данные в ReactiveValue для MVVM UI (HUD), (3) DeadEntityCleanupSystem -- обнаруживает entities с DeadTag и освобождает связанные GameObjects. Все три покрываются EditMode тестами (TST-10).

Output: CollisionBridge, ObservableBridgeSystem, DeadEntityCleanupSystem, 3 тестовых файла.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bridge-layer-integration/05-CONTEXT.md
@.planning/phases/05-bridge-layer-integration/05-RESEARCH.md

<interfaces>
<!-- ECS Components needed by bridge systems -->
From Assets/Scripts/ECS/Components/CollisionEventData.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct CollisionEventData : IBufferElementData
    {
        public Entity EntityA;
        public Entity EntityB;
    }
}
```

From Assets/Scripts/ECS/Components/MoveData.cs:
```csharp
public struct MoveData : IComponentData { public float2 Position; public float Speed; public float2 Direction; }
```

From Assets/Scripts/ECS/Components/RotateData.cs:
```csharp
public struct RotateData : IComponentData { public const float DegreePerSecond = 90f; public float TargetDirection; public float2 Rotation; }
```

From Assets/Scripts/ECS/Components/LaserData.cs:
```csharp
public struct LaserData : IComponentData { public int MaxShoots; public float UpdateDurationSec; public int CurrentShoots; public float ReloadRemaining; public bool Shooting; public float2 Direction; public float2 ShootPosition; }
```

From Assets/Scripts/ECS/Components/ThrustData.cs:
```csharp
public struct ThrustData : IComponentData { public float UnitsPerSecond; public float MaxSpeed; public bool IsActive; }
```

From Assets/Scripts/ECS/Components/ScoreData.cs:
```csharp
public struct ScoreData : IComponentData { public int Value; }
public struct ScoreValue : IComponentData { public int Score; }
```

From Assets/Scripts/ECS/Components/Tags/DeadTag.cs:
```csharp
public struct DeadTag : IComponentData { }
```

From Assets/Scripts/ECS/Components/Tags/ShipTag.cs:
```csharp
public struct ShipTag : IComponentData { }
```

<!-- MVVM types that ObservableBridge pushes to -->
From Assets/Scripts/View/HudVisual.cs:
```csharp
namespace SelStrom.Asteroids
{
    public sealed class HudData : AbstractViewModel
    {
        public readonly ReactiveValue<string> Coordinates = new();
        public readonly ReactiveValue<string> RotationAngle = new();
        public readonly ReactiveValue<string> Speed = new();
        public readonly ReactiveValue<string> LaserShootCount = new();
        public readonly ReactiveValue<string> LaserReloadTime = new();
        public readonly ReactiveValue<bool> IsLaserReloadTimeVisible = new();
    }
}
```

From Assets/Scripts/View/ShipVisual.cs:
```csharp
namespace SelStrom.Asteroids
{
    public class ShipViewModel : AbstractViewModel
    {
        public readonly ReactiveValue<Vector2> Position = new();
        public readonly ReactiveValue<Vector2> Rotation = new();
        public readonly ReactiveValue<Sprite> Sprite = new();
        public readonly ReactiveValue<Action<Collision2D>> OnCollision = new();
    }
}
```

From Assets/Scripts/Application/Screens/GameScreen.cs (formatting callbacks):
```csharp
// Форматирование значений для HUD:
// Position -> $"Coordinates: {position.ToString("F1")}"
// Speed -> $"Speed: {speed.ToString("F1", CultureInfo.InvariantCulture)} points/sec"
// Rotation -> $"Rotation: {(Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg).ToString("F1", CultureInfo.InvariantCulture)} degrees"
// LaserShoots -> $"Laser shoots: {shoots.ToString()}"
// LaserReload -> $"Reload laser: {TimeSpan.FromSeconds((int)timeRemaining):%s} sec"
// IsLaserReloadTimeVisible -> shoots < maxShoots
```

From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs:
```csharp
public class AsteroidsEcsTestFixture
{
    protected World World;
    protected EntityManager m_Manager;
    // ... хелперы CreateShipEntity, CreateAsteroidEntity, etc.
    protected Entity CreateCollisionEventSingleton()
    {
        var entity = m_Manager.CreateEntity();
        m_Manager.AddBuffer<CollisionEventData>(entity);
        return entity;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CollisionBridge utility и тесты</name>
  <files>
    Assets/Scripts/ECS/Bridge/CollisionBridge.cs,
    Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/Components/CollisionEventData.cs,
    Assets/Scripts/View/ShipVisual.cs,
    Assets/Scripts/View/BulletVisual.cs,
    Assets/Scripts/View/UfoVisual.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
  </read_first>
  <behavior>
    - RegisterMapping(go, entity) сохраняет маппинг GameObject->Entity
    - UnregisterMapping(go) удаляет маппинг
    - ReportCollision(selfGo, otherGo): если оба GameObject зарегистрированы -> добавляет CollisionEventData(EntityA=selfEntity, EntityB=otherEntity) в singleton buffer
    - ReportCollision(selfGo, otherGo): если один из GameObject не зарегистрирован -> ничего не делает (без исключения)
    - ReportCollision: после вызова buffer.Length увеличивается на 1
    - Initialize(entityManager) устанавливает EntityManager и создает/находит singleton buffer entity
  </behavior>
  <action>
    1. Создать директорию `Assets/Scripts/ECS/Bridge/` (будет в Asteroids assembly -- нет asmdef в этой папке, ближайший Asteroids.asmdef в Assets/).

    ВАЖНО: CollisionBridge размещается НЕ в Assets/Scripts/ECS/ (который имеет свой AsteroidsECS.asmdef), а в НОВОЙ директории внутри Assets/Scripts/ -- например `Assets/Scripts/Bridge/CollisionBridge.cs`. Причина: файлы в Assets/Scripts/ECS/ принадлежат AsteroidsECS assembly, который не имеет ссылки на Asteroids. Bridge-классы должны быть в Asteroids assembly для доступа к EntitiesCatalog и MVVM-типам.

    Альтернатива (проще): разместить в `Assets/Scripts/ECS/Bridge/` но тогда нужно убедиться, что AsteroidsECS.asmdef НЕ покрывает эту директорию. НО asmdef покрывает все вложенные папки автоматически. Поэтому правильный путь: `Assets/Scripts/Bridge/CollisionBridge.cs` -- вне AsteroidsECS.asmdef scope.

    2. **CollisionBridge.cs** -- создать в `Assets/Scripts/Bridge/CollisionBridge.cs` (в Asteroids assembly, namespace SelStrom.Asteroids):
    ```csharp
    using System.Collections.Generic;
    using SelStrom.Asteroids.ECS;
    using Unity.Entities;
    using UnityEngine;

    namespace SelStrom.Asteroids
    {
        public class CollisionBridge
        {
            private readonly Dictionary<GameObject, Entity> _goToEntity = new();
            private EntityManager _entityManager;
            private Entity _collisionBufferEntity;

            public void Initialize(EntityManager entityManager, Entity collisionBufferEntity)
            {
                _entityManager = entityManager;
                _collisionBufferEntity = collisionBufferEntity;
            }

            public void RegisterMapping(GameObject go, Entity entity)
            {
                _goToEntity[go] = entity;
            }

            public void UnregisterMapping(GameObject go)
            {
                _goToEntity.Remove(go);
            }

            public void ReportCollision(GameObject selfGo, GameObject otherGo)
            {
                if (!_goToEntity.TryGetValue(selfGo, out var selfEntity))
                {
                    return;
                }

                if (!_goToEntity.TryGetValue(otherGo, out var otherEntity))
                {
                    return;
                }

                var buffer = _entityManager.GetBuffer<CollisionEventData>(_collisionBufferEntity);
                buffer.Add(new CollisionEventData
                {
                    EntityA = selfEntity,
                    EntityB = otherEntity
                });
            }

            public void Clear()
            {
                _goToEntity.Clear();
            }
        }
    }
    ```
    Per D-02: Dictionary<GameObject, Entity> для O(1) lookup. Per D-08: вызывается из OnCollisionEnter2D визуалов.

    3. **CollisionBridgeTests.cs** -- создать тесты:
    - `ReportCollision_BothRegistered_AddsEventToBuffer`: создать 2 entity, 2 GameObject, зарегистрировать маппинг, вызвать ReportCollision -> buffer.Length == 1, EntityA и EntityB соответствуют
    - `ReportCollision_SelfNotRegistered_DoesNothing`: ReportCollision с незарегистрированным self -> buffer.Length == 0
    - `ReportCollision_OtherNotRegistered_DoesNothing`: ReportCollision с незарегистрированным other -> buffer.Length == 0
    - `UnregisterMapping_PreventsCollision`: зарегистрировать, отменить регистрацию, вызвать ReportCollision -> buffer.Length == 0
    - `MultipleCollisions_AddMultipleEvents`: три коллизии -> buffer.Length == 3

    Тесты наследуют AsteroidsEcsTestFixture для доступа к World/EntityManager. Создают реальные GameObject через `new GameObject("test")` и `Object.DestroyImmediate` в TearDown.
  </action>
  <verify>
    <automated>grep -r "class CollisionBridge" Assets/Scripts/Bridge/CollisionBridge.cs && grep -r "class CollisionBridgeTests" Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs && grep -c "\[Test\]" Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - CollisionBridge.cs находится в Assets/Scripts/Bridge/ (Asteroids assembly)
    - CollisionBridge.cs содержит `Dictionary<GameObject, Entity> _goToEntity`
    - CollisionBridge.cs содержит методы Initialize, RegisterMapping, UnregisterMapping, ReportCollision, Clear
    - CollisionBridge.cs содержит `buffer.Add(new CollisionEventData`
    - CollisionBridgeTests.cs содержит минимум 4 теста с [Test] атрибутом
    - CollisionBridgeTests.cs наследует AsteroidsEcsTestFixture
    - CollisionBridgeTests.cs содержит `new GameObject(` для создания тестовых объектов
  </acceptance_criteria>
  <done>
    CollisionBridge проксирует Physics2D коллизии из GameObject в ECS CollisionEventData буфер через Dictionary<GameObject, Entity> маппинг. Тесты покрывают: успешная коллизия, незарегистрированные объекты, отмена регистрации, множественные коллизии.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ObservableBridgeSystem, DeadEntityCleanupSystem и тесты</name>
  <files>
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs,
    Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs,
    Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Scripts/ECS/Components/RotateData.cs,
    Assets/Scripts/ECS/Components/LaserData.cs,
    Assets/Scripts/ECS/Components/ThrustData.cs,
    Assets/Scripts/ECS/Components/ScoreData.cs,
    Assets/Scripts/ECS/Components/Tags/ShipTag.cs,
    Assets/Scripts/ECS/Components/Tags/DeadTag.cs,
    Assets/Scripts/View/HudVisual.cs,
    Assets/Scripts/View/ShipVisual.cs,
    Assets/Scripts/Application/Screens/GameScreen.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
  </read_first>
  <behavior>
    ObservableBridgeSystem:
    - При наличии Ship entity с MoveData(Position=(5.5,3.2), Speed=7.8) -> HudData.Coordinates.Value == "Coordinates: (5.5, 3.2)", HudData.Speed.Value == "Speed: 7.8 points/sec"
    - При наличии Ship entity с RotateData(Rotation=(0,1)) -> HudData.RotationAngle.Value == "Rotation: 90.0 degrees"
    - При наличии Ship entity с LaserData(CurrentShoots=2, ReloadRemaining=3.0, MaxShoots=3) -> HudData.LaserShootCount.Value == "Laser shoots: 2", HudData.IsLaserReloadTimeVisible.Value == true
    - При наличии Ship entity с ThrustData(IsActive=true) -> ShipViewModel.Sprite == ThrustSprite (через callback)
    - Без установленных HudData/ShipViewModel -> не крашится (ранний выход)

    DeadEntityCleanupSystem:
    - Entity с DeadTag + GameObjectRef -> callback вызывается с goRef.GameObject, entity уничтожается
    - Entity с DeadTag без GameObjectRef -> entity уничтожается, callback не вызывается
    - Entity без DeadTag + GameObjectRef -> ничего не происходит
    - Несколько dead entities -> все обрабатываются за один Update
  </behavior>
  <action>
    ВАЖНО: ObservableBridgeSystem и DeadEntityCleanupSystem размещаются в `Assets/Scripts/Bridge/` (Asteroids assembly), а НЕ в `Assets/Scripts/ECS/Systems/` (AsteroidsECS assembly), потому что они используют managed-типы из Asteroids assembly (HudData, ShipViewModel, ReactiveValue).

    1. **ObservableBridgeSystem.cs** -- создать в `Assets/Scripts/Bridge/ObservableBridgeSystem.cs`:
    ```csharp
    using System;
    using System.Globalization;
    using SelStrom.Asteroids.ECS;
    using Unity.Entities;
    using Unity.Mathematics;
    using UnityEngine;

    namespace SelStrom.Asteroids
    {
        [UpdateInGroup(typeof(PresentationSystemGroup))]
        public partial class ObservableBridgeSystem : SystemBase
        {
            private HudData _hudData;
            private ShipViewModel _shipViewModel;
            private int _laserMaxShoots;
            private Sprite _mainSprite;
            private Sprite _thrustSprite;

            public void SetHudData(HudData hudData)
            {
                _hudData = hudData;
            }

            public void SetShipViewModel(ShipViewModel viewModel, Sprite mainSprite, Sprite thrustSprite)
            {
                _shipViewModel = viewModel;
                _mainSprite = mainSprite;
                _thrustSprite = thrustSprite;
            }

            public void SetLaserMaxShoots(int maxShoots)
            {
                _laserMaxShoots = maxShoots;
            }

            public void ClearReferences()
            {
                _hudData = null;
                _shipViewModel = null;
                _mainSprite = null;
                _thrustSprite = null;
            }

            protected override void OnUpdate()
            {
                if (_hudData == null && _shipViewModel == null)
                {
                    return;
                }

                foreach (var (move, rotate, thrust, laser) in
                         SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, RefRO<ThrustData>, RefRO<LaserData>>()
                             .WithAll<ShipTag>())
                {
                    if (_hudData != null)
                    {
                        var pos = move.ValueRO.Position;
                        _hudData.Coordinates.Value =
                            $"Coordinates: ({pos.x.ToString("F1", CultureInfo.InvariantCulture)}, {pos.y.ToString("F1", CultureInfo.InvariantCulture)})";

                        _hudData.Speed.Value =
                            $"Speed: {move.ValueRO.Speed.ToString("F1", CultureInfo.InvariantCulture)} points/sec";

                        var rot = rotate.ValueRO.Rotation;
                        var angle = math.atan2(rot.y, rot.x) * Mathf.Rad2Deg;
                        _hudData.RotationAngle.Value =
                            $"Rotation: {angle.ToString("F1", CultureInfo.InvariantCulture)} degrees";

                        var shoots = laser.ValueRO.CurrentShoots;
                        _hudData.LaserShootCount.Value = $"Laser shoots: {shoots.ToString()}";
                        _hudData.LaserReloadTime.Value =
                            $"Reload laser: {TimeSpan.FromSeconds((int)laser.ValueRO.ReloadRemaining):%s} sec";
                        _hudData.IsLaserReloadTimeVisible.Value = shoots < _laserMaxShoots;
                    }

                    if (_shipViewModel != null)
                    {
                        var pos = move.ValueRO.Position;
                        _shipViewModel.Position.Value = new Vector2(pos.x, pos.y);

                        var rot = rotate.ValueRO.Rotation;
                        _shipViewModel.Rotation.Value = new Vector2(rot.x, rot.y);

                        if (_mainSprite != null && _thrustSprite != null)
                        {
                            _shipViewModel.Sprite.Value =
                                thrust.ValueRO.IsActive ? _thrustSprite : _mainSprite;
                        }
                    }
                }
            }
        }
    }
    ```
    Per D-10, D-11, D-12: пушит ECS-данные в ReactiveValue каждый кадр. Форматирование строк 1:1 с GameScreen.ActivateHud() callbacks.

    ПРИМЕЧАНИЕ: Форматирование координат `position.ToString("F1")` в оригинале выдает `(X.X, Y.X)` формат через Vector2.ToString. Здесь мы эмулируем то же через float форматирование. Нужно проверить, что формат совпадает с оригиналом: `new Vector2(5.5f, 3.2f).ToString("F1")` -> `(5.5, 3.2)`. Поэтому в action используем прямое форматирование `$"Coordinates: ({pos.x:F1}, {pos.y:F1})"` -- но InvariantCulture нужен для десятичной точки.

    2. **DeadEntityCleanupSystem.cs** -- создать в `Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs`:
    ```csharp
    using System;
    using SelStrom.Asteroids.ECS;
    using Unity.Collections;
    using Unity.Entities;
    using UnityEngine;

    namespace SelStrom.Asteroids
    {
        [UpdateInGroup(typeof(LateSimulationSystemGroup))]
        public partial class DeadEntityCleanupSystem : SystemBase
        {
            private Action<GameObject> _onDeadEntity;

            public void SetOnDeadEntityCallback(Action<GameObject> callback)
            {
                _onDeadEntity = callback;
            }

            protected override void OnUpdate()
            {
                var ecb = new EntityCommandBuffer(Allocator.Temp);

                // Entities с DeadTag + GameObjectRef -> cleanup + уничтожение
                foreach (var (goRef, entity) in
                         SystemAPI.Query<GameObjectRef>()
                             .WithAll<DeadTag>()
                             .WithEntityAccess())
                {
                    _onDeadEntity?.Invoke(goRef.GameObject);
                    ecb.RemoveComponent<GameObjectRef>(entity);
                    ecb.DestroyEntity(entity);
                }

                // Entities с DeadTag без GameObjectRef -> просто уничтожение
                foreach (var entity in
                         SystemAPI.Query<RefRO<DeadTag>>()
                             .WithNone<GameObjectRef>()
                             .WithEntityAccess())
                {
                    ecb.DestroyEntity(entity.Item2);
                }

                ecb.Playback(EntityManager);
                ecb.Dispose();
            }
        }
    }
    ```
    Per D-14: DeadTag триггерит cleanup. Callback `_onDeadEntity` вызывается с GameObject для возврата в пул через EntitiesCatalog. `RemoveComponent<GameObjectRef>` перед DestroyEntity -- для ICleanupComponentData нужно явное удаление, иначе entity не уничтожится.

    ВАЖНО: ICleanupComponentData предотвращает DestroyEntity от фактического удаления entity. Нужно сначала RemoveComponent<GameObjectRef>, затем DestroyEntity. Либо можно использовать обычный class IComponentData вместо ICleanupComponentData. Per D-01 решение -- ICleanupComponentData, поэтому используем RemoveComponent перед Destroy.

    3. **ObservableBridgeSystemTests.cs** -- создать тесты. Поскольку ObservableBridgeSystem -- managed SystemBase, используем `World.CreateSystemManaged<ObservableBridgeSystem>()`. Тесты:
    - `PushesCoordinates_ToHudData`: ship entity с MoveData(5.5f, 3.2f) -> _hudData.Coordinates.Value содержит "5.5" и "3.2"
    - `PushesSpeed_ToHudData`: ship entity с MoveData Speed=7.8f -> _hudData.Speed.Value содержит "7.8"
    - `PushesRotation_ToHudData`: ship entity с RotateData(0,1) -> _hudData.RotationAngle.Value содержит "90.0"
    - `PushesLaserData_ToHudData`: ship entity с LaserData(CurrentShoots=2, MaxShoots=3) -> _hudData.LaserShootCount.Value == "Laser shoots: 2", IsLaserReloadTimeVisible == true
    - `PushesPosition_ToShipViewModel`: ship entity с MoveData(1,2) -> _shipViewModel.Position.Value == (1,2)
    - `DoesNotCrash_WhenNoHudDataSet`: system.Update() без SetHudData -> нет исключения

    Каждый тест: создать ship entity (CreateShipEntity из fixture), задать нужные компоненты через m_Manager.SetComponentData, вызвать system.SetHudData(new HudData()), system.Update(), проверить значения.

    4. **DeadEntityCleanupSystemTests.cs** -- создать тесты:
    - `CleansUp_EntityWithDeadTagAndGameObjectRef`: entity с DeadTag + GameObjectRef -> callback вызван с правильным GameObject, entity уничтожен
    - `Destroys_EntityWithDeadTagWithoutGameObjectRef`: entity с DeadTag без GameObjectRef -> entity уничтожен, callback не вызван
    - `Ignores_EntityWithoutDeadTag`: entity с GameObjectRef без DeadTag -> entity жив, callback не вызван
    - `HandlesMultipleDeadEntities`: 3 dead entities -> все 3 обработаны

    Для теста callback: использовать List<GameObject> и проверять count/содержимое.
  </action>
  <verify>
    <automated>grep -r "class CollisionBridge" Assets/Scripts/Bridge/CollisionBridge.cs && grep -r "class ObservableBridgeSystem" Assets/Scripts/Bridge/ObservableBridgeSystem.cs && grep -r "class DeadEntityCleanupSystem" Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs && grep -c "\[Test\]" Assets/Tests/EditMode/ECS/CollisionBridgeTests.cs && grep -c "\[Test\]" Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs && grep -c "\[Test\]" Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - CollisionBridge.cs в Assets/Scripts/Bridge/, namespace SelStrom.Asteroids
    - CollisionBridge.cs содержит `Dictionary<GameObject, Entity> _goToEntity` и метод `ReportCollision`
    - ObservableBridgeSystem.cs содержит `public partial class ObservableBridgeSystem : SystemBase`
    - ObservableBridgeSystem.cs содержит `[UpdateInGroup(typeof(PresentationSystemGroup))]`
    - ObservableBridgeSystem.cs содержит методы SetHudData, SetShipViewModel, SetLaserMaxShoots, ClearReferences
    - ObservableBridgeSystem.cs содержит `SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, RefRO<ThrustData>, RefRO<LaserData>>().WithAll<ShipTag>()`
    - DeadEntityCleanupSystem.cs содержит `public partial class DeadEntityCleanupSystem : SystemBase`
    - DeadEntityCleanupSystem.cs содержит `[UpdateInGroup(typeof(LateSimulationSystemGroup))]`
    - DeadEntityCleanupSystem.cs содержит `WithAll<DeadTag>()` и `ecb.RemoveComponent<GameObjectRef>` и `ecb.DestroyEntity`
    - CollisionBridgeTests.cs содержит минимум 4 теста
    - ObservableBridgeSystemTests.cs содержит минимум 5 тестов
    - DeadEntityCleanupSystemTests.cs содержит минимум 3 теста
  </acceptance_criteria>
  <done>
    CollisionBridge проксирует Physics2D->ECS, ObservableBridgeSystem транслирует ECS->MVVM для HUD, DeadEntityCleanupSystem уничтожает dead entities с возвратом GameObjects. Все три покрыты EditMode тестами.
  </done>
</task>

</tasks>

<verification>
- CollisionBridge в Assets/Scripts/Bridge/ разрешает GameObject->Entity и записывает CollisionEventData
- ObservableBridgeSystem пушит данные в HudData и ShipViewModel
- DeadEntityCleanupSystem очищает entities с DeadTag+GameObjectRef
- EditMode тесты покрывают все три bridge-компонента (TST-10)
- Все bridge-системы в Asteroids assembly (не AsteroidsECS) для доступа к managed-типам
</verification>

<success_criteria>
- CollisionBridge, ObservableBridgeSystem, DeadEntityCleanupSystem созданы и тестопокрыты
- Все bridge-системы размещены в Asteroids assembly (Assets/Scripts/Bridge/)
- EditMode тесты (TST-10): минимум 12 тестов суммарно по трём системам
- Формат HUD-строк соответствует оригиналу из GameScreen.cs
</success_criteria>

<output>
After completion, create `.planning/phases/05-bridge-layer-integration/05-02-SUMMARY.md`
</output>
