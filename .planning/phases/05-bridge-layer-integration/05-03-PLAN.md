---
phase: 05-bridge-layer-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/Game.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
  - Assets/Scripts/View/ShipVisual.cs
  - Assets/Scripts/View/BulletVisual.cs
  - Assets/Scripts/View/UfoVisual.cs
  - Assets/Scripts/ECS/EntityFactory.cs
  - Assets/Tests/PlayMode/PlayModeTests.asmdef
  - Assets/Tests/PlayMode/GameplayCycleTests.cs
autonomous: false
requirements:
  - BRG-05
  - BRG-06
  - TST-12
must_haves:
  truths:
    - "EntitiesCatalog создает Entity параллельно с GameObject и регистрирует GameObjectRef + CollisionBridge маппинг"
    - "Application.cs инициализирует ECS World, создает singleton entities, подключает bridge-системы"
    - "Application.cs не вызывает _model.Update() при _useEcs=true -- ECS World обновляется автоматически"
    - "Game.cs передает input в ECS (Shooting, TargetDirection, IsActive) вместо Model-компонентов"
    - "Game.cs обрабатывает GunShootEvent/LaserShootEvent из буферов для создания пуль/лазера"
    - "Визуалы (ShipVisual, BulletVisual, UfoVisual) при _useEcs вызывают CollisionBridge.ReportCollision"
    - "Игра проходит полный цикл: старт -> корабль управляется -> враги спавнятся -> столкновение -> конец"
    - "PlayMode тест TST-12 проверяет минимальный цикл"
  artifacts:
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "Расширенный каталог с Entity+GameObject параллельным созданием"
      contains: "AddComponentObject.*GameObjectRef"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "Инициализация ECS World и bridge-систем"
      contains: "_useEcs"
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "ECS-интеграция: input->ECS, shoot-events обработка"
      contains: "GunShootEvent"
    - path: "Assets/Tests/PlayMode/GameplayCycleTests.cs"
      provides: "PlayMode тест полного игрового цикла"
      contains: "class GameplayCycleTests"
  key_links:
    - from: "EntitiesCatalog.CreateShip"
      to: "EntityFactory.CreateShip + AddComponentObject<GameObjectRef>"
      via: "Параллельное создание"
      pattern: "EntityFactory\\.Create.*AddComponentObject"
    - from: "Application.OnUpdate"
      to: "ECS World (автоматический Update)"
      via: "_useEcs флаг"
      pattern: "_useEcs"
    - from: "Game.OnAttack"
      to: "ECS GunData.Shooting"
      via: "EntityManager.SetComponentData"
      pattern: "GunData.*Shooting"
    - from: "ShipVisual.OnCollisionEnter2D"
      to: "CollisionBridge.ReportCollision"
      via: "callback"
      pattern: "CollisionBridge.*ReportCollision"
---

<objective>
Интеграция bridge-систем в игровой код и финальная верификация геймплея 1:1.

Purpose: Соединить bridge-компоненты (Plan 01, 02) с существующим игровым кодом: расширить EntitiesCatalog для параллельного создания Entity+GameObject, переключить Application.cs на ECS World, интегрировать input и shoot-events в Game.cs, подключить визуалы к CollisionBridge. Финальный результат -- полностью работающая игра на гибридном DOTS с геймплеем 1:1.

Output: Модифицированные Application.cs, EntitiesCatalog.cs, Game.cs, визуалы, PlayMode тест TST-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bridge-layer-integration/05-CONTEXT.md
@.planning/phases/05-bridge-layer-integration/05-RESEARCH.md
@.planning/phases/05-bridge-layer-integration/05-01-SUMMARY.md
@.planning/phases/05-bridge-layer-integration/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
GameObjectRef.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public class GameObjectRef : ICleanupComponentData
    {
        public Transform Transform;
        public GameObject GameObject;
    }
}
```

GunShootEvent.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct GunShootEvent : IBufferElementData
    {
        public Entity ShooterEntity;
        public float2 Position;
        public float2 Direction;
        public bool IsPlayer;
    }
}
```

LaserShootEvent.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct LaserShootEvent : IBufferElementData
    {
        public Entity ShooterEntity;
        public float2 Position;
        public float2 Direction;
    }
}
```

<!-- From Plan 02 output -->
CollisionBridge.cs:
```csharp
namespace SelStrom.Asteroids
{
    public class CollisionBridge
    {
        public void Initialize(EntityManager entityManager, Entity collisionBufferEntity);
        public void RegisterMapping(GameObject go, Entity entity);
        public void UnregisterMapping(GameObject go);
        public void ReportCollision(GameObject selfGo, GameObject otherGo);
        public void Clear();
    }
}
```

ObservableBridgeSystem.cs:
```csharp
namespace SelStrom.Asteroids
{
    [UpdateInGroup(typeof(PresentationSystemGroup))]
    public partial class ObservableBridgeSystem : SystemBase
    {
        public void SetHudData(HudData hudData);
        public void SetShipViewModel(ShipViewModel viewModel, Sprite mainSprite, Sprite thrustSprite);
        public void SetLaserMaxShoots(int maxShoots);
        public void ClearReferences();
    }
}
```

DeadEntityCleanupSystem.cs:
```csharp
namespace SelStrom.Asteroids
{
    [UpdateInGroup(typeof(LateSimulationSystemGroup))]
    public partial class DeadEntityCleanupSystem : SystemBase
    {
        public void SetOnDeadEntityCallback(Action<GameObject> callback);
    }
}
```

<!-- Existing interfaces being modified -->
From Assets/Scripts/ECS/EntityFactory.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public static class EntityFactory
    {
        public static Entity CreateShip(EntityManager em, float2 position, float moveSpeed, float thrustAcceleration, float thrustMaxSpeed, int gunMaxShoots, float gunReloadSec, int laserMaxShoots, float laserReloadSec);
        public static Entity CreateAsteroid(EntityManager em, float2 position, float speed, float2 direction, int age, int score);
        public static Entity CreateBullet(EntityManager em, float2 position, float speed, float2 direction, float lifeTime, bool isPlayer);
        public static Entity CreateUfoBig(EntityManager em, float2 position, float speed, float2 direction, int gunMaxShoots, float gunReloadSec, float shootToEvery, int score);
        public static Entity CreateUfo(EntityManager em, float2 position, float speed, float2 direction, int gunMaxShoots, float gunReloadSec, float shootToEvery, float moveToEvery, int score);
    }
}
```

From Assets/Scripts/Application/Application.cs:
```csharp
namespace SelStrom.Asteroids
{
    public class Application
    {
        private void OnUpdate(float deltaTime) { _model.Update(deltaTime); }
    }
}
```

From Assets/Scripts/Application/Game.cs:
```csharp
namespace SelStrom.Asteroids
{
    public class Game
    {
        // Input handlers:
        private void OnAttack() { _shipModel.Gun.Shooting = true; ... }
        private void OnLaser() { _shipModel.Laser.Shooting = true; ... }
        private void OnRotateAction(float direction) { _shipModel.Rotate.TargetDirection = direction; }
        private void OnTrust(bool isTrust) { _shipModel.Thrust.IsActive.Value = isTrust; }
        // Shooting callbacks:
        private void OnUserGunShooting(GunComponent gun) { _catalog.CreateBullet(...); }
        private void OnUserLaserShooting(LaserComponent laser) { ... Physics2D.RaycastNonAlloc ...; }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Application и EntitiesCatalog -- ECS World инициализация и параллельное создание Entity+GameObject</name>
  <files>
    Assets/Scripts/Application/Application.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Scripts/ECS/EntityFactory.cs
  </files>
  <read_first>
    Assets/Scripts/Application/Application.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Scripts/ECS/Components/GameObjectRef.cs,
    Assets/Scripts/ECS/Components/GunShootEvent.cs,
    Assets/Scripts/ECS/Components/LaserShootEvent.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Components/LaserData.cs,
    Assets/Scripts/ECS/Components/ThrustData.cs,
    Assets/Scripts/ECS/Components/RotateData.cs,
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Scripts/ECS/Components/ScoreData.cs,
    Assets/Scripts/ECS/Components/GameAreaData.cs,
    Assets/Scripts/ECS/Components/ShipPositionData.cs,
    Assets/Scripts/ECS/Components/CollisionEventData.cs,
    Assets/Scripts/ECS/Components/AgeData.cs,
    Assets/Scripts/Bridge/CollisionBridge.cs,
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs,
    Assets/Scripts/Model/Model.cs,
    Assets/Scripts/Configs/GameData.cs,
    Assets/Scripts/Configs/AsteroidData.cs,
    Assets/Scripts/Configs/UfoData.cs,
    Assets/Scripts/Configs/GunData.cs
  </read_first>
  <action>
    ## 1. Application.cs -- инициализация ECS World и миграционный флаг

    Добавить поля:
    ```csharp
    private bool _useEcs = true;
    private CollisionBridge _collisionBridge;
    ```

    В `Start()` после вычисления sceneWidth/sceneHeight:
    ```csharp
    if (_useEcs)
    {
        var world = World.DefaultGameObjectInjectionWorld;
        var em = world.EntityManager;

        // Создать singleton entities для ECS
        var gameAreaEntity = em.CreateEntity();
        em.AddComponentData(gameAreaEntity, new GameAreaData
        {
            Size = new float2(sceneWidth, sceneHeight)
        });

        var shipPosEntity = em.CreateEntity();
        em.AddComponentData(shipPosEntity, new ShipPositionData());

        var scoreEntity = em.CreateEntity();
        em.AddComponentData(scoreEntity, new ScoreData { Value = 0 });

        var collisionBufferEntity = em.CreateEntity();
        em.AddBuffer<CollisionEventData>(collisionBufferEntity);

        var gunEventEntity = em.CreateEntity();
        em.AddBuffer<GunShootEvent>(gunEventEntity);

        var laserEventEntity = em.CreateEntity();
        em.AddBuffer<LaserShootEvent>(laserEventEntity);

        // CollisionBridge
        _collisionBridge = new CollisionBridge();
        _collisionBridge.Initialize(em, collisionBufferEntity);

        // DeadEntityCleanupSystem callback
        var cleanupSystem = world.GetExistingSystemManaged<DeadEntityCleanupSystem>();
        cleanupSystem.SetOnDeadEntityCallback(OnDeadEntity);

        // EntitiesCatalog с ECS
        _catalog.ConnectEcs(em, _collisionBridge);
    }
    ```

    Метод `OnDeadEntity(GameObject go)`:
    ```csharp
    private void OnDeadEntity(GameObject go)
    {
        _collisionBridge.UnregisterMapping(go);

        // Дробление астероидов и VFX
        if (_catalog.TryFindModel<AsteroidModel>(go, out var asteroidModel))
        {
            _game.PlayEffect(_configs.VfxBlowPrefab, asteroidModel.Move.Position.Value);
            var age = asteroidModel.Age - 1;
            if (age > 0)
            {
                var position = asteroidModel.Move.Position.Value;
                var speed = Math.Min(asteroidModel.Move.Speed.Value * 2, 10f);
                _catalog.CreateAsteroid(age, position, speed);
                _catalog.CreateAsteroid(age, position, speed);
            }
        }
        else if (_catalog.TryFindModel<ShipModel>(go, out var shipModel))
        {
            _game.PlayEffect(_configs.VfxBlowPrefab, shipModel.Move.Position.Value);
            _game.StopFromEcs();
        }
        else if (_catalog.TryFindModel<UfoBigModel>(go, out var ufoModel))
        {
            _game.PlayEffect(_configs.VfxBlowPrefab, ufoModel.Move.Position.Value);
        }

        _catalog.ReleaseByGameObject(go);
    }
    ```

    В `OnUpdate`:
    ```csharp
    private void OnUpdate(float deltaTime)
    {
        if (_useEcs)
        {
            // ECS World обновляется автоматически Unity runtime
            // Только ActionScheduler нужно обновлять вручную
            _model.ActionScheduler.Update(deltaTime);
        }
        else
        {
            _model.Update(deltaTime);
        }
    }
    ```
    Per D-15: Game.cs сохраняется, D-16: ActionScheduler остается в managed.
    Per Pitfall 7 (RESEARCH): НЕ вызывать World.Update() -- Unity обновляет DefaultWorld автоматически.

    Добавить `using Unity.Entities;`, `using Unity.Mathematics;`, `using SelStrom.Asteroids.ECS;`.

    В `Quit()`:
    ```csharp
    if (_collisionBridge != null)
    {
        _collisionBridge.Clear();
        _collisionBridge = null;
    }
    ```

    ## 2. EntitiesCatalog.cs -- параллельное создание Entity+GameObject

    Добавить поля:
    ```csharp
    private EntityManager _entityManager;
    private CollisionBridge _collisionBridge;
    private bool _useEcs;
    private readonly Dictionary<GameObject, Entity> _gameObjectToEntity = new();
    ```

    Добавить метод `ConnectEcs`:
    ```csharp
    public void ConnectEcs(EntityManager entityManager, CollisionBridge collisionBridge)
    {
        _entityManager = entityManager;
        _collisionBridge = collisionBridge;
        _useEcs = true;
    }
    ```

    Добавить метод `ReleaseByGameObject(GameObject go)`:
    ```csharp
    public void ReleaseByGameObject(GameObject go)
    {
        if (_gameObjectToModel.TryGetValue(go, out var model))
        {
            if (_modelToBindings.TryGetValue(model, out var bindings))
            {
                bindings.CleanUp();
                _modelToBindings.Remove(model);
            }

            var view = _modelToVisual[model];
            _modelToVisual.Remove(model);
            _visualToModel.Remove(view);
            _gameObjectToModel.Remove(go);

            _viewFactory.Release(view);
            _modelFactory.Release(model);
        }

        if (_gameObjectToEntity.TryGetValue(go, out _))
        {
            _gameObjectToEntity.Remove(go);
        }
    }
    ```

    Добавить `TryGetEntity`:
    ```csharp
    public bool TryGetEntity(GameObject go, out Entity entity)
    {
        return _gameObjectToEntity.TryGetValue(go, out entity);
    }
    ```

    В каждом `Create*` методе (CreateShip, CreateAsteroid, CreateBullet, CreateBigUfo, CreateUfo) ПОСЛЕ `AddToCatalog(model, view, bindings)` добавить блок создания Entity:

    **CreateShip** -- после AddToCatalog:
    ```csharp
    if (_useEcs)
    {
        var entity = EntityFactory.CreateShip(
            _entityManager,
            new float2(model.Move.Position.Value.x, model.Move.Position.Value.y),
            _configs.Ship.Speed,
            _configs.Ship.ThrustUnitsPerSecond,
            _configs.Ship.MaxSpeed,
            _configs.Ship.Gun.MaxShoots,
            _configs.Ship.Gun.ReloadDurationSec,
            _configs.Laser.LaserMaxShoots,
            _configs.Laser.LaserUpdateDurationSec
        );
        _entityManager.AddComponentObject(entity, new GameObjectRef
        {
            Transform = view.transform,
            GameObject = view.gameObject
        });
        _collisionBridge.RegisterMapping(view.gameObject, entity);
        _gameObjectToEntity[view.gameObject] = entity;
    }
    ```

    **CreateAsteroid** -- после AddToCatalog:
    ```csharp
    if (_useEcs)
    {
        var entity = EntityFactory.CreateAsteroid(
            _entityManager,
            new float2(model.Move.Position.Value.x, model.Move.Position.Value.y),
            model.Move.Speed.Value,
            new float2(model.Move.Direction.x, model.Move.Direction.y),
            size,
            data.Score
        );
        _entityManager.AddComponentObject(entity, new GameObjectRef
        {
            Transform = view.transform,
            GameObject = view.gameObject
        });
        _collisionBridge.RegisterMapping(view.gameObject, entity);
        _gameObjectToEntity[view.gameObject] = entity;
    }
    ```

    **CreateBullet** -- после AddToCatalog:
    ```csharp
    if (_useEcs)
    {
        var isPlayer = (prefab == _configs.Bullet.Prefab);
        var entity = EntityFactory.CreateBullet(
            _entityManager,
            new float2(position.x, position.y),
            data.Speed,
            new float2(direction.x, direction.y),
            data.LifeTimeSec,
            isPlayer
        );
        _entityManager.AddComponentObject(entity, new GameObjectRef
        {
            Transform = view.transform,
            GameObject = view.gameObject
        });
        _collisionBridge.RegisterMapping(view.gameObject, entity);
        _gameObjectToEntity[view.gameObject] = entity;
    }
    ```

    **CreateBigUfo** -- после AddToCatalog:
    ```csharp
    if (_useEcs)
    {
        var entity = EntityFactory.CreateUfoBig(
            _entityManager,
            new float2(position.x, position.y),
            _configs.UfoBig.Speed,
            new float2(direction.x, direction.y),
            _configs.UfoBig.Gun.MaxShoots,
            _configs.UfoBig.Gun.ReloadDurationSec,
            _configs.UfoBig.ShootToEvery,
            _configs.UfoBig.Score
        );
        _entityManager.AddComponentObject(entity, new GameObjectRef
        {
            Transform = view.transform,
            GameObject = view.gameObject
        });
        _collisionBridge.RegisterMapping(view.gameObject, entity);
        _gameObjectToEntity[view.gameObject] = entity;
    }
    ```

    **CreateUfo** -- после AddToCatalog:
    ```csharp
    if (_useEcs)
    {
        var entity = EntityFactory.CreateUfo(
            _entityManager,
            new float2(position.x, position.y),
            _configs.Ufo.Speed,
            new float2(direction.x, direction.y),
            _configs.Ufo.Gun.MaxShoots,
            _configs.Ufo.Gun.ReloadDurationSec,
            3f,     // shootToEvery -- хардкод 1:1 с оригиналом
            3f,     // moveToEvery -- хардкод 1:1 с оригиналом
            _configs.Ufo.Score
        );
        _entityManager.AddComponentObject(entity, new GameObjectRef
        {
            Transform = view.transform,
            GameObject = view.gameObject
        });
        _collisionBridge.RegisterMapping(view.gameObject, entity);
        _gameObjectToEntity[view.gameObject] = entity;
    }
    ```

    ПРИМЕЧАНИЕ: Нужно проверить GameData/UfoData/AsteroidData на наличие полей Score, ShootToEvery, MoveToEvery, LifeTimeSec, Speed. Если они называются иначе -- использовать правильные имена из read_first файлов.

    В `CleanUp()`:
    ```csharp
    if (_useEcs)
    {
        _collisionBridge?.Clear();
        _gameObjectToEntity.Clear();
    }
    ```

    Collision callbacks -- при _useEcs переключить на CollisionBridge. В `CreateShip`:
    ```csharp
    if (_useEcs)
    {
        viewModel.OnCollision.Value = (col) =>
        {
            _collisionBridge.ReportCollision(view.gameObject, col.gameObject);
        };
    }
    else
    {
        viewModel.OnCollision.Value = onRegisterCollision;
    }
    ```

    Аналогично для BulletVisual и UfoVisual -- при _useEcs collision callbacks используют CollisionBridge.ReportCollision. Проверить BulletVisual и UfoVisual callback patterns при read_first.

    ВАЖНО: Проверить как UfoVisual.OnCollisionEnter2D работает -- если он не передает otherGo, нужно расширить ViewModel. Прочитать UfoVisual.cs при исполнении.
  </action>
  <verify>
    <automated>grep -r "_useEcs" Assets/Scripts/Application/Application.cs && grep -r "ConnectEcs" Assets/Scripts/Application/EntitiesCatalog.cs && grep -r "CollisionBridge" Assets/Scripts/Application/EntitiesCatalog.cs && grep "ReleaseByGameObject" Assets/Scripts/Application/EntitiesCatalog.cs && grep "AddComponentObject.*GameObjectRef" Assets/Scripts/Application/EntitiesCatalog.cs && grep "DeadEntityCleanupSystem" Assets/Scripts/Application/Application.cs</automated>
  </verify>
  <acceptance_criteria>
    - Application.cs содержит `private bool _useEcs = true` и не вызывает `_model.Update(deltaTime)` при _useEcs
    - Application.cs создает singleton entities: GameAreaData, ShipPositionData, ScoreData, CollisionEventData buffer, GunShootEvent buffer, LaserShootEvent buffer
    - Application.cs инициализирует CollisionBridge и передает в EntitiesCatalog
    - Application.cs подключает DeadEntityCleanupSystem callback
    - Application.cs содержит OnDeadEntity с дроблением астероидов, VFX и StopFromEcs
    - EntitiesCatalog.cs содержит метод ConnectEcs(EntityManager, CollisionBridge)
    - EntitiesCatalog.cs содержит метод ReleaseByGameObject(GameObject)
    - EntitiesCatalog.cs содержит `_entityManager.AddComponentObject(entity, new GameObjectRef` в каждом Create методе
    - EntitiesCatalog.cs содержит `_collisionBridge.RegisterMapping` в каждом Create методе
    - EntitiesCatalog Create* методы при _useEcs устанавливают collision callbacks на CollisionBridge.ReportCollision
  </acceptance_criteria>
  <done>
    Application.cs инициализирует ECS World с singleton entities и bridge-системами, EntitiesCatalog параллельно создает Entity+GameObject с GameObjectRef и CollisionBridge маппингом для всех типов сущностей.
  </done>
</task>

<task type="auto">
  <name>Task 2: Game.cs input/shoot-events и GameScreen ObservableBridge интеграция</name>
  <files>
    Assets/Scripts/Application/Game.cs,
    Assets/Scripts/Application/Screens/GameScreen.cs
  </files>
  <read_first>
    Assets/Scripts/Application/Game.cs,
    Assets/Scripts/Application/Screens/GameScreen.cs,
    Assets/Scripts/Application/Application.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Components/LaserData.cs,
    Assets/Scripts/ECS/Components/ThrustData.cs,
    Assets/Scripts/ECS/Components/RotateData.cs,
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Scripts/ECS/Components/GunShootEvent.cs,
    Assets/Scripts/ECS/Components/LaserShootEvent.cs,
    Assets/Scripts/ECS/Components/Tags/ShipTag.cs,
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Scripts/View/HudVisual.cs,
    Assets/Scripts/View/ShipVisual.cs,
    Assets/Scripts/Configs/GameData.cs
  </read_first>
  <action>
    ## 1. Game.cs -- input->ECS, shoot-events обработка

    Добавить поля:
    ```csharp
    private bool _useEcs;
    private CollisionBridge _collisionBridge;
    private EntityManager _entityManager;
    ```

    Добавить метод для ECS-подключения:
    ```csharp
    public void ConnectEcs(bool useEcs, CollisionBridge collisionBridge, EntityManager entityManager)
    {
        _useEcs = useEcs;
        _collisionBridge = collisionBridge;
        _entityManager = entityManager;
    }
    ```

    Сделать `PlayEffect` публичным. Добавить метод `StopFromEcs()`:
    ```csharp
    public void StopFromEcs()
    {
        Stop();
    }
    ```

    **Input handlers** -- при _useEcs записывать в ECS. Используем EntityManager для прямого доступа к ECS данным корабля через ShipTag singleton query:

    `OnAttack()`:
    ```csharp
    private void OnAttack()
    {
        if (_useEcs)
        {
            var query = _entityManager.CreateEntityQuery(typeof(ShipTag), typeof(GunData), typeof(RotateData), typeof(MoveData));
            if (query.CalculateEntityCount() > 0)
            {
                var entity = query.GetSingletonEntity();
                var gunData = _entityManager.GetComponentData<GunData>(entity);
                var rotateData = _entityManager.GetComponentData<RotateData>(entity);
                var moveData = _entityManager.GetComponentData<MoveData>(entity);
                gunData.Shooting = true;
                gunData.Direction = rotateData.Rotation;
                gunData.ShootPosition = moveData.Position;
                _entityManager.SetComponentData(entity, gunData);
            }
        }
        else
        {
            _shipModel.Gun.Shooting = true;
            _shipModel.Gun.Direction = _shipModel.Rotate.Rotation.Value;
            _shipModel.Gun.ShootPosition = _shipModel.ShootPoint;
        }
    }
    ```

    `OnLaser()`:
    ```csharp
    private void OnLaser()
    {
        if (_useEcs)
        {
            var query = _entityManager.CreateEntityQuery(typeof(ShipTag), typeof(LaserData), typeof(RotateData), typeof(MoveData));
            if (query.CalculateEntityCount() > 0)
            {
                var entity = query.GetSingletonEntity();
                var laserData = _entityManager.GetComponentData<LaserData>(entity);
                var rotateData = _entityManager.GetComponentData<RotateData>(entity);
                var moveData = _entityManager.GetComponentData<MoveData>(entity);
                laserData.Shooting = true;
                laserData.Direction = rotateData.Rotation;
                laserData.ShootPosition = moveData.Position;
                _entityManager.SetComponentData(entity, laserData);
            }
        }
        else
        {
            _shipModel.Laser.Shooting = true;
            _shipModel.Laser.Direction = _shipModel.Rotate.Rotation.Value;
            _shipModel.Laser.ShootPosition = _shipModel.ShootPoint;
        }
    }
    ```

    `OnRotateAction(float direction)`:
    ```csharp
    private void OnRotateAction(float direction)
    {
        if (_useEcs)
        {
            var query = _entityManager.CreateEntityQuery(typeof(ShipTag), typeof(RotateData));
            if (query.CalculateEntityCount() > 0)
            {
                var entity = query.GetSingletonEntity();
                var rotateData = _entityManager.GetComponentData<RotateData>(entity);
                rotateData.TargetDirection = direction;
                _entityManager.SetComponentData(entity, rotateData);
            }
        }
        else
        {
            _shipModel.Rotate.TargetDirection = direction;
        }
    }
    ```

    `OnTrust(bool isTrust)`:
    ```csharp
    private void OnTrust(bool isTrust)
    {
        if (_useEcs)
        {
            var query = _entityManager.CreateEntityQuery(typeof(ShipTag), typeof(ThrustData));
            if (query.CalculateEntityCount() > 0)
            {
                var entity = query.GetSingletonEntity();
                var thrustData = _entityManager.GetComponentData<ThrustData>(entity);
                thrustData.IsActive = isTrust;
                _entityManager.SetComponentData(entity, thrustData);
            }
        }
        else
        {
            _shipModel.Thrust.IsActive.Value = isTrust;
        }
    }
    ```

    **Shoot event обработка** -- добавить метод для вызова из Application.OnUpdate:
    ```csharp
    public void ProcessShootEvents()
    {
        if (!_useEcs)
        {
            return;
        }

        // Обработать GunShootEvents
        var gunQuery = _entityManager.CreateEntityQuery(typeof(GunShootEvent));
        if (gunQuery.CalculateEntityCount() > 0)
        {
            var bufferEntity = gunQuery.GetSingletonEntity();
            var gunEvents = _entityManager.GetBuffer<GunShootEvent>(bufferEntity);
            for (int i = 0; i < gunEvents.Length; i++)
            {
                var evt = gunEvents[i];
                var position = new Vector2(evt.Position.x, evt.Position.y);
                var direction = new Vector2(evt.Direction.x, evt.Direction.y);
                if (evt.IsPlayer)
                {
                    _catalog.CreateBullet(_configs.Bullet, _configs.Bullet.Prefab, position, direction, OnUserBulletCollided);
                }
                else
                {
                    _catalog.CreateBullet(_configs.Bullet, _configs.Bullet.EnemyPrefab, position, direction, OnEnemyBulletCollided);
                }
            }
            gunEvents.Clear();
        }

        // Обработать LaserShootEvents
        var laserQuery = _entityManager.CreateEntityQuery(typeof(LaserShootEvent));
        if (laserQuery.CalculateEntityCount() > 0)
        {
            var bufferEntity = laserQuery.GetSingletonEntity();
            var laserEvents = _entityManager.GetBuffer<LaserShootEvent>(bufferEntity);
            for (int i = 0; i < laserEvents.Length; i++)
            {
                var evt = laserEvents[i];
                var position = new Vector2(evt.Position.x, evt.Position.y);
                var direction = new Vector2(evt.Direction.x, evt.Direction.y);

                // Визуальный эффект лазера (1:1 с OnUserLaserShooting)
                var effect = _catalog.ViewFactory.Get<LineRenderer>(_configs.Laser.Prefab);
                effect.transform.position = position;
                var angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;
                effect.transform.rotation = Quaternion.Euler(new Vector3(0, 0, angle));
                _model.ActionScheduler.ScheduleAction(() =>
                {
                    _catalog.ViewFactory.Release(effect.gameObject);
                }, _configs.Laser.BeamEffectLifetimeSec);

                // Raycast для попадания
                var hits = new RaycastHit2D[30];
                var size = Physics2D.RaycastNonAlloc(position, direction, hits,
                    _model.GameArea.magnitude, LayerMask.GetMask("Asteroid", "Enemy"));
                if (size > 0)
                {
                    for (var j = 0; j < size; j++)
                    {
                        var hit = hits[j];
                        var gameObject = hit.collider.gameObject;
                        if (_catalog.TryFindModel<IGameEntityModel>(gameObject, out var model))
                        {
                            _model.ReceiveScore(model);
                            Kill(model);
                        }
                    }
                }
            }
            laserEvents.Clear();
        }
    }
    ```

    В Application.OnUpdate вызвать ProcessShootEvents после ActionScheduler (уже добавлено в Task 1):
    ```csharp
    if (_useEcs)
    {
        _model.ActionScheduler.Update(deltaTime);
        _game.ProcessShootEvents();
    }
    ```

    Добавить `using Unity.Entities;`, `using Unity.Mathematics;`, `using SelStrom.Asteroids.ECS;`.

    ## 2. GameScreen.cs -- подключение ObservableBridgeSystem

    В `ActivateHud()` при _useEcs вместо `Bind.From(shipModel.Move.Position).To(...)` подключить ObservableBridgeSystem:
    ```csharp
    private void ActivateHud()
    {
        _hudData = new HudData();
        _hudVisual.Connect(_hudData);

        if (_useEcs)
        {
            var world = World.DefaultGameObjectInjectionWorld;
            var bridge = world.GetExistingSystemManaged<ObservableBridgeSystem>();
            bridge.SetHudData(_hudData);
            bridge.SetLaserMaxShoots(_configs.Laser.LaserMaxShoots);
        }
        else
        {
            var shipModel = _data.ShipModel;
            Bind.From(shipModel.Move.Position).To(OnShipPositionChanged);
            // ... остальные привязки
            Bind.InvokeAll();
        }
    }
    ```

    В `DeactivateHud()`:
    ```csharp
    private void DeactivateHud()
    {
        if (_useEcs)
        {
            var world = World.DefaultGameObjectInjectionWorld;
            if (world != null && world.IsCreated)
            {
                var bridge = world.GetExistingSystemManaged<ObservableBridgeSystem>();
                bridge.ClearReferences();
            }
        }
        CleanUp();
        _hudData = null;
    }
    ```

    Добавить `_useEcs` поле и передать через GameScreenData:
    ```csharp
    public struct GameScreenData
    {
        public ShipModel ShipModel;
        public Model Model;
        public Game Game;
        public bool UseEcs;
    }
    ```

    Добавить `using Unity.Entities;`.

    ## 3. ObservableBridgeSystem -- подключение ShipViewModel

    В Application.cs при создании корабля (после Game.Start) вызвать:
    ```csharp
    if (_useEcs)
    {
        var world = World.DefaultGameObjectInjectionWorld;
        var bridge = world.GetExistingSystemManaged<ObservableBridgeSystem>();
        bridge.SetShipViewModel(shipViewModel, _configs.Ship.MainSprite, _configs.Ship.ThrustSprite);
    }
    ```
    ShipViewModel и спрайты нужно сделать доступными -- executor должен прочитать как текущий код передает спрайты и адаптировать.
  </action>
  <verify>
    <automated>grep -r "GunShootEvent" Assets/Scripts/Application/Game.cs && grep -r "ProcessShootEvents" Assets/Scripts/Application/Game.cs && grep -r "ConnectEcs" Assets/Scripts/Application/Game.cs && grep -r "ObservableBridgeSystem" Assets/Scripts/Application/Screens/GameScreen.cs && grep -r "_useEcs" Assets/Scripts/Application/Screens/GameScreen.cs</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs содержит метод ProcessShootEvents() обрабатывающий GunShootEvent и LaserShootEvent буферы
    - Game.cs input handlers (OnAttack, OnLaser, OnRotateAction, OnTrust) при _useEcs записывают в ECS через EntityManager
    - Game.cs содержит ConnectEcs(bool, CollisionBridge, EntityManager)
    - Game.cs содержит публичные PlayEffect и StopFromEcs
    - GameScreen.cs подключает ObservableBridgeSystem вместо Bind.From при _useEcs
    - GameScreen.cs вызывает ClearReferences в DeactivateHud при _useEcs
    - GameScreenData содержит bool UseEcs
  </acceptance_criteria>
  <done>
    Game.cs передает input в ECS и обрабатывает shoot-event буферы для создания пуль/лазера. GameScreen.cs переключается на ObservableBridgeSystem для HUD при _useEcs.
  </done>
</task>

<task type="auto">
  <name>Task 3: PlayMode тест полного игрового цикла (TST-12)</name>
  <files>
    Assets/Tests/PlayMode/PlayModeTests.asmdef,
    Assets/Tests/PlayMode/GameplayCycleTests.cs
  </files>
  <read_first>
    Assets/Tests/PlayMode/PlayModeTests.asmdef,
    Assets/Scripts/Application/Application.cs,
    Assets/Scripts/Application/Game.cs,
    Assets/Scripts/Application/Screens/GameScreen.cs,
    Assets/Scripts/Application/ApplicationEntry.cs
  </read_first>
  <action>
    1. **PlayModeTests.asmdef** -- добавить ссылки `"AsteroidsECS"`, `"Unity.Entities"`, `"Unity.Mathematics"` в references. Сохранить все существующие.

    2. **GameplayCycleTests.cs** -- PlayMode тест полного игрового цикла. Минимальный тест:
    - Загрузить сцену Main
    - Подождать несколько кадров для инициализации
    - Проверить что ApplicationEntry создан (FindFirstObjectByType)
    - Проверить что ECS World существует и содержит entities

    ```csharp
    using System.Collections;
    using NUnit.Framework;
    using SelStrom.Asteroids;
    using Unity.Entities;
    using UnityEngine;
    using UnityEngine.SceneManagement;
    using UnityEngine.TestTools;

    namespace SelStrom.Asteroids.Tests.PlayMode
    {
        public class GameplayCycleTests
        {
            [UnitySetUp]
            public IEnumerator SetUp()
            {
                SceneManager.LoadScene("Main");
                yield return null; // Ждем загрузку сцены
                yield return null; // Ждем Awake/Start
            }

            [UnityTest]
            public IEnumerator GameStarts_AndEntitiesExistInWorld()
            {
                // Ждем инициализацию
                yield return null;
                yield return null;
                yield return null;

                var entry = Object.FindFirstObjectByType<ApplicationEntry>();
                Assert.IsNotNull(entry, "ApplicationEntry должен существовать в сцене");

                var world = World.DefaultGameObjectInjectionWorld;
                Assert.IsNotNull(world, "DefaultGameObjectInjectionWorld должен существовать");

                var shipQuery = world.EntityManager.CreateEntityQuery(
                    typeof(SelStrom.Asteroids.ECS.ShipTag));
                Assert.GreaterOrEqual(shipQuery.CalculateEntityCount(), 1,
                    "В мире должен быть минимум 1 Ship entity");

                var gameAreaQuery = world.EntityManager.CreateEntityQuery(
                    typeof(SelStrom.Asteroids.ECS.GameAreaData));
                Assert.AreEqual(1, gameAreaQuery.CalculateEntityCount(),
                    "GameArea singleton должен существовать");
            }

            [UnityTest]
            public IEnumerator GameLoop_EcsSystemsUpdateEveryFrame()
            {
                for (int i = 0; i < 10; i++)
                {
                    yield return null;
                }

                var world = World.DefaultGameObjectInjectionWorld;
                var shipQuery = world.EntityManager.CreateEntityQuery(
                    typeof(SelStrom.Asteroids.ECS.ShipTag),
                    typeof(SelStrom.Asteroids.ECS.MoveData));

                if (shipQuery.CalculateEntityCount() > 0)
                {
                    var entity = shipQuery.GetSingletonEntity();
                    var moveData = world.EntityManager.GetComponentData<SelStrom.Asteroids.ECS.MoveData>(entity);
                    Assert.IsFalse(float.IsNaN(moveData.Position.x),
                        "Позиция корабля X не должна быть NaN");
                    Assert.IsFalse(float.IsNaN(moveData.Position.y),
                        "Позиция корабля Y не должна быть NaN");
                }
            }

            [UnityTearDown]
            public IEnumerator TearDown()
            {
                yield return null;
            }
        }
    }
    ```

    ПРИМЕЧАНИЕ: Полноценный тест столкновений сложен в PlayMode. Базовый тест проверяет: (1) сцена загружается, (2) ECS World инициализируется, (3) Ship entity существует, (4) системы обновляются без ошибок.
  </action>
  <verify>
    <automated>grep -r "class GameplayCycleTests" Assets/Tests/PlayMode/GameplayCycleTests.cs && grep "AsteroidsECS" Assets/Tests/PlayMode/PlayModeTests.asmdef && grep -c "UnityTest" Assets/Tests/PlayMode/GameplayCycleTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - PlayModeTests.asmdef содержит `"AsteroidsECS"`, `"Unity.Entities"`, `"Unity.Mathematics"` в references
    - GameplayCycleTests.cs содержит `public class GameplayCycleTests`
    - GameplayCycleTests.cs содержит минимум 2 метода с атрибутом [UnityTest]
    - GameplayCycleTests.cs загружает сцену Main через `SceneManager.LoadScene("Main")`
    - GameplayCycleTests.cs проверяет наличие ApplicationEntry, ShipTag entity, GameAreaData singleton
    - GameplayCycleTests.cs проверяет что ECS обновляется без ошибок (позиция не NaN)
  </acceptance_criteria>
  <done>
    PlayMode тесты TST-12 проверяют полный цикл: загрузка сцены, инициализация ECS, наличие entities, работа систем без ошибок.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ручная верификация геймплея 1:1</name>
  <files></files>
  <action>Человек проверяет полный геймплей в Unity Editor.</action>
  <what-built>
    Полная интеграция гибридного DOTS: ECS-системы управляют логикой, GameObjects отвечают за рендеринг, Bridge Layer связывает оба мира. Миграционный флаг _useEcs=true активирован.
  </what-built>
  <how-to-verify>
    1. Открыть проект в Unity Editor
    2. Открыть сцену Main, нажать Play
    3. Проверить базовый геймплей:
       - Корабль отображается и управляется (W/A/D)
       - При нажатии Space корабль стреляет, пули летят
       - При нажатии Q срабатывает лазер
       - Астероиды движутся и обертываются через границу экрана
       - UFO спавнятся и стреляют
       - Столкновение пули с астероидом уничтожает обоих, астероид дробится
       - Столкновение корабля с врагом завершает игру
       - HUD показывает координаты, скорость, ротацию, заряды лазера
       - Score считается при уничтожении врагов
    4. Запустить тесты: Window -> General -> Test Runner -> Run All
       - Все EditMode ECS тесты проходят зеленым
       - PlayMode тесты проходят (загрузка сцены, наличие entities)
    5. Общее впечатление: геймплей 1:1 с оригиналом (до миграции)
  </how-to-verify>
  <resume-signal>Введите "approved" если геймплей 1:1, или опишите проблемы</resume-signal>
  <verify>Ручная проверка геймплея</verify>
  <done>Геймплей 1:1 подтвержден человеком</done>
</task>

</tasks>

<verification>
- Application.cs инициализирует ECS World с singleton entities и bridge-системами
- EntitiesCatalog параллельно создает Entity+GameObject для всех типов сущностей
- Game.cs передает input в ECS и обрабатывает shoot-event буферы
- Визуалы используют CollisionBridge при _useEcs
- GameScreen подключает ObservableBridgeSystem для HUD
- PlayMode тест проверяет наличие ECS entities после загрузки сцены
- Ручная верификация подтверждает геймплей 1:1
</verification>

<success_criteria>
- Игра запускается в Editor с _useEcs=true и воспроизводит весь геймплей 1:1
- Корабль управляется, стреляет, враги спавнятся, коллизии работают, HUD отображается
- PlayMode тесты (TST-12) проходят
- Все EditMode тесты (TST-10 и существующие) проходят
- Миграционный флаг позволяет переключиться обратно на Model (_useEcs=false)
</success_criteria>

<output>
After completion, create `.planning/phases/05-bridge-layer-integration/05-03-SUMMARY.md`
</output>
