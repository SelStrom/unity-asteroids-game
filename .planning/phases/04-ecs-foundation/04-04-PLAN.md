---
phase: 04-ecs-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Assets/Scripts/ECS/Systems/EcsShootToSystem.cs
  - Assets/Scripts/ECS/Systems/EcsMoveToSystem.cs
  - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
  - Assets/Scripts/ECS/Components/CollisionEventData.cs
  - Assets/Scripts/ECS/Components/ScoreData.cs
  - Assets/Scripts/ECS/Components/Tags/DeadTag.cs
  - Assets/Tests/EditMode/ECS/ShootToSystemTests.cs
  - Assets/Tests/EditMode/ECS/MoveToSystemTests.cs
  - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
autonomous: true
requirements:
  - ECS-09
  - ECS-10
  - ECS-11
  - TST-07
  - TST-08
  - TST-09

must_haves:
  truths:
    - "ShootToSystem рассчитывает упреждающее направление стрельбы на основе позиции и скорости корабля"
    - "ShootToSystem устанавливает Shooting=true и Direction в GunData когда CurrentShoots > 0"
    - "MoveToSystem пересчитывает Direction каждые Every секунд по направлению к предсказанной позиции корабля"
    - "CollisionHandler обрабатывает коллизионные события из mock-данных CollisionEventData"
  artifacts:
    - path: "Assets/Scripts/ECS/Systems/EcsShootToSystem.cs"
      provides: "AI стрельба с упреждением"
      contains: "struct EcsShootToSystem : ISystem"
    - path: "Assets/Scripts/ECS/Systems/EcsMoveToSystem.cs"
      provides: "AI движение к цели"
      contains: "struct EcsMoveToSystem : ISystem"
    - path: "Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs"
      provides: "Обработчик коллизий"
      contains: "struct EcsCollisionHandlerSystem : ISystem"
    - path: "Assets/Scripts/ECS/Components/CollisionEventData.cs"
      provides: "Буфер коллизионных событий для mock-тестов"
      contains: "struct CollisionEventData"
  key_links:
    - from: "EcsShootToSystem"
      to: "ShipPositionData"
      via: "SystemAPI.GetSingleton"
      pattern: "GetSingleton<ShipPositionData>"
    - from: "EcsMoveToSystem"
      to: "ShipPositionData"
      via: "SystemAPI.GetSingleton"
      pattern: "GetSingleton<ShipPositionData>"
---

<objective>
Создать AI-системы (ShootTo, MoveTo) и CollisionHandler с mock-данными коллизий. Покрыть EditMode-тестами.

Purpose: AI-системы используют ShipPositionData singleton (per D-08) для предиктивного прицеливания и преследования. CollisionHandler подготовлен к Phase 5 Bridge Layer (per D-13, D-14).
Output: 3 системы + тесты, все без Burst (per D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecs-foundation/04-CONTEXT.md
@.planning/phases/04-ecs-foundation/04-RESEARCH.md
@.planning/phases/04-ecs-foundation/04-01-SUMMARY.md

<interfaces>
<!-- Оригинальные системы для портирования -->

From Assets/Scripts/Model/Systems/ShootToSystem.cs:
```csharp
protected override void UpdateNode(
    (MoveComponent Move, GunComponent Gun, ShootToComponent ShootTo) node, float deltaTime)
{
    if (node.Gun.CurrentShoots <= 0) { return; }

    var ship = node.ShootTo.Ship;
    var time = (ship.Move.Position.Value - node.Move.Position.Value).magnitude
               / (20 - ship.Move.Speed.Value);

    var pendingPosition = ship.Move.Position.Value
        + (ship.Move.Direction * ship.Move.Speed.Value) * time;
    var direction = (pendingPosition - node.Move.Position.Value).normalized;

    node.Gun.Shooting = true;
    node.Gun.Direction = direction;
    node.Gun.ShootPosition = node.Move.Position.Value;
}
```
Note: скорость пули хардкод 20 -- воспроизводим 1:1 (deferred QUAL-01).
Note: division by zero когда ship.Speed == 20 -- воспроизводим 1:1.

From Assets/Scripts/Model/Systems/MoveToSystem.cs:
```csharp
protected override void UpdateNode(
    (MoveComponent Move, MoveToComponent MoveTo) node, float deltaTime)
{
    node.MoveTo.ReadyRemaining -= deltaTime;
    if (node.MoveTo.ReadyRemaining > 0) { return; }

    node.MoveTo.ReadyRemaining = node.MoveTo.Every;

    var ship = node.MoveTo.Ship;
    var time = (ship.Move.Position.Value - node.Move.Position.Value).magnitude
               / (node.Move.Speed.Value - ship.Move.Speed.Value);

    var pendingPosition = ship.Move.Position.Value
        + (ship.Move.Direction * ship.Move.Speed.Value) * time;
    node.Move.Direction = (pendingPosition - node.Move.Position.Value).normalized;
}
```
Note: division by zero когда ufo.Speed == ship.Speed -- воспроизводим 1:1.

From Assets/Scripts/Model/Model.cs (коллизии + подсчет очков):
```csharp
// Game.OnUserBulletCollided: пуля уничтожается + ReceiveScore + entity Kill
// Game.OnEnemyBulletCollided: пуля уничтожается, коллайдер отключается
// Game.OnShipCollided: корабль уничтожается, игра завершается
// Game.OnUfoCollided: UFO уничтожается
// ReceiveScore: switch(entity) { AsteroidModel -> +score, UfoModel -> +score, UfoBigModel -> +score }
```

From Assets/Scripts/ECS/Components/ShipPositionData.cs (from Plan 01):
```csharp
public struct ShipPositionData : IComponentData
{
    public float2 Position;
    public float Speed;
    public float2 Direction;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI-системы (ShootTo, MoveTo) и тесты</name>
  <files>
    Assets/Scripts/ECS/Systems/EcsShootToSystem.cs,
    Assets/Scripts/ECS/Systems/EcsMoveToSystem.cs,
    Assets/Tests/EditMode/ECS/ShootToSystemTests.cs,
    Assets/Tests/EditMode/ECS/MoveToSystemTests.cs
  </files>
  <read_first>
    Assets/Scripts/Model/Systems/ShootToSystem.cs,
    Assets/Scripts/Model/Systems/MoveToSystem.cs,
    Assets/Scripts/Model/Components/ShootToComponent.cs,
    Assets/Scripts/Model/Components/MoveToComponent.cs,
    Assets/Scripts/ECS/Components/ShootToData.cs,
    Assets/Scripts/ECS/Components/MoveToData.cs,
    Assets/Scripts/ECS/Components/ShipPositionData.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    .planning/phases/04-ecs-foundation/04-RESEARCH.md
  </read_first>
  <behavior>
    ShootToSystem:
    - Test: CurrentShoots > 0, ship at (10,0), ufo at (0,0), ship speed=5, ship direction=(1,0) -- Direction рассчитывается с упреждением, Shooting=true
    - Test: CurrentShoots == 0 -- ничего не происходит, Shooting остается false
    - Test: ShootPosition устанавливается в текущую позицию UFO

    MoveToSystem:
    - Test: ReadyRemaining > 0 -- Direction не меняется (cooldown)
    - Test: ReadyRemaining <= 0 -- Direction пересчитывается к предсказанной позиции, ReadyRemaining = Every
    - Test: Ship at (10,0), ufo at (0,0), ufo speed=8, ship speed=5 -- Direction направлен к предсказанной позиции
  </behavior>
  <action>
1. Создать `Assets/Scripts/ECS/Systems/EcsShootToSystem.cs`:
   - `public partial struct EcsShootToSystem : ISystem` (per D-03, без [BurstCompile])
   - `[UpdateAfter(typeof(EcsLaserSystem))]` `[UpdateBefore(typeof(EcsMoveToSystem))]`
   - OnCreate: `state.RequireForUpdate<ShipPositionData>()` (per Pitfall 4)
   - OnUpdate:
     - `var shipPos = SystemAPI.GetSingleton<ShipPositionData>()`
     - Итерация: `SystemAPI.Query<RefRO<MoveData>, RefRW<GunData>, RefRO<ShootToData>>()`
     - Если gun.CurrentShoots <= 0 -- continue
     - Формула 1:1 из оригинала (hardcoded bullet speed 20):
```
var distance = math.length(shipPos.Position - move.Position);
var time = distance / (20f - shipPos.Speed);
var pendingPosition = shipPos.Position + (shipPos.Direction * shipPos.Speed) * time;
var direction = math.normalizesafe(pendingPosition - move.Position);

gun.Shooting = true;
gun.Direction = direction;
gun.ShootPosition = move.Position;
```
   - НЕ исправлять division by zero при shipPos.Speed == 20 (deferred QUAL-01)

2. Создать `Assets/Scripts/ECS/Systems/EcsMoveToSystem.cs`:
   - `public partial struct EcsMoveToSystem : ISystem` (per D-03, без [BurstCompile])
   - `[UpdateAfter(typeof(EcsShootToSystem))]` -- последняя в цепочке
   - OnCreate: `state.RequireForUpdate<ShipPositionData>()`
   - OnUpdate:
     - `var shipPos = SystemAPI.GetSingleton<ShipPositionData>()`
     - Итерация: `SystemAPI.Query<RefRW<MoveData>, RefRW<MoveToData>>()`
     - Формула 1:1:
```
moveTo.ReadyRemaining -= deltaTime;
if (moveTo.ReadyRemaining > 0) { continue; }
moveTo.ReadyRemaining = moveTo.Every;

var distance = math.length(shipPos.Position - move.Position);
var time = distance / (move.Speed - shipPos.Speed);
var pendingPosition = shipPos.Position + (shipPos.Direction * shipPos.Speed) * time;
move.Direction = math.normalizesafe(pendingPosition - move.Position);
```
   - НЕ исправлять division by zero при move.Speed == shipPos.Speed (deferred QUAL-01)

3. Создать `Assets/Tests/EditMode/ECS/ShootToSystemTests.cs`:
   - Наследует AsteroidsEcsTestFixture
   - SetUp: создать EcsShootToSystem, ShipPositionData singleton (position=(10,0), speed=5, direction=(1,0)), entity с MoveData + GunData + ShootToData
   - 3 теста:
     - `Update_HasAmmo_SetsShootingAndDirection`: gun.CurrentShoots=3, проверить gun.Shooting==true, gun.Direction != float2.zero
     - `Update_NoAmmo_DoesNotShoot`: gun.CurrentShoots=0, проверить gun.Shooting==false
     - `Update_SetsShootPositionToUfoPosition`: проверить gun.ShootPosition == move.Position
   - Для теста с конкретными значениями: ufo at (0,0), ship at (10,0), speed=5, direction=(1,0). time = 10 / (20-5) = 0.6667. pendingPosition = (10,0) + (1,0)*5*0.6667 = (13.333, 0). direction = normalize((13.333,0)-(0,0)) = (1,0). Проверить gun.Direction.x > 0.

4. Создать `Assets/Tests/EditMode/ECS/MoveToSystemTests.cs`:
   - Наследует AsteroidsEcsTestFixture
   - SetUp: создать EcsMoveToSystem, ShipPositionData singleton, entity с MoveData + MoveToData
   - 3 теста:
     - `Update_CooldownActive_DirectionUnchanged`: moveTo.ReadyRemaining=5, dt=1 -- Direction не меняется
     - `Update_CooldownExpired_DirectionUpdated`: moveTo.ReadyRemaining=0.5, dt=1 -- Direction пересчитывается
     - `Update_CooldownExpired_ReadyRemainingReset`: проверить ReadyRemaining == Every после пересчета
  </action>
  <verify>
    <automated>grep -c "\[Test\]" Assets/Tests/EditMode/ECS/ShootToSystemTests.cs Assets/Tests/EditMode/ECS/MoveToSystemTests.cs | awk -F: '{sum+=$2} END {if(sum >= 6) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - EcsShootToSystem.cs содержит `partial struct EcsShootToSystem : ISystem`
    - EcsShootToSystem.cs НЕ содержит `[BurstCompile]` (per D-03)
    - EcsShootToSystem.cs содержит `RequireForUpdate<ShipPositionData>` (per Pitfall 4)
    - EcsShootToSystem.cs содержит `GetSingleton<ShipPositionData>` (per D-08)
    - EcsShootToSystem.cs содержит хардкод `20f` для скорости пули (1:1 воспроизведение)
    - EcsMoveToSystem.cs содержит `partial struct EcsMoveToSystem : ISystem`
    - EcsMoveToSystem.cs содержит `RequireForUpdate<ShipPositionData>`
    - EcsMoveToSystem.cs содержит `moveTo.ValueRW.ReadyRemaining = moveTo.ValueRO.Every`
    - ShootToSystemTests.cs содержит минимум 3 теста [Test]
    - MoveToSystemTests.cs содержит минимум 3 теста [Test]
    - Тесты обходят division by zero (ship.Speed != 20, ufo.Speed != ship.Speed)
  </acceptance_criteria>
  <done>ShootToSystem и MoveToSystem портированы 1:1, 6+ тестов</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CollisionHandler и тесты</name>
  <files>
    Assets/Scripts/ECS/Components/CollisionEventData.cs,
    Assets/Scripts/ECS/Components/ScoreData.cs,
    Assets/Scripts/ECS/Components/Tags/DeadTag.cs,
    Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs,
    Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
  </files>
  <read_first>
    Assets/Scripts/Model/Model.cs,
    Assets/Scripts/ECS/Components/Tags/ShipTag.cs,
    Assets/Scripts/ECS/Components/Tags/AsteroidTag.cs,
    Assets/Scripts/ECS/Components/Tags/BulletTag.cs,
    Assets/Scripts/ECS/Components/Tags/PlayerBulletTag.cs,
    Assets/Scripts/ECS/Components/Tags/EnemyBulletTag.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    .planning/phases/04-ecs-foundation/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: PlayerBullet + Asteroid -- пуля получает DeadTag, asteroid получает DeadTag
    - Test: PlayerBullet + Asteroid -- ScoreData singleton увеличивается на Score астероида
    - Test: PlayerBullet + UfoBig -- пуля DeadTag, ufoBig DeadTag, score увеличивается
    - Test: EnemyBullet + Ship -- пуля DeadTag, ship DeadTag
    - Test: Ship + Asteroid -- ship DeadTag (collision)
    - Test: Нет CollisionEventData -- ничего не происходит
  </behavior>
  <action>
1. Создать `Assets/Scripts/ECS/Components/CollisionEventData.cs` (per D-13, D-14):
   - Namespace: `SelStrom.Asteroids.ECS`
   - Mock-компонент для передачи коллизионных данных. В Phase 5 CollisionBridge будет создавать эти данные из Physics2D.
   - Использовать IBufferElementData для списка событий на entity-синглтоне:
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct CollisionEventData : IBufferElementData
    {
        public Entity EntityA;
        public Entity EntityB;
    }
}
```

2. Создать `Assets/Scripts/ECS/Components/ScoreData.cs` (singleton для подсчета очков):
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct ScoreData : IComponentData
    {
        public int Value;
    }

    public struct ScoreValue : IComponentData
    {
        public int Score;
    }
}
```
ScoreValue добавляется к entities с очками (Asteroid, Ufo, UfoBig) -- аналог `BaseGameEntityData.Score`.

3. Создать `Assets/Scripts/ECS/Components/Tags/DeadTag.cs`:
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct DeadTag : IComponentData { }
}
```

4. Создать `Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs` (per D-13, D-14):
   - `public partial struct EcsCollisionHandlerSystem : ISystem` (без [BurstCompile])
   - `[UpdateAfter(typeof(EcsMoveToSystem))]` -- последняя система
   - OnCreate: `state.RequireForUpdate<ScoreData>()`
   - OnUpdate:
     - Получить DynamicBuffer<CollisionEventData> из singleton entity (если существует)
     - Для каждого события определить типы столкнувшихся entities через HasComponent<Tag>
     - Логика (1:1 из Game.cs):
       a) PlayerBullet + Asteroid/Ufo/UfoBig: пуля DeadTag, враг DeadTag, score += enemy.ScoreValue.Score
       b) EnemyBullet + Ship: пуля DeadTag, ship DeadTag
       c) Ship + Asteroid/Ufo/UfoBig: ship DeadTag
     - После обработки очистить буфер
     - Добавление DeadTag: `EntityManager.AddComponent<DeadTag>(entity)` или `state.EntityManager.AddComponent<DeadTag>(entity)`. Если entity уже имеет DeadTag -- пропустить (HasComponent check).

5. Создать `Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs`:
   - Наследует AsteroidsEcsTestFixture
   - SetUp: создать EcsCollisionHandlerSystem, ScoreData singleton (Value=0), entity-синглтон с DynamicBuffer<CollisionEventData>
   - Helper: AddCollisionEvent(Entity a, Entity b) -- добавляет в буфер
   - 6 тестов по поведениям
   - Для создания тестовых entities: использовать EntityFactory или вручную через m_Manager
   - Asteroid entity должен иметь ScoreValue (Score = 100)
  </action>
  <verify>
    <automated>grep -c "\[Test\]" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs | xargs -I{} test {} -ge 5 && grep -q "struct EcsCollisionHandlerSystem : ISystem" Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CollisionEventData.cs содержит `struct CollisionEventData : IBufferElementData` с полями EntityA, EntityB
    - ScoreData.cs содержит `struct ScoreData : IComponentData` с полем Value
    - ScoreData.cs содержит `struct ScoreValue : IComponentData` с полем Score
    - DeadTag.cs содержит `struct DeadTag : IComponentData { }`
    - EcsCollisionHandlerSystem.cs содержит `partial struct EcsCollisionHandlerSystem : ISystem`
    - EcsCollisionHandlerSystem.cs НЕ содержит `[BurstCompile]`
    - EcsCollisionHandlerSystem.cs содержит `AddComponent<DeadTag>`
    - EcsCollisionHandlerSystem.cs содержит логику определения типов через `HasComponent<PlayerBulletTag>`, `HasComponent<AsteroidTag>` и т.д.
    - CollisionHandlerTests.cs содержит минимум 5 тестов [Test]
    - Тесты покрывают: PlayerBullet+Asteroid, PlayerBullet+Ufo, EnemyBullet+Ship, Ship+Asteroid, пустой буфер
  </acceptance_criteria>
  <done>CollisionHandler с mock CollisionEventData, DeadTag маркировка, score подсчет, 5+ тестов</done>
</task>

</tasks>

<verification>
- `grep -r "ISystem" Assets/Scripts/ECS/Systems/Ecs{ShootTo,MoveTo,CollisionHandler}System.cs | wc -l` == 3
- НИ ОДНА из трех систем не содержит [BurstCompile]
- `grep -r "\[Test\]" Assets/Tests/EditMode/ECS/{ShootTo,MoveTo,CollisionHandler}*Tests.cs | wc -l` >= 11
- ShootToSystem и MoveToSystem содержат `GetSingleton<ShipPositionData>`
- CollisionHandlerSystem содержит `AddComponent<DeadTag>`
</verification>

<success_criteria>
1. EcsShootToSystem с упреждающим прицеливанием через ShipPositionData singleton + 3 теста
2. EcsMoveToSystem с периодическим пересчетом Direction + 3 теста
3. EcsCollisionHandlerSystem с mock CollisionEventData buffer + 5 тестов
4. CollisionEventData, ScoreData, ScoreValue, DeadTag компоненты
5. Полная цепочка UpdateAfter: Laser -> ShootTo -> MoveTo -> CollisionHandler
</success_criteria>

<output>
After completion, create `.planning/phases/04-ecs-foundation/04-04-SUMMARY.md`
</output>
