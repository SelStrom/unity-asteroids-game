---
phase: 04-ecs-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Assets/Scripts/ECS/EntityFactory.cs
  - Assets/Scripts/ECS/Systems/EcsRotateSystem.cs
  - Assets/Scripts/ECS/Systems/EcsThrustSystem.cs
  - Assets/Scripts/ECS/Systems/EcsMoveSystem.cs
  - Assets/Scripts/ECS/Systems/EcsShipPositionUpdateSystem.cs
  - Assets/Scripts/ECS/Systems/EcsLifeTimeSystem.cs
  - Assets/Tests/EditMode/ECS/EntityFactoryTests.cs
  - Assets/Tests/EditMode/ECS/RotateSystemTests.cs
  - Assets/Tests/EditMode/ECS/ThrustSystemTests.cs
  - Assets/Tests/EditMode/ECS/MoveSystemTests.cs
autonomous: true
requirements:
  - ECS-03
  - ECS-04
  - ECS-05
  - ECS-06
  - TST-02
  - TST-03
  - TST-04

must_haves:
  truths:
    - "EntityFactory создает entities с правильными наборами компонентов для каждого типа сущности"
    - "EntityFactory добавляет ScoreValue к Asteroid, UfoBig и Ufo entities"
    - "RotateSystem поворачивает entity по/против часовой стрелки с 90 grad/sec"
    - "ThrustSystem ускоряет entity и замедляет при отключении тяги"
    - "MoveSystem перемещает entity и оборачивает через границы тороидального пространства"
    - "EcsShipPositionUpdateSystem обновляет ShipPositionData singleton из данных ShipTag entity после MoveSystem"
    - "LifeTimeSystem уменьшает оставшееся время жизни"
    - "Все 3 Burst-системы компилируются с [BurstCompile]"
  artifacts:
    - path: "Assets/Scripts/ECS/EntityFactory.cs"
      provides: "Фабрика ECS-сущностей"
      exports: ["CreateShip", "CreateAsteroid", "CreateBullet", "CreateUfoBig", "CreateUfo"]
    - path: "Assets/Scripts/ECS/Systems/EcsRotateSystem.cs"
      provides: "Система вращения с Burst"
      contains: "[BurstCompile]"
    - path: "Assets/Scripts/ECS/Systems/EcsThrustSystem.cs"
      provides: "Система тяги с Burst"
      contains: "[BurstCompile]"
    - path: "Assets/Scripts/ECS/Systems/EcsMoveSystem.cs"
      provides: "Система движения с Burst и тороидальным wrapping"
      contains: "[BurstCompile]"
    - path: "Assets/Scripts/ECS/Systems/EcsShipPositionUpdateSystem.cs"
      provides: "Обновление ShipPositionData singleton из ShipTag entity"
      contains: "struct EcsShipPositionUpdateSystem : ISystem"
    - path: "Assets/Scripts/ECS/Systems/EcsLifeTimeSystem.cs"
      provides: "Система времени жизни пуль"
      contains: "struct EcsLifeTimeSystem : ISystem"
  key_links:
    - from: "EcsMoveSystem"
      to: "GameAreaData"
      via: "SystemAPI.GetSingleton"
      pattern: "GetSingleton<GameAreaData>"
    - from: "EcsThrustSystem"
      to: "RotateData"
      via: "SystemAPI.Query RefRO"
      pattern: "RefRO<RotateData>"
    - from: "EcsShipPositionUpdateSystem"
      to: "ShipPositionData"
      via: "SystemAPI.SetSingleton"
      pattern: "SetSingleton<ShipPositionData>"
    - from: "EntityFactory.CreateAsteroid"
      to: "ScoreValue"
      via: "AddComponentData"
      pattern: "AddComponentData.*ScoreValue"
---

<objective>
Создать EntityFactory (с ScoreValue для Asteroid/Ufo/UfoBig) и 5 систем: EcsRotateSystem, EcsThrustSystem, EcsMoveSystem (все три с Burst), EcsShipPositionUpdateSystem (без Burst, обновляет singleton), EcsLifeTimeSystem. Покрыть EditMode-тестами.

Purpose: Ядро ECS-движения -- Rotate/Thrust/Move -- критический путь для всех сущностей. EntityFactory нужен для единообразного создания entities. ScoreValue на entities необходим для подсчета очков в CollisionHandler (Plan 04).
Output: 5 систем + фабрика + тесты, Burst-компиляция на 3 системах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecs-foundation/04-CONTEXT.md
@.planning/phases/04-ecs-foundation/04-RESEARCH.md
@.planning/phases/04-ecs-foundation/04-01-SUMMARY.md

<interfaces>
<!-- Компоненты из Plan 01 -- executor должен прочитать эти файлы -->
<!-- Оригинальные системы для портирования логики 1:1 -->

From Assets/Scripts/Model/Systems/RotateSystem.cs:
```csharp
protected override void UpdateNode(RotateComponent node, float deltaTime)
{
    if (node.TargetDirection == 0) { return; }
    node.Rotation.Value =
        Quaternion.Euler(0, 0, RotateComponent.DegreePerSecond * deltaTime * node.TargetDirection) *
        node.Rotation.Value;
}
```

From Assets/Scripts/Model/Systems/ThrustSystem.cs:
```csharp
protected override void UpdateNode((...) node, float deltaTime)
{
    if (node.Thrust.IsActive.Value)
    {
        var acceleration = node.Thrust.UnitsPerSecond * deltaTime;
        var velocity = node.Move.Direction * node.Move.Speed.Value + node.Rotate.Rotation.Value * acceleration;
        node.Move.Direction = velocity.normalized;
        node.Move.Speed.Value = Math.Min(velocity.magnitude, node.Thrust.MaxSpeed);
    }
    else
    {
        node.Move.Speed.Value = Math.Max(node.Move.Speed.Value - node.Thrust.UnitsPerSecond / 2 * deltaTime, ThrustComponent.MinSpeed);
    }
}
```

From Assets/Scripts/Model/Systems/MoveSystem.cs:
```csharp
protected override void UpdateNode(MoveComponent node, float deltaTime)
{
    var position = node.Position.Value + node.Direction * (node.Speed.Value * deltaTime);
    Model.PlaceWithinGameArea(ref position.x, _owner.GameArea.x);
    Model.PlaceWithinGameArea(ref position.y, _owner.GameArea.y);
    node.Position.Value = position;
}

public static void PlaceWithinGameArea(ref float position, float side)
{
    if (position > side / 2) { position = -side + position; }
    if (position < -side / 2) { position = side - position; }
}
```

From Assets/Scripts/Model/Systems/LifeTimeSystem.cs:
```csharp
protected override void UpdateNode(LifeTimeComponent node, float deltaTime)
{
    node.TimeRemaining = Math.Max(node.TimeRemaining - deltaTime, 0);
}
```

From Assets/Scripts/Model/Model.cs (порядок регистрации систем):
```csharp
RegisterSystem<RotateSystem>();
RegisterSystem<ThrustSystem>();
RegisterSystem<MoveSystem>().Connect(this);
RegisterSystem<LifeTimeSystem>();
RegisterSystem<GunSystem>();
RegisterSystem<LaserSystem>();
RegisterSystem<ShootToSystem>();
RegisterSystem<MoveToSystem>();
```

From Assets/Scripts/Model/Model.cs (маппинг компонентов по сущностям):
```csharp
// Ship -> Move+Rotate+Thrust+Gun+Laser
// Asteroid -> Move (+ Age в AsteroidModel)
// Bullet -> Move+LifeTime
// UfoBig -> Move+Gun+ShootTo
// Ufo -> Move+Gun+ShootTo+MoveTo
```

From Assets/Scripts/ECS/Components/ScoreData.cs (из Plan 01):
```csharp
public struct ScoreValue : IComponentData
{
    public int Score;
}
```
ScoreValue добавляется к entities с очками (Asteroid, Ufo, UfoBig) в EntityFactory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntityFactory и тесты</name>
  <files>
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Tests/EditMode/ECS/EntityFactoryTests.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Scripts/ECS/Components/ThrustData.cs,
    Assets/Scripts/ECS/Components/RotateData.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Components/LaserData.cs,
    Assets/Scripts/ECS/Components/ShootToData.cs,
    Assets/Scripts/ECS/Components/MoveToData.cs,
    Assets/Scripts/ECS/Components/LifeTimeData.cs,
    Assets/Scripts/ECS/Components/AgeData.cs,
    Assets/Scripts/ECS/Components/ScoreData.cs,
    Assets/Scripts/ECS/Components/Tags/ShipTag.cs,
    Assets/Scripts/Model/Entities/ShipModel.cs,
    Assets/Scripts/Model/Model.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    .planning/phases/04-ecs-foundation/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: CreateShip -- entity имеет ShipTag, MoveData, RotateData, ThrustData, GunData, LaserData (6 компонентов)
    - Test: CreateShip -- MoveData.Position == переданная позиция, RotateData.Rotation == (1,0)
    - Test: CreateAsteroid -- entity имеет AsteroidTag, MoveData, AgeData, ScoreValue (4 компонента)
    - Test: CreateAsteroid -- AgeData.Age == переданный age, ScoreValue.Score == переданный score
    - Test: CreateBullet(isPlayer=true) -- entity имеет BulletTag, PlayerBulletTag, MoveData, LifeTimeData
    - Test: CreateBullet(isPlayer=false) -- entity имеет BulletTag, EnemyBulletTag, MoveData, LifeTimeData
    - Test: CreateUfoBig -- entity имеет UfoBigTag, MoveData, GunData, ShootToData, ScoreValue (5 компонентов)
    - Test: CreateUfoBig -- ScoreValue.Score == переданный score
    - Test: CreateUfo -- entity имеет UfoTag, MoveData, GunData, ShootToData, MoveToData, ScoreValue (6 компонентов)
    - Test: CreateUfo -- ScoreValue.Score == переданный score
  </behavior>
  <action>
1. Создать `Assets/Scripts/ECS/EntityFactory.cs` (per D-15, D-16):
   - Namespace: `SelStrom.Asteroids.ECS`
   - `public static class EntityFactory` с методами:

```csharp
public static Entity CreateShip(EntityManager em,
    float2 position, float moveSpeed, float thrustAcceleration,
    float thrustMaxSpeed, int gunMaxShoots, float gunReloadSec,
    int laserMaxShoots, float laserReloadSec)
```
Создает entity с: ShipTag, MoveData (Position=position, Speed=moveSpeed, Direction=(1,0)), RotateData (Rotation=(1,0)), ThrustData (UnitsPerSecond=thrustAcceleration, MaxSpeed=thrustMaxSpeed), GunData (MaxShoots=gunMaxShoots, ReloadDurationSec=gunReloadSec, CurrentShoots=gunMaxShoots, ReloadRemaining=gunReloadSec), LaserData (MaxShoots=laserMaxShoots, UpdateDurationSec=laserReloadSec, CurrentShoots=laserMaxShoots, ReloadRemaining=laserReloadSec).
Корабль НЕ получает ScoreValue -- у него нет очков.

```csharp
public static Entity CreateAsteroid(EntityManager em,
    float2 position, float speed, float2 direction, int age, int score)
```
Создает entity с: AsteroidTag, MoveData (Position, Speed, Direction), AgeData (Age), **ScoreValue (Score=score)**.

```csharp
public static Entity CreateBullet(EntityManager em,
    float2 position, float speed, float2 direction, float lifeTime, bool isPlayer)
```
Создает entity с: BulletTag, MoveData, LifeTimeData (TimeRemaining=lifeTime). Если isPlayer=true -- добавляет PlayerBulletTag, иначе EnemyBulletTag.
Пуля НЕ получает ScoreValue -- у неё нет очков.

```csharp
public static Entity CreateUfoBig(EntityManager em,
    float2 position, float speed, float2 direction,
    int gunMaxShoots, float gunReloadSec, float shootToEvery, int score)
```
Создает entity с: UfoBigTag, MoveData, GunData (MaxShoots, ReloadDurationSec, CurrentShoots=MaxShoots, ReloadRemaining=ReloadDurationSec), ShootToData (Every=shootToEvery, ReadyRemaining=shootToEvery), **ScoreValue (Score=score)**.

```csharp
public static Entity CreateUfo(EntityManager em,
    float2 position, float speed, float2 direction,
    int gunMaxShoots, float gunReloadSec, float shootToEvery, float moveToEvery, int score)
```
Создает entity с: UfoTag, MoveData, GunData, ShootToData, MoveToData (Every=moveToEvery, ReadyRemaining=moveToEvery), **ScoreValue (Score=score)**.

ВАЖНО: Каждый метод должен вызывать `em.AddComponentData<ScoreValue>(entity, new ScoreValue { Score = score })` для Asteroid, UfoBig, Ufo. Это критично -- CollisionHandler (Plan 04) читает ScoreValue.Score для подсчёта очков. Без ScoreValue `GetComponentData<ScoreValue>(entity)` бросит исключение.

2. Создать `Assets/Tests/EditMode/ECS/EntityFactoryTests.cs`:
   - Наследует AsteroidsEcsTestFixture
   - 10 тестов по поведениям выше, используя `m_Manager.HasComponent<T>(entity)` и `m_Manager.GetComponentData<T>(entity)` для проверки
   - Тесты ScoreValue: проверить `m_Manager.HasComponent<ScoreValue>(entity)` == true для Asteroid/UfoBig/Ufo и проверить `m_Manager.GetComponentData<ScoreValue>(entity).Score == expectedScore`
   - Тест: CreateShip entity НЕ имеет ScoreValue
   - Тест: CreateBullet entity НЕ имеет ScoreValue
  </action>
  <verify>
    <automated>grep -c "\[Test\]" Assets/Tests/EditMode/ECS/EntityFactoryTests.cs | xargs -I{} test {} -ge 8 && grep -q "static class EntityFactory" Assets/Scripts/ECS/EntityFactory.cs && grep -q "ScoreValue" Assets/Scripts/ECS/EntityFactory.cs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - EntityFactory.cs содержит `public static class EntityFactory` в namespace `SelStrom.Asteroids.ECS`
    - EntityFactory.cs содержит методы CreateShip, CreateAsteroid, CreateBullet, CreateUfoBig, CreateUfo
    - EntityFactory.CreateAsteroid принимает параметр `int score` и вызывает `AddComponentData<ScoreValue>(entity, new ScoreValue { Score = score })`
    - EntityFactory.CreateUfoBig принимает параметр `int score` и добавляет ScoreValue
    - EntityFactory.CreateUfo принимает параметр `int score` и добавляет ScoreValue
    - EntityFactory.CreateShip и CreateBullet НЕ добавляют ScoreValue
    - EntityFactoryTests.cs содержит минимум 8 тестов [Test]
    - Тесты проверяют HasComponent для всех tag-ов и data-компонентов каждого типа entity
    - Тесты проверяют ScoreValue.Score == переданный score для Asteroid, UfoBig, Ufo
  </acceptance_criteria>
  <done>EntityFactory создает entities с корректными archetypes включая ScoreValue, 8+ тестов покрывают все типы сущностей</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Burst-системы (Rotate, Thrust, Move), ShipPositionUpdate и тесты</name>
  <files>
    Assets/Scripts/ECS/Systems/EcsRotateSystem.cs,
    Assets/Scripts/ECS/Systems/EcsThrustSystem.cs,
    Assets/Scripts/ECS/Systems/EcsMoveSystem.cs,
    Assets/Scripts/ECS/Systems/EcsShipPositionUpdateSystem.cs,
    Assets/Scripts/ECS/Systems/EcsLifeTimeSystem.cs,
    Assets/Tests/EditMode/ECS/RotateSystemTests.cs,
    Assets/Tests/EditMode/ECS/ThrustSystemTests.cs,
    Assets/Tests/EditMode/ECS/MoveSystemTests.cs
  </files>
  <read_first>
    Assets/Scripts/Model/Systems/RotateSystem.cs,
    Assets/Scripts/Model/Systems/ThrustSystem.cs,
    Assets/Scripts/Model/Systems/MoveSystem.cs,
    Assets/Scripts/Model/Model.cs,
    Assets/Scripts/ECS/Components/MoveData.cs,
    Assets/Scripts/ECS/Components/RotateData.cs,
    Assets/Scripts/ECS/Components/ThrustData.cs,
    Assets/Scripts/ECS/Components/GameAreaData.cs,
    Assets/Scripts/ECS/Components/ShipPositionData.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    .planning/phases/04-ecs-foundation/04-RESEARCH.md
  </read_first>
  <behavior>
    RotateSystem:
    - Test: TargetDirection=1 (против часовой), deltaTime=1.0 -- Rotation поворачивается на 90 градусов от (1,0) к (0,1)
    - Test: TargetDirection=-1 (по часовой), deltaTime=1.0 -- Rotation поворачивается на -90 градусов от (1,0) к (0,-1)
    - Test: TargetDirection=0 -- Rotation не меняется (остается (1,0))

    ThrustSystem:
    - Test: IsActive=true, acceleration применяется к velocity, Speed растет
    - Test: IsActive=true, Speed не превышает MaxSpeed (clamp)
    - Test: IsActive=false, Speed уменьшается на UnitsPerSecond/2 * dt, не ниже 0

    MoveSystem:
    - Test: entity с Speed=10, Direction=(1,0), dt=0.1 -- Position.x += 1.0
    - Test: Position за правой границей (>side/2) -- wrap на левую сторону (1:1 с формулой оригинала)
    - Test: Position за левой границей (<-side/2) -- wrap на правую сторону

    ShipPositionUpdateSystem:
    - Test: Ship entity с MoveData, ShipPositionData singleton -- после update singleton содержит данные из Ship MoveData
  </behavior>
  <action>
1. Создать `Assets/Scripts/ECS/Systems/EcsRotateSystem.cs` (per D-01, D-02, D-05):
   - `[BurstCompile]` на struct И на OnUpdate
   - `[UpdateBefore(typeof(EcsThrustSystem))]` -- первая в цепочке (per Model.cs порядок)
   - `public partial struct EcsRotateSystem : ISystem`
   - OnUpdate: итерация `SystemAPI.Query<RefRW<RotateData>>()`, если TargetDirection == 0 -- continue
   - Формула вращения 2D (НЕ Quaternion, а математически):
     ```
     angle = math.radians(RotateData.DegreePerSecond * deltaTime * targetDirection)
     cos = math.cos(angle); sin = math.sin(angle)
     newRotation = float2(current.x * cos - current.y * sin, current.x * sin + current.y * cos)
     ```
   - Pitfall 3: использовать `math.radians()` -- НЕ градусы напрямую
   - OnCreate/OnDestroy -- пустые

2. Создать `Assets/Scripts/ECS/Systems/EcsThrustSystem.cs` (per D-01, D-02):
   - `[BurstCompile]` на struct И на OnUpdate
   - `[UpdateBefore(typeof(EcsMoveSystem))]` `[UpdateAfter(typeof(EcsRotateSystem))]`
   - Итерация: `SystemAPI.Query<RefRO<ThrustData>, RefRW<MoveData>, RefRO<RotateData>>()`
   - Если IsActive == true:
     ```
     acceleration = UnitsPerSecond * deltaTime
     velocity = Direction * Speed + Rotation * acceleration
     Direction = math.normalizesafe(velocity)
     Speed = math.min(math.length(velocity), MaxSpeed)
     ```
   - Если IsActive == false:
     ```
     Speed = math.max(Speed - UnitsPerSecond / 2f * deltaTime, ThrustData.MinSpeed)
     ```

3. Создать `Assets/Scripts/ECS/Systems/EcsMoveSystem.cs` (per D-01, D-02, D-06):
   - `[BurstCompile]` на struct И на OnUpdate
   - `[UpdateBefore(typeof(EcsShipPositionUpdateSystem))]` `[UpdateAfter(typeof(EcsThrustSystem))]`
   - OnCreate: `state.RequireForUpdate<GameAreaData>()`
   - OnUpdate:
     a) Получить GameAreaData singleton: `var gameArea = SystemAPI.GetSingleton<GameAreaData>();`
     b) Итерация всех MoveData: position += Direction * Speed * dt, PlaceWithinGameArea по x и y
   - `private static void PlaceWithinGameArea(ref float position, float side)` -- 1:1 копия оригинала (включая известный баг):
     ```
     if (position > side / 2) { position = -side + position; }
     if (position < -side / 2) { position = side - position; }
     ```
   - EcsMoveSystem НЕ обновляет ShipPositionData -- это делает отдельная система (per D-09).

4. Создать `Assets/Scripts/ECS/Systems/EcsShipPositionUpdateSystem.cs` (per D-09):
   - `public partial struct EcsShipPositionUpdateSystem : ISystem` (БЕЗ [BurstCompile] -- использует SetSingleton)
   - `[UpdateAfter(typeof(EcsMoveSystem))]` `[UpdateBefore(typeof(EcsLifeTimeSystem))]`
   - OnCreate: `state.RequireForUpdate<ShipPositionData>()`
   - OnUpdate:
     ```
     foreach (var move in SystemAPI.Query<RefRO<MoveData>>().WithAll<ShipTag>())
     {
         SystemAPI.SetSingleton(new ShipPositionData
         {
             Position = move.ValueRO.Position,
             Speed = move.ValueRO.Speed,
             Direction = move.ValueRO.Direction
         });
     }
     ```
   - Этот подход чист и однозначен: EcsMoveSystem остаётся полностью Burst-совместимым, а обновление singleton вынесено в отдельную лёгкую систему.

5. Создать `Assets/Scripts/ECS/Systems/EcsLifeTimeSystem.cs`:
   - `public partial struct EcsLifeTimeSystem : ISystem`
   - `[BurstCompile]` на struct и OnUpdate
   - `[UpdateAfter(typeof(EcsShipPositionUpdateSystem))]` `[UpdateBefore(typeof(EcsGunSystem))]`
   - OnUpdate: `foreach (var lifeTime in SystemAPI.Query<RefRW<LifeTimeData>>())` -- `TimeRemaining = math.max(TimeRemaining - dt, 0)`

6. Тесты -- по 3 файла:

**RotateSystemTests.cs**: наследует AsteroidsEcsTestFixture
- SetUp: создать систему EcsRotateSystem, entity с RotateData (Rotation = (1,0))
- Контроль deltaTime через `World.PushTime(new TimeData(elapsed, delta))` / `World.PopTime()`. Если PushTime недоступен -- использовать рефлексию или альтернативный подход.
- Для update: `systemHandle.Update(World.Unmanaged)`

**ThrustSystemTests.cs**: наследует AsteroidsEcsTestFixture
- SetUp: создать EcsThrustSystem, entity с ThrustData + MoveData + RotateData

**MoveSystemTests.cs**: наследует AsteroidsEcsTestFixture
- SetUp: создать EcsMoveSystem + EcsShipPositionUpdateSystem, GameAreaData singleton (20x15), ShipPositionData singleton
- Тест wrapping: entity на 9.5 + скорость 10 * dt=0.1 = 10.5 > 10 (half=10) -> wrap = -20 + 10.5 = -9.5
- Тест ShipPositionData: создать Ship entity (ShipTag + MoveData), прогнать MoveSystem + ShipPositionUpdateSystem, проверить что ShipPositionData singleton обновился
  </action>
  <verify>
    <automated>test -f Assets/Scripts/ECS/Systems/EcsRotateSystem.cs && test -f Assets/Scripts/ECS/Systems/EcsThrustSystem.cs && test -f Assets/Scripts/ECS/Systems/EcsMoveSystem.cs && test -f Assets/Scripts/ECS/Systems/EcsShipPositionUpdateSystem.cs && test -f Assets/Scripts/ECS/Systems/EcsLifeTimeSystem.cs && grep -c "\[BurstCompile\]" Assets/Scripts/ECS/Systems/EcsRotateSystem.cs | grep -q "[12]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - EcsRotateSystem.cs содержит `[BurstCompile]` и `partial struct EcsRotateSystem : ISystem`
    - EcsRotateSystem.cs содержит `math.radians(RotateData.DegreePerSecond` (не градусы напрямую)
    - EcsRotateSystem.cs содержит `[UpdateBefore(typeof(EcsThrustSystem))]`
    - EcsThrustSystem.cs содержит `[BurstCompile]` и `[UpdateAfter(typeof(EcsRotateSystem))]`
    - EcsThrustSystem.cs содержит `math.normalizesafe` и `math.min` и `ThrustData.MinSpeed`
    - EcsMoveSystem.cs содержит `[BurstCompile]` и `PlaceWithinGameArea`
    - EcsMoveSystem.cs содержит `RequireForUpdate<GameAreaData>`
    - EcsMoveSystem.cs содержит `GetSingleton<GameAreaData>` или `SystemAPI.Query` для GameAreaData
    - EcsMoveSystem.cs НЕ содержит обращений к ShipPositionData (вынесено в отдельную систему)
    - EcsShipPositionUpdateSystem.cs содержит `partial struct EcsShipPositionUpdateSystem : ISystem`
    - EcsShipPositionUpdateSystem.cs НЕ содержит `[BurstCompile]`
    - EcsShipPositionUpdateSystem.cs содержит `[UpdateAfter(typeof(EcsMoveSystem))]`
    - EcsShipPositionUpdateSystem.cs содержит `SystemAPI.SetSingleton(new ShipPositionData` или `SetSingleton<ShipPositionData>`
    - EcsShipPositionUpdateSystem.cs содержит `.WithAll<ShipTag>()`
    - EcsLifeTimeSystem.cs содержит `ISystem` и `math.max`
    - RotateSystemTests.cs содержит минимум 3 теста [Test]
    - ThrustSystemTests.cs содержит минимум 3 теста [Test]
    - MoveSystemTests.cs содержит минимум 3 теста [Test] включая тест wrapping и тест ShipPositionData обновления
    - Порядок систем: Rotate -> Thrust -> Move -> ShipPositionUpdate -> LifeTime через [UpdateBefore]/[UpdateAfter]
  </acceptance_criteria>
  <done>5 систем (3 с Burst + ShipPositionUpdate без Burst + LifeTime с Burst), порядок Rotate->Thrust->Move->ShipPositionUpdate->LifeTime, 9+ тестов</done>
</task>

</tasks>

<verification>
- `grep -r "\[BurstCompile\]" Assets/Scripts/ECS/Systems/Ecs{Rotate,Thrust,Move}System.cs | wc -l` >= 6 (struct + OnUpdate)
- `grep -r "ISystem" Assets/Scripts/ECS/Systems/*.cs | wc -l` >= 5
- EcsShipPositionUpdateSystem.cs существует и содержит SetSingleton<ShipPositionData>
- EcsMoveSystem.cs НЕ содержит ShipPositionData
- `grep -r "\[Test\]" Assets/Tests/EditMode/ECS/{Rotate,Thrust,Move}SystemTests.cs Assets/Tests/EditMode/ECS/EntityFactoryTests.cs | wc -l` >= 17
- `grep -q "ScoreValue" Assets/Scripts/ECS/EntityFactory.cs` -- EntityFactory добавляет ScoreValue
- UpdateBefore/UpdateAfter цепочка: Rotate -> Thrust -> Move -> ShipPositionUpdate -> LifeTime
</verification>

<success_criteria>
1. EntityFactory с 5 методами создания сущностей (включая ScoreValue для Asteroid/UfoBig/Ufo) + 8+ тестов
2. EcsRotateSystem с [BurstCompile] + 3 теста вращения
3. EcsThrustSystem с [BurstCompile] + 3 теста тяги/торможения
4. EcsMoveSystem с [BurstCompile] + toroidal wrap + 3+ теста (БЕЗ ShipPositionData обновления)
5. EcsShipPositionUpdateSystem без Burst, [UpdateAfter(EcsMoveSystem)] -- обновляет ShipPositionData singleton из ShipTag entity
6. EcsLifeTimeSystem с [BurstCompile]
7. Порядок систем: Rotate -> Thrust -> Move -> ShipPositionUpdate -> LifeTime
</success_criteria>

<output>
After completion, create `.planning/phases/04-ecs-foundation/04-02-SUMMARY.md`
</output>
