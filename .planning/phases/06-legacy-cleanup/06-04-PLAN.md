---
phase: 06-legacy-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified: []
autonomous: false
requirements: [LC-06, LC-07]

must_haves:
  truths:
    - "Все EditMode и PlayMode тесты проходят зеленым"
    - "Игра воспроизводит весь геймплей 1:1 без legacy-слоя"
  artifacts: []
  key_links: []
---

<objective>
Верификация Phase 6: запуск всех тестов и ручная проверка геймплея.

Purpose: Убедиться, что удаление legacy-слоя не сломало функциональность.
Output: Подтверждение работоспособности.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-legacy-cleanup/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Запуск всех тестов</name>
  <files></files>
  <read_first>
    - Assets/Tests/EditMode/ECS/EcsBridgeRegressionTests.cs
    - Assets/Tests/PlayMode/GameplayCycleTests.cs
  </read_first>
  <action>
Запустить полный набор тестов через Unity-MCP:

1. EditMode тесты: все ECS-тесты (компоненты, системы, bridge, regression)
2. PlayMode тесты: GameplayCycleTests (полный игровой цикл)

Если тесты падают -- исправить и перезапустить.
  </action>
  <verify>
    <automated>echo "Run tests via Unity-MCP: EditMode + PlayMode suites"</automated>
  </verify>
  <acceptance_criteria>
    - Все EditMode тесты проходят зеленым (0 failures)
    - Все PlayMode тесты проходят зеленым (0 failures)
    - Нет compilation errors в Unity Console
  </acceptance_criteria>
  <done>Все тесты зелёные, компиляция чистая</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ручная верификация геймплея 1:1</name>
  <files></files>
  <action>
Пользователь проверяет геймплей в Unity Editor после удаления legacy-слоя.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint"</automated>
  </verify>
  <done>Пользователь подтвердил геймплей 1:1 без legacy-слоя</done>
  <what-built>
    Полное удаление legacy MonoBehaviour-слоя:
    - 26 legacy-файлов удалены (9 систем, 6 моделей+интерфейсов, 9 компонентов, Model.cs, ModelFactory.cs)
    - Единый ECS data path без _useEcs переключателя
    - ActionScheduler как standalone managed-класс
    - Score читается напрямую из ECS ScoreData singleton
    - Тесты рефакторены на ECS-only
  </what-built>
  <how-to-verify>
    1. Открыть проект в Unity Editor -- проверить 0 ошибок компиляции в Console
    2. Запустить игру (Play Mode в Editor):
       - Корабль появляется, управление WASD/Space/Q работает
       - Астероиды спавнятся и двигаются
       - Пули стреляются и уничтожают астероиды (дробление работает)
       - Лазер стреляется и уничтожает объекты
       - НЛО (маленький и большой) спавнятся и стреляют
       - Score отображается в HUD и увеличивается при уничтожении врагов
       - При гибели корабля появляется EndGame экран с правильным Score
       - Рестарт работает корректно (Space на EndGame экране)
       - Тороидальный wrap работает (объекты телепортируются через края)
    3. Проверить лидерборд (если есть интернет): Score отправляется, таблица отображается
  </how-to-verify>
  <resume-signal>Введите "approved" если геймплей 1:1, или опишите проблемы</resume-signal>
</task>

</tasks>

<verification>
- EditMode + PlayMode тесты: 0 failures
- Ручная верификация: геймплей 1:1 без legacy-слоя
</verification>

<success_criteria>
- Все тесты зелёные
- Пользователь подтвердил геймплей 1:1
- Phase 6 завершена
</success_criteria>

<output>
After completion, create `.planning/phases/06-legacy-cleanup/06-04-SUMMARY.md`
</output>
