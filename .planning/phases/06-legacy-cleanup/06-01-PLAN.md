---
phase: 06-legacy-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Application/Application.cs
  - Assets/Scripts/Application/Game.cs
autonomous: true
requirements: [LC-03, LC-04]

must_haves:
  truths:
    - "Переключатель _useEcs и все else-ветки с legacy code paths удалены"
    - "ActionScheduler используется как standalone поле, не через Model"
    - "_model.GameArea заменен на _gameArea Vector2"
    - "Проект компилируется и ECS-тесты проходят"
  artifacts:
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "Standalone ActionScheduler, _gameArea, без _useEcs"
      contains: "private ActionScheduler _actionScheduler"
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "Без _useEcs, без else-веток, ActionScheduler/GameArea как поля"
      contains: "private readonly ActionScheduler _actionScheduler"
  key_links:
    - from: "Application.cs"
      to: "Game.cs"
      via: "ActionScheduler и gameArea передаются в конструктор Game"
      pattern: "new Game.*actionScheduler.*gameArea"
---

<objective>
Удалить переключатель _useEcs и все legacy else-ветки из Application.cs и Game.cs. Извлечь ActionScheduler из Model в standalone поле. Заменить _model.GameArea на прямое поле _gameArea.

Purpose: Устранение dual data path -- первый шаг к удалению legacy-слоя. После этого плана код использует только ECS path.
Output: Application.cs и Game.cs без _useEcs, с standalone ActionScheduler и _gameArea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-legacy-cleanup/06-RESEARCH.md
</context>

<interfaces>
<!-- Ключевые типы из ActionScheduler (остается без изменений) -->
From Assets/Scripts/Model/ActionScheduler.cs:
```csharp
public class ActionScheduler
{
    public void ScheduleAction(Action action, float durationSec);
    public void Update(float deltaTime);
    public void ResetSchedule();
}
```

<!-- Model -- текущие поля, которые нужно заменить -->
From Assets/Scripts/Model/Model.cs:
```csharp
public class Model
{
    public Vector2 GameArea;
    public int Score { get; private set; }
    public void SetScore(int value);
    public ActionScheduler ActionScheduler { get; }
    public event Action<IGameEntityModel> OnEntityDestroyed;
    public void CleanUp();
    public void Update(float deltaTime);
    public void AddEntity(IGameEntityModel entityModel);
    public void ReceiveScore(IGameEntityModel scoreHolder);
    public static void PlaceWithinGameArea(ref float position, float side);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Application.cs -- убрать _useEcs, извлечь ActionScheduler и gameArea</name>
  <files>Assets/Scripts/Application/Application.cs</files>
  <read_first>
    - Assets/Scripts/Application/Application.cs
    - Assets/Scripts/Model/ActionScheduler.cs
    - Assets/Scripts/Model/Model.cs
  </read_first>
  <action>
Рефакторинг Application.cs:

1. **Убрать поле `_useEcs`** (строка 23) и все проверки `if (_useEcs)`.

2. **Добавить standalone поля:**
   - `private ActionScheduler _actionScheduler;`
   - `private Vector2 _gameArea;`

3. **В методе Start():**
   - После расчета sceneWidth/sceneHeight: `_gameArea = new Vector2(sceneWidth, sceneHeight);`
   - `_actionScheduler = new ActionScheduler();`
   - Оставить `_model = new Model { GameArea = _gameArea };` (пока Model используется в других файлах)
   - Блок `if (_useEcs) {` -- убрать условие, оставить содержимое (ECS-инициализация выполняется всегда)
   - `_game.ConnectEcs(_useEcs, ...)` заменить на `_game.ConnectEcs(_collisionBridge, _entityManager)` -- передать EntityManager напрямую
   - Во втором `if (_useEcs)` с ObservableBridge на OnTitleScreenStart -- убрать условие, оставить содержимое

4. **В конструкторе Game:** вместо `new Game(_catalog, _model, _configs, _playerInput, _gameScreen)` передать `new Game(_catalog, _model, _actionScheduler, _gameArea, _configs, _playerInput, _gameScreen)`

5. **В OnUpdate:** убрать `if (_useEcs)` / `else` -- оставить только ECS path:
   ```csharp
   _actionScheduler.Update(deltaTime);
   _game.ProcessShootEvents();
   ```

6. **Сохранить** `_model` как поле (пока нужен для EntitiesCatalog, GameScreen, ObservableBridgeSystem).

7. **В Dispose():** добавить `_actionScheduler = null;`
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "_useEcs" Assets/Scripts/Application/Application.cs | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Application.cs не содержит `_useEcs`
    - Application.cs содержит `private ActionScheduler _actionScheduler`
    - Application.cs содержит `private Vector2 _gameArea`
    - Application.cs содержит `_actionScheduler.Update(deltaTime)`
    - Application.cs НЕ содержит `_model.Update(deltaTime)`
    - Application.cs НЕ содержит `_model.ActionScheduler`
  </acceptance_criteria>
  <done>Application.cs использует standalone ActionScheduler и _gameArea, без _useEcs</done>
</task>

<task type="auto">
  <name>Task 2: Game.cs -- убрать _useEcs, принять ActionScheduler/gameArea как параметры</name>
  <files>Assets/Scripts/Application/Game.cs</files>
  <read_first>
    - Assets/Scripts/Application/Game.cs
    - Assets/Scripts/Application/Application.cs (после Task 1)
    - Assets/Scripts/Model/Model.cs
    - Assets/Scripts/Model/ActionScheduler.cs
  </read_first>
  <action>
Рефакторинг Game.cs:

1. **Добавить поля:**
   - `private readonly ActionScheduler _actionScheduler;`
   - `private readonly Vector2 _gameArea;`

2. **Изменить конструктор:**
   ```csharp
   public Game(EntitiesCatalog catalog, Model model, ActionScheduler actionScheduler, Vector2 gameArea,
       GameData configs, PlayerInput playerInput, GameScreen gameScreen)
   ```
   Сохранить `_actionScheduler = actionScheduler;` и `_gameArea = gameArea;`.

3. **Убрать поле `_useEcs`** (строка 24).

4. **ConnectEcs:** убрать параметр `bool useEcs`, оставить `(CollisionBridge collisionBridge, EntityManager entityManager)`. Убрать `_useEcs = useEcs;`.

5. **Заменить все `_model.ActionScheduler` на `_actionScheduler`** (5 мест: строки 65, 87, 236, 284, 403).

6. **Заменить все `_model.GameArea` на `_gameArea`** (4 места: строки 244, 289, 296, 305, 411).

7. **Убрать `if (_useEcs)` / `else` из методов ввода:**
   - `OnRotateAction`: оставить только ECS-ветку (EntityQuery для ShipTag+RotateData)
   - `OnTrust`: оставить только ECS-ветку (ShipTag+ThrustData)
   - `OnAttack`: оставить только ECS-ветку (ShipTag+EcsGunData+RotateData+MoveData)
   - `OnLaser`: оставить только ECS-ветку (ShipTag+LaserData+RotateData+MoveData)

8. **ProcessShootEvents:** убрать `if (!_useEcs) return;` -- метод всегда выполняется.

9. **SyncEcsScoreToModel:** убрать `if (!_useEcs) return;` -- всегда выполняется.

10. **Restart():** убрать `if (_useEcs)` -- ClearEcsEventBuffers() вызывается всегда.

11. **GameScreenData:** убрать поле `UseEcs` (struct в GameScreen.cs). В Start() при создании GameScreenData убрать `UseEcs = _useEcs`.

**ВАЖНО:** НЕ удалять: `_shipModel` (пока нужен для SpawnNewEnemy/SpawnUfo/SpawnBigUfo позиций), `_model` (пока нужен для ReceiveScore, CleanUp, OnEntityDestroyed), legacy callbacks (OnUserGunShooting, OnUserLaserShooting, OnShipCollided) -- они будут убраны в следующих планах.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "_useEcs" Assets/Scripts/Application/Game.cs | grep "^0$" && grep -c "_model.ActionScheduler" Assets/Scripts/Application/Game.cs | grep "^0$" && grep -c "_model.GameArea" Assets/Scripts/Application/Game.cs | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Game.cs не содержит `_useEcs`
    - Game.cs не содержит `_model.ActionScheduler`
    - Game.cs не содержит `_model.GameArea`
    - Game.cs содержит `private readonly ActionScheduler _actionScheduler`
    - Game.cs содержит `private readonly Vector2 _gameArea`
    - GameScreenData struct не содержит поле `UseEcs`
    - Конструктор Game принимает ActionScheduler и Vector2 gameArea
    - OnRotateAction, OnTrust, OnAttack, OnLaser содержат только ECS-ветки (нет `else`)
  </acceptance_criteria>
  <done>Game.cs без _useEcs, ActionScheduler и gameArea как параметры конструктора, только ECS code paths</done>
</task>

</tasks>

<verification>
- `grep -rn "_useEcs" Assets/Scripts/Application/` возвращает 0 результатов
- `grep -rn "_model.ActionScheduler" Assets/Scripts/Application/` возвращает 0 результатов
- `grep -rn "_model.GameArea" Assets/Scripts/Application/` возвращает 0 результатов
- `grep -rn "_model.Update" Assets/Scripts/Application/` возвращает 0 результатов
- Проект компилируется (проверка через Unity-MCP или grep на отсутствие синтаксических ошибок)
</verification>

<success_criteria>
- Переключатель _useEcs полностью удален из Application.cs и Game.cs
- ActionScheduler работает как standalone поле, не через Model
- GameArea хранится как Vector2 поле, не через Model
- Все input-методы используют только ECS path
- Код компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/06-legacy-cleanup/06-01-SUMMARY.md`
</output>
