---
phase: 01-dev-tooling-shtl-mvvm-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/work/projects/shtl-mvvm/package.json"
  - "~/work/projects/shtl-mvvm/Runtime/DevWidget.cs"
autonomous: false
requirements: [MVVM-01, MVVM-02, MVVM-03, MVVM-04, MVVM-05]
user_setup:
  - service: github
    why: "Push доступ к github.com/SelStrom/shtl-mvvm"
    env_vars: []
    dashboard_config:
      - task: "Убедиться что git push работает для SelStrom/shtl-mvvm"
        location: "GitHub Settings -> SSH keys или HTTPS credentials"

must_haves:
  truths:
    - "package.json shtl-mvvm не содержит com.unity.textmeshpro"
    - "package.json shtl-mvvm содержит com.unity.ugui 1.0.0 как зависимость"
    - "DevWidget.cs использует FindObjectsByType на Unity 2023.1+"
    - "DevWidget.cs использует FindObjectsOfType на Unity < 2023.1"
    - "asmdef ссылка Unity.TextMeshPro НЕ изменена"
    - "Git tag v1.1.0 опубликован в github.com/SelStrom/shtl-mvvm"
  artifacts:
    - path: "package.json"
      provides: "Зависимость на com.unity.ugui вместо com.unity.textmeshpro"
      contains: "com.unity.ugui"
    - path: "Runtime/DevWidget.cs"
      provides: "Условная компиляция FindObjectsByType"
      contains: "UNITY_2023_1_OR_NEWER"
  key_links:
    - from: "package.json"
      to: "UPM dependency resolution"
      via: "com.unity.ugui тянет TMP в Unity 6, проект тянет TMP в Unity 2022.3"
      pattern: "com\\.unity\\.ugui"
---

<objective>
Исправить библиотеку shtl-mvvm для совместимости с Unity 6.3 при сохранении обратной совместимости с Unity 2022.3+.

Purpose: shtl-mvvm зависит от com.unity.textmeshpro, который удален из реестра Unity 6. Замена зависимости на com.unity.ugui и фикс deprecated API разблокирует апгрейд на Unity 6.3.
Output: Опубликованный git tag v1.1.0 в github.com/SelStrom/shtl-mvvm с исправленной библиотекой.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dev-tooling-shtl-mvvm-fix/01-CONTEXT.md
@.planning/phases/01-dev-tooling-shtl-mvvm-fix/01-RESEARCH.md

<interfaces>
<!-- Текущее состояние файлов shtl-mvvm (из Library/PackageCache) -->

From package.json (ТЕКУЩЕЕ -- нужно изменить):
```json
{
  "name": "com.shtl.mvvm",
  "author": "SelStrom",
  "displayName": "shtl-mvvm",
  "description": "Mvvm framework for Unity with clean view model",
  "unity": "2020.3",
  "version": "0.1.0",
  "keywords": ["mvvm"],
  "dependencies": {
    "com.unity.textmeshpro": "3.0.7",
    "com.unity.nuget.newtonsoft-json": "3.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SelStrom/shtl-mvvm.git"
  }
}
```

From Runtime/Shtl.Mvvm.asmdef (НЕ МЕНЯТЬ):
```json
{
  "name": "Shtl.Mvvm",
  "rootNamespace": "Shtl.Mvvm",
  "references": ["Unity.TextMeshPro"],
  "autoReferenced": false
}
```

From Runtime/DevWidget.cs строка 38 (НУЖНО ИСПРАВИТЬ):
```csharp
var widgets = FindObjectsOfType<DevWidget>();
```

From Runtime/Utils/ViewModelToUIEventBindExtensions.cs (НЕ МЕНЯТЬ -- using TMPro работает в обеих версиях):
```csharp
using TMPro;
// ... 4 метода с TMP_Text параметром
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Клонировать shtl-mvvm и применить фикс</name>
  <files>
    ~/work/projects/shtl-mvvm/package.json,
    ~/work/projects/shtl-mvvm/Runtime/DevWidget.cs
  </files>
  <read_first>
    Library/PackageCache/com.shtl.mvvm@c7bda1c328/package.json,
    Library/PackageCache/com.shtl.mvvm@c7bda1c328/Runtime/DevWidget.cs,
    Library/PackageCache/com.shtl.mvvm@c7bda1c328/Runtime/Shtl.Mvvm.asmdef,
    Library/PackageCache/com.shtl.mvvm@c7bda1c328/Runtime/Utils/ViewModelToUIEventBindExtensions.cs
  </read_first>
  <action>
1. Клонировать репозиторий shtl-mvvm:
   ```bash
   git clone https://github.com/SelStrom/shtl-mvvm.git ~/work/projects/shtl-mvvm
   ```

2. Изменить `package.json` -- две правки:
   - Заменить `"com.unity.textmeshpro": "3.0.7"` на `"com.unity.ugui": "1.0.0"` в dependencies (per D-01, MVVM-01)
   - Обновить `"version"` с `"0.1.0"` на `"1.1.0"` (per D-09)
   - Итоговый dependencies блок:
     ```json
     "dependencies": {
       "com.unity.ugui": "1.0.0",
       "com.unity.nuget.newtonsoft-json": "3.2.2"
     }
     ```

3. Изменить `Runtime/DevWidget.cs` строка 38 -- заменить:
   ```csharp
   var widgets = FindObjectsOfType<DevWidget>();
   ```
   на условную компиляцию (per D-01):
   ```csharp
   #if UNITY_2023_1_OR_NEWER
               var widgets = FindObjectsByType<DevWidget>(FindObjectsSortMode.None);
   #else
               var widgets = FindObjectsOfType<DevWidget>();
   #endif
   ```
   Обратить внимание: код находится внутри блока `#if UNITY_EDITOR`, отступ -- 12 пробелов (3 уровня по 4).

4. НЕ МЕНЯТЬ файлы:
   - `Runtime/Shtl.Mvvm.asmdef` -- ссылка `Unity.TextMeshPro` остается (assembly forwarding в Unity 6, per MVVM-02)
   - `Runtime/Utils/ViewModelToUIEventBindExtensions.cs` -- `using TMPro` и типы `TMP_Text` работают в обеих версиях Unity
  </action>
  <verify>
    <automated>cd ~/work/projects/shtl-mvvm && grep -c "com.unity.textmeshpro" package.json | grep "^0$" && grep -c "com.unity.ugui" package.json | grep "^1$" && grep "UNITY_2023_1_OR_NEWER" Runtime/DevWidget.cs && grep "FindObjectsByType" Runtime/DevWidget.cs && grep "FindObjectsOfType" Runtime/DevWidget.cs && grep '"version": "1.1.0"' package.json && grep "Unity.TextMeshPro" Runtime/Shtl.Mvvm.asmdef</automated>
  </verify>
  <acceptance_criteria>
    - package.json содержит `"com.unity.ugui": "1.0.0"` в dependencies
    - package.json НЕ содержит `com.unity.textmeshpro`
    - package.json содержит `"version": "1.1.0"`
    - DevWidget.cs содержит `#if UNITY_2023_1_OR_NEWER`
    - DevWidget.cs содержит `FindObjectsByType<DevWidget>(FindObjectsSortMode.None)`
    - DevWidget.cs содержит `FindObjectsOfType<DevWidget>()` (в ветке #else)
    - Shtl.Mvvm.asmdef содержит `Unity.TextMeshPro` (не изменен)
    - ViewModelToUIEventBindExtensions.cs содержит `using TMPro` (не изменен)
  </acceptance_criteria>
  <done>package.json имеет зависимость ugui вместо textmeshpro, version=1.1.0; DevWidget.cs имеет условную компиляцию FindObjectsByType; asmdef и TMP-binding код не изменены</done>
</task>

<task type="auto">
  <name>Task 2: Закоммитить, создать тег и запушить</name>
  <files>~/work/projects/shtl-mvvm (git operations)</files>
  <read_first>
    ~/work/projects/shtl-mvvm/package.json,
    ~/work/projects/shtl-mvvm/Runtime/DevWidget.cs
  </read_first>
  <action>
1. В директории `~/work/projects/shtl-mvvm`:
   ```bash
   git add package.json Runtime/DevWidget.cs
   git commit -m "fix: replace textmeshpro dependency with ugui for Unity 6 compatibility

   - Replace com.unity.textmeshpro with com.unity.ugui in package.json
   - Add conditional compilation for FindObjectsByType (Unity 2023.1+)
   - Keep Unity.TextMeshPro asmdef reference (assembly forwarding in Unity 6)
   - Bump version to 1.1.0"
   ```

2. Создать тег (per D-09):
   ```bash
   git tag v1.1.0
   ```

3. Запушить коммит и тег:
   ```bash
   git push origin main
   git push origin v1.1.0
   ```

Если push требует аутентификации -- создать checkpoint для пользователя.
  </action>
  <verify>
    <automated>cd ~/work/projects/shtl-mvvm && git log --oneline -1 | grep "replace textmeshpro" && git tag -l "v1.1.0" | grep "v1.1.0" && git ls-remote --tags origin v1.1.0 | grep "v1.1.0"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` содержит "replace textmeshpro"
    - `git tag -l "v1.1.0"` выводит "v1.1.0"
    - `git ls-remote --tags origin v1.1.0` выводит строку с "v1.1.0"
    - `git diff HEAD` пуст (все изменения закоммичены)
  </acceptance_criteria>
  <done>Коммит с фиксом создан, тег v1.1.0 создан и запушен в origin, тег виден через git ls-remote</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Проверить публикацию тега v1.1.0</name>
  <files>~/work/projects/shtl-mvvm (remote verification)</files>
  <action>
Пользователь проверяет публикацию тега v1.1.0 на GitHub:
1. Открыть https://github.com/SelStrom/shtl-mvvm/tags -- убедиться что тег v1.1.0 присутствует
2. Проверить package.json в теге: зависимость com.unity.ugui вместо com.unity.textmeshpro, версия 1.1.0
3. Проверить DevWidget.cs: наличие #if UNITY_2023_1_OR_NEWER с FindObjectsByType
  </action>
  <verify>
    <automated>cd ~/work/projects/shtl-mvvm && git ls-remote --tags origin v1.1.0 | grep -q v1.1.0 && echo "PASS"</automated>
  </verify>
  <done>Тег v1.1.0 подтвержден на GitHub, содержимое фикса корректно</done>
</task>

</tasks>

<verification>
- `git ls-remote --tags https://github.com/SelStrom/shtl-mvvm.git v1.1.0` возвращает хэш тега
- В package.json тега v1.1.0: `com.unity.ugui` в dependencies, нет `com.unity.textmeshpro`
- В DevWidget.cs тега v1.1.0: `#if UNITY_2023_1_OR_NEWER` с `FindObjectsByType`
- Shtl.Mvvm.asmdef в теге v1.1.0: ссылка `Unity.TextMeshPro` сохранена
</verification>

<success_criteria>
- Тег v1.1.0 доступен по URL `https://github.com/SelStrom/shtl-mvvm.git#v1.1.0`
- Фикс содержит ровно 2 изменённых файла: package.json и DevWidget.cs
- Обратная совместимость с Unity 2022.3+ сохранена (FindObjectsOfType в #else ветке)
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-tooling-shtl-mvvm-fix/01-01-SUMMARY.md`
</output>
