---
phase: 14-config-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Configs/GameData.cs
  - Assets/Scripts/ECS/EntityFactory.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/View/RocketVisual.cs
  - Assets/Scripts/Application/Application.cs
autonomous: true
requirements: [CONF-01, VIS-02, VIS-04]

must_haves:
  truths:
    - "RocketData struct содержит поля Speed, LifeTimeSec, TurnRateDegPerSec, MaxAmmo, ReloadDurationSec, Score"
    - "EntitiesCatalog.CreateRocket читает параметры из _configs.Rocket, а не из hardcoded констант"
    - "EntitiesCatalog.CreateShip читает rocketMaxAmmo и rocketReloadSec из _configs.Rocket"
    - "EntityFactory.CreateRocket добавляет ScoreValue компонент на entity"
    - "RocketVisual управляет ParticleSystem trail: Play при активации, Stop+Clear при деактивации"
    - "При уничтожении ракеты воспроизводится взрыв VFX через существующий VfxBlowPrefab"
  artifacts:
    - path: "Assets/Scripts/Configs/GameData.cs"
      provides: "RocketData struct с полными полями конфигурации"
      contains: "public float Speed"
    - path: "Assets/Scripts/ECS/EntityFactory.cs"
      provides: "CreateRocket с параметром score и ScoreValue компонентом"
      contains: "new ScoreValue"
    - path: "Assets/Scripts/View/RocketVisual.cs"
      provides: "Trail ParticleSystem lifecycle"
      contains: "ParticleSystemStopBehavior.StopEmittingAndClear"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "Ветка EntityType.Rocket в OnDeadEntity"
      contains: "EntityType.Rocket"
  key_links:
    - from: "Assets/Scripts/Application/EntitiesCatalog.cs"
      to: "Assets/Scripts/Configs/GameData.cs"
      via: "_configs.Rocket.Speed, _configs.Rocket.LifeTimeSec, etc."
      pattern: "_configs\\.Rocket\\."
    - from: "Assets/Scripts/Application/Application.cs"
      to: "Assets/Scripts/Application/Game.cs"
      via: "PlayEffect при EntityType.Rocket"
      pattern: "EntityType\\.Rocket"
---

<objective>
Вынести все hardcoded параметры ракеты в ScriptableObject конфигурацию, добавить ScoreValue на rocket entity, добавить поддержку trail ParticleSystem в RocketVisual и взрыв VFX при уничтожении ракеты.

Purpose: Устранить магические числа из кода ракеты, обеспечить настраиваемость через инспектор Unity. Добавить код визуальных эффектов (trail + explosion).
Output: Все C#-файлы обновлены, код компилируется, параметры читаются из конфига.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-config-visual-polish/14-CONTEXT.md
@.planning/phases/14-config-visual-polish/14-RESEARCH.md

@Assets/Scripts/Configs/GameData.cs
@Assets/Scripts/ECS/EntityFactory.cs
@Assets/Scripts/Application/EntitiesCatalog.cs
@Assets/Scripts/View/RocketVisual.cs
@Assets/Scripts/Application/Application.cs
@Assets/Scripts/ECS/Components/ScoreData.cs
@Assets/Scripts/Application/Game.cs

<interfaces>
<!-- Существующие контракты, используемые в этом плане -->

From Assets/Scripts/ECS/Components/ScoreData.cs:
```csharp
public struct ScoreValue : IComponentData
{
    public int Score;
}
```

From Assets/Scripts/Configs/GameData.cs (текущее состояние RocketData):
```csharp
[Serializable]
public struct RocketData
{
    public GameObject Prefab;
}
```

From Assets/Scripts/ECS/EntityFactory.cs (текущая сигнатура):
```csharp
public static Entity CreateRocket(
    EntityManager em,
    float2 position,
    float speed,
    float2 direction,
    float lifeTime,
    float turnRateDegPerSec)
```

From Assets/Scripts/View/RocketVisual.cs (текущее состояние):
```csharp
public class RocketVisual : AbstractWidgetView<RocketViewModel>, IEntityView
{
    [SerializeField] private Collider2D _collider = default;
    protected override void OnConnected() { _collider.enabled = true; }
    private void OnCollisionEnter2D(Collision2D col) { ... }
}
```

From Assets/Scripts/Application/Application.cs (OnDeadEntity, строки 207-238):
```csharp
private void OnDeadEntity(DeadEntityInfo info)
{
    // ... существующие ветки: Asteroid, Ship, UfoBig/Ufo
    // Нужно добавить: EntityType.Rocket
    _catalog.ReleaseByGameObject(go);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить RocketData, обновить EntityFactory и EntitiesCatalog для чтения из конфига</name>
  <files>
    Assets/Scripts/Configs/GameData.cs,
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs
  </files>
  <read_first>
    Assets/Scripts/Configs/GameData.cs,
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Scripts/Application/EntitiesCatalog.cs,
    Assets/Scripts/ECS/Components/ScoreData.cs
  </read_first>
  <action>
1. В `Assets/Scripts/Configs/GameData.cs` -- расширить struct `RocketData` (строки 60-64). Заменить текущее содержимое на (per D-01):
```csharp
[Serializable]
public struct RocketData
{
    public GameObject Prefab;
    [Space]
    public float Speed;
    public float LifeTimeSec;
    public float TurnRateDegPerSec;
    [Space]
    public int MaxAmmo;
    public float ReloadDurationSec;
    [Space]
    public int Score;
}
```

2. В `Assets/Scripts/ECS/EntityFactory.cs` -- обновить метод `CreateRocket` (строки 154-180). Добавить параметр `int score` и компонент `ScoreValue` (per D-04, D-05):
```csharp
public static Entity CreateRocket(
    EntityManager em,
    float2 position,
    float speed,
    float2 direction,
    float lifeTime,
    float turnRateDegPerSec,
    int score)
{
    var entity = em.CreateEntity();
    em.AddComponentData(entity, new RocketTag());
    em.AddComponentData(entity, new MoveData
    {
        Position = position,
        Speed = speed,
        Direction = direction
    });
    em.AddComponentData(entity, new LifeTimeData
    {
        TimeRemaining = lifeTime
    });
    em.AddComponentData(entity, new RocketTargetData
    {
        Target = Entity.Null,
        TurnRateDegPerSec = turnRateDegPerSec
    });
    em.AddComponentData(entity, new ScoreValue
    {
        Score = score
    });
    return entity;
}
```

3. В `Assets/Scripts/Application/EntitiesCatalog.cs` строка 282-288 -- заменить hardcoded значения в `CreateRocket()` на чтение из `_configs.Rocket` (per D-02, D-05):
```csharp
var entity = EntityFactory.CreateRocket(
    _entityManager,
    new float2(position.x, position.y),
    _configs.Rocket.Speed,
    new float2(direction.x, direction.y),
    _configs.Rocket.LifeTimeSec,
    _configs.Rocket.TurnRateDegPerSec,
    _configs.Rocket.Score
);
```
Удалить TODO-комментарий на строке 281.

4. В `Assets/Scripts/Application/EntitiesCatalog.cs` строки 110-111 -- заменить hardcoded `rocketMaxAmmo: 3` и `rocketReloadSec: 5f` на (per D-03):
```csharp
rocketMaxAmmo: _configs.Rocket.MaxAmmo,
rocketReloadSec: _configs.Rocket.ReloadDurationSec
```
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -n "public float Speed" Assets/Scripts/Configs/GameData.cs && grep -n "new ScoreValue" Assets/Scripts/ECS/EntityFactory.cs && grep -n "_configs.Rocket.Speed" Assets/Scripts/Application/EntitiesCatalog.cs && grep -n "_configs.Rocket.MaxAmmo" Assets/Scripts/Application/EntitiesCatalog.cs</automated>
  </verify>
  <acceptance_criteria>
    - GameData.cs RocketData struct содержит: `public float Speed`, `public float LifeTimeSec`, `public float TurnRateDegPerSec`, `public int MaxAmmo`, `public float ReloadDurationSec`, `public int Score`
    - EntityFactory.cs CreateRocket содержит параметр `int score` и строку `new ScoreValue`
    - EntitiesCatalog.cs CreateRocket вызов содержит `_configs.Rocket.Speed`, `_configs.Rocket.LifeTimeSec`, `_configs.Rocket.TurnRateDegPerSec`, `_configs.Rocket.Score`
    - EntitiesCatalog.cs CreateShip вызов содержит `_configs.Rocket.MaxAmmo` и `_configs.Rocket.ReloadDurationSec` вместо hardcoded 3 и 5f
    - Нет hardcoded `speed: 8f`, `lifeTime: 5f`, `turnRateDegPerSec: 180f`, `rocketMaxAmmo: 3`, `rocketReloadSec: 5f` в EntitiesCatalog.cs
  </acceptance_criteria>
  <done>RocketData struct расширен всеми полями конфигурации, EntityFactory добавляет ScoreValue, EntitiesCatalog читает все параметры из _configs.Rocket</done>
</task>

<task type="auto">
  <name>Task 2: Trail ParticleSystem в RocketVisual и взрыв VFX в OnDeadEntity</name>
  <files>
    Assets/Scripts/View/RocketVisual.cs,
    Assets/Scripts/Application/Application.cs
  </files>
  <read_first>
    Assets/Scripts/View/RocketVisual.cs,
    Assets/Scripts/Application/Application.cs,
    Assets/Scripts/View/EffectVisual.cs,
    Assets/Scripts/Application/Game.cs
  </read_first>
  <action>
1. В `Assets/Scripts/View/RocketVisual.cs` -- добавить поддержку trail ParticleSystem (per D-06, D-08, D-09). Заменить весь класс RocketVisual на:
```csharp
public class RocketVisual : AbstractWidgetView<RocketViewModel>, IEntityView
{
    [SerializeField] private Collider2D _collider = default;
    [SerializeField] private ParticleSystem _trailEffect = default;

    protected override void OnConnected()
    {
        _collider.enabled = true;
        if (_trailEffect != null)
        {
            _trailEffect.Clear();
            _trailEffect.Play();
        }
    }

    private void OnDisable()
    {
        if (_trailEffect != null)
        {
            _trailEffect.Stop(true, ParticleSystemStopBehavior.StopEmittingAndClear);
        }
    }

    private void OnCollisionEnter2D(Collision2D col)
    {
        ViewModel.OnCollision.Value?.Invoke(col);
    }
}
```
Класс RocketViewModel остаётся без изменений.

2. В `Assets/Scripts/Application/Application.cs` метод `OnDeadEntity` (строка 207) -- добавить ветку `EntityType.Rocket` перед финальным `_catalog.ReleaseByGameObject(go)` (per D-10, D-11). После блока `else if (entityType == EntityType.UfoBig || entityType == EntityType.Ufo)` (строка 231-234) добавить:
```csharp
else if (entityType == EntityType.Rocket)
{
    _game.PlayEffect(_configs.VfxBlowPrefab, position);
}
```
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -n "ParticleSystem _trailEffect" Assets/Scripts/View/RocketVisual.cs && grep -n "StopEmittingAndClear" Assets/Scripts/View/RocketVisual.cs && grep -n "EntityType.Rocket" Assets/Scripts/Application/Application.cs</automated>
  </verify>
  <acceptance_criteria>
    - RocketVisual.cs содержит `[SerializeField] private ParticleSystem _trailEffect`
    - RocketVisual.cs OnConnected содержит `_trailEffect.Clear()` и `_trailEffect.Play()`
    - RocketVisual.cs OnDisable содержит `_trailEffect.Stop(true, ParticleSystemStopBehavior.StopEmittingAndClear)`
    - Application.cs OnDeadEntity содержит `entityType == EntityType.Rocket`
    - Application.cs Rocket ветка вызывает `_game.PlayEffect(_configs.VfxBlowPrefab, position)`
  </acceptance_criteria>
  <done>RocketVisual управляет trail ParticleSystem lifecycle (play/stop/clear), Application.OnDeadEntity воспроизводит взрыв VFX при уничтожении ракеты</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config -> ECS | Значения из ScriptableObject передаются в ECS компоненты без валидации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | GameData.asset | accept | Однопользовательская игра, конфиг не передаётся по сети. Некорректные значения (Speed=0) приведут только к визуальным багам, легко поправить в инспекторе |
| T-14-02 | Denial of Service | ParticleSystem emission | accept | Emission rate контролируется через инспектор, не через пользовательский ввод. Высокий rate замедлит только редактор |
</threat_model>

<verification>
1. `grep -rn "speed: 8f\|lifeTime: 5f\|turnRateDegPerSec: 180f\|rocketMaxAmmo: 3\|rocketReloadSec: 5f" Assets/Scripts/` -- не должно возвращать результатов (все hardcoded убраны)
2. `grep -n "ScoreValue" Assets/Scripts/ECS/EntityFactory.cs` -- содержит строку с ScoreValue в CreateRocket
3. `grep -n "ParticleSystem" Assets/Scripts/View/RocketVisual.cs` -- содержит SerializeField и lifecycle методы
4. `grep -n "EntityType.Rocket" Assets/Scripts/Application/Application.cs` -- содержит ветку в OnDeadEntity
5. Существующие тесты: `unity-mcp-cli run-tool tests-run --input '{"testMode": "EditMode", "testFilter": "Rocket"}'` -- все тесты зелёные
</verification>

<success_criteria>
- Все параметры ракеты читаются из GameData.RocketData, нет hardcoded констант в EntitiesCatalog
- EntityFactory.CreateRocket добавляет ScoreValue компонент
- RocketVisual поддерживает trail ParticleSystem с корректным lifecycle для пулинга
- Application.OnDeadEntity воспроизводит VFX при уничтожении ракеты
- Все существующие тесты Rocket проходят
</success_criteria>

<output>
After completion, create `.planning/phases/14-config-visual-polish/14-01-SUMMARY.md`
</output>
