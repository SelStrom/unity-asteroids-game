---
phase: 15-hud
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - Assets/Scenes/Main.unity
autonomous: false
requirements:
  - HUD-01
  - HUD-02
  - TEST-03

must_haves:
  truths:
    - "В HUD Canvas существуют два TMP_Text объекта для ракетного боезапаса и таймера"
    - "TMP_Text объекты привязаны к полям _rocketAmmoCount и _rocketReloadTime в HudVisual"
    - "При запуске PlayMode ракетный HUD отображается корректно"
    - "Счётчик ракет обновляется при запуске и перезарядке"
    - "Таймер перезарядки появляется/скрывается в зависимости от боезапаса"
  artifacts:
    - path: "Assets/Scenes/Main.unity"
      provides: "TMP_Text объекты Rocket Ammo Count и Rocket Reload Time в HUD Canvas, привязанные к HudVisual"
      contains: "Rocket"
  key_links:
    - from: "HudVisual._rocketAmmoCount"
      to: "TMP_Text Rocket Ammo Count (scene)"
      via: "SerializeField reference in Main.unity"
      pattern: "_rocketAmmoCount"
    - from: "HudVisual._rocketReloadTime"
      to: "TMP_Text Rocket Reload Time (scene)"
      via: "SerializeField reference in Main.unity"
      pattern: "_rocketReloadTime"
---

<objective>
Создать UI элементы в сцене через MCP и верифицировать полную цепочку HUD ракет в PlayMode.

Purpose: Завершить визуальную часть HUD -- привязать код из Plan 01 к реальным UI элементам сцены
Output: Рабочий HUD с ракетным боезапасом и таймером, подтверждённый MCP-скриншотом
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hud/15-CONTEXT.md
@.planning/phases/15-hud/15-RESEARCH.md
@.planning/phases/15-hud/15-01-SUMMARY.md

@Assets/Scripts/View/HudVisual.cs
@Assets/Scenes/Main.unity

<interfaces>
<!-- Контракты из Plan 01 -- к этому моменту HudVisual уже содержит поля -->

From Assets/Scripts/View/HudVisual.cs (после Plan 01):
```csharp
public class HudVisual : AbstractWidgetView<HudData>
{
    [SerializeField] private TMP_Text _coordinates = default;
    [SerializeField] private TMP_Text _rotationAngle = default;
    [SerializeField] private TMP_Text _speed = default;
    [SerializeField] private TMP_Text _laserShootCount = default;
    [SerializeField] private TMP_Text _laserReloadTime = default;
    [SerializeField] private TMP_Text _rocketAmmoCount = default;
    [SerializeField] private TMP_Text _rocketReloadTime = default;
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP -- создать TMP_Text объекты в HUD Canvas и привязать к HudVisual</name>
  <files>Assets/Scenes/Main.unity</files>
  <read_first>
    - Assets/Scripts/View/HudVisual.cs
    - Assets/Scenes/Main.unity (через MCP -- найти Hud GameObject и структуру LaserReloadTime)
  </read_first>
  <action>
Через Unity MCP выполнить следующие шаги (решения D-11, D-12):

1. **Найти родительский объект HUD** -- через MCP найти GameObject "Hud" в иерархии сцены. Определить его fileID и структуру дочерних объектов. Найти объект LaserReloadTime как ориентир для размещения.

2. **Создать TMP_Text "RocketAmmoCount"**:
   - Создать дочерний GameObject "RocketAmmoCount" под тем же родителем, что и LaserReloadTime (или под Hud)
   - Добавить компонент TextMeshProUGUI
   - Настроить шрифт, размер и цвет единообразно с существующими HUD-элементами (скопировать с LaserShootCount)
   - Расположить ниже LaserReloadTime (сдвинуть Y вниз на ~30 единиц от LaserReloadTime)
   - Установить текст по умолчанию: "Rockets: 0"
   - Установить alignment: Left, Top

3. **Создать TMP_Text "RocketReloadTime"**:
   - Создать дочерний GameObject "RocketReloadTime" под тем же родителем
   - Добавить компонент TextMeshProUGUI
   - Настроить шрифт, размер и цвет единообразно с LaserReloadTime
   - Расположить ниже RocketAmmoCount (сдвинуть Y вниз ещё на ~30 единиц)
   - Установить текст по умолчанию: "Reload rocket: 0 sec"
   - Установить alignment: Left, Top

4. **Привязать SerializeField ссылки в HudVisual**:
   - Найти компонент HudVisual на объекте Hud
   - Установить поле `_rocketAmmoCount` -> ссылка на созданный объект RocketAmmoCount
   - Установить поле `_rocketReloadTime` -> ссылка на созданный объект RocketReloadTime

5. **Сохранить сцену** через MCP.
  </action>
  <verify>
    <automated>grep -c "RocketAmmoCount\|RocketReloadTime" Assets/Scenes/Main.unity</automated>
  </verify>
  <acceptance_criteria>
    - Main.unity содержит GameObject с именем "RocketAmmoCount"
    - Main.unity содержит GameObject с именем "RocketReloadTime"
    - Main.unity содержит ссылки _rocketAmmoCount и _rocketReloadTime в компоненте HudVisual (не null)
  </acceptance_criteria>
  <done>Два TMP_Text объекта созданы в HUD Canvas, привязаны к HudVisual._rocketAmmoCount и _rocketReloadTime</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MCP-верификация -- PlayMode скриншот и динамическая проверка HUD</name>
  <files>Assets/Scenes/Main.unity</files>
  <action>
Перед checkpoint: запустить PlayMode через MCP, сделать скриншот Game View через MCP. Это автоматизированная часть (решения D-13, D-14).

Что построено: полная цепочка HUD ракет: ECS RocketAmmoData -> ObservableBridgeSystem -> HudData -> HudVisual -> TMP_Text. Два новых UI элемента в HUD: счётчик боезапаса ракет и таймер перезарядки.

Как верифицировать:

1. Запустить PlayMode в Unity Editor
2. Сделать скриншот Game View через MCP
3. Визуально подтвердить:
   - В левой части экрана видны строки "Rockets: N" и (если боезапас неполный) "Reload rocket: N sec"
   - Формат единообразен с "Laser shoots: N" и "Reload laser: N sec"
   - Шрифт и размер совпадают с другими HUD элементами
4. Выстрелить ракетой (нажать R):
   - Счётчик "Rockets:" уменьшается на 1
   - Появляется таймер "Reload rocket: N sec"
5. Дождаться перезарядки:
   - Таймер обратного отсчёта обновляется
   - Когда боезапас полный, таймер исчезает
  </action>
  <verify>
    <automated>echo "MCP скриншот Game View -- ручная верификация пользователем"</automated>
  </verify>
  <done>Пользователь подтвердил: HUD ракет отображается корректно, счётчик и таймер обновляются динамически</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP -> Unity Scene | MCP изменяет сцену, привязки SerializeField |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | T (Tampering) | Main.unity | accept | Сцена под version control, MCP-изменения ревьюятся через diff |
</threat_model>

<verification>
1. MCP скриншот Game View показывает rocket HUD элементы
2. Динамическая проверка: R -> счётчик уменьшается, таймер появляется, при полном боезапасе таймер скрывается
</verification>

<success_criteria>
- TMP_Text объекты RocketAmmoCount и RocketReloadTime существуют в сцене
- SerializeField привязки HudVisual._rocketAmmoCount и _rocketReloadTime не null
- PlayMode: ракетный HUD отображается, обновляется при запуске/перезарядке
- MCP-скриншот подтверждает визуал
</success_criteria>

<output>
After completion, create `.planning/phases/15-hud/15-02-SUMMARY.md`
</output>
