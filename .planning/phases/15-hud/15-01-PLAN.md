---
phase: 15-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/View/HudVisual.cs
  - Assets/Scripts/Bridge/ObservableBridgeSystem.cs
  - Assets/Scripts/Application/Screens/GameScreen.cs
  - Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
autonomous: true
requirements:
  - HUD-01
  - HUD-02

must_haves:
  truths:
    - "HudData содержит ReactiveValue поля для ракетного боезапаса и таймера перезарядки"
    - "ObservableBridgeSystem читает RocketAmmoData и пишет в HudData каждый кадр"
    - "HudVisual привязывает rocket поля к TMP_Text элементам"
    - "GameScreen передаёт MaxAmmo в bridge при активации HUD"
    - "Существующие тесты ObservableBridgeSystem проходят после расширения Query"
    - "Новые тесты подтверждают корректность rocket данных в HUD"
  artifacts:
    - path: "Assets/Scripts/View/HudVisual.cs"
      provides: "HudData с RocketAmmoCount, RocketReloadTime, IsRocketReloadTimeVisible; HudVisual с SerializeField и Bind"
      contains: "RocketAmmoCount"
    - path: "Assets/Scripts/Bridge/ObservableBridgeSystem.cs"
      provides: "Расширенный Query с RefRO<RocketAmmoData>, запись в HudData"
      contains: "RocketAmmoData"
    - path: "Assets/Scripts/Application/Screens/GameScreen.cs"
      provides: "Вызов SetRocketMaxAmmo в ActivateHud"
      contains: "SetRocketMaxAmmo"
    - path: "Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs"
      provides: "Тесты PushesRocketData_ToHudData и PushesRocketReloadVisibility"
      contains: "PushesRocketData_ToHudData"
  key_links:
    - from: "ObservableBridgeSystem.OnUpdate()"
      to: "HudData.RocketAmmoCount"
      via: "SystemAPI.Query<RefRO<RocketAmmoData>>"
      pattern: "rocketAmmo\\.ValueRO\\.CurrentAmmo"
    - from: "GameScreen.ActivateHud()"
      to: "ObservableBridgeSystem.SetRocketMaxAmmo()"
      via: "bridge.SetRocketMaxAmmo"
      pattern: "SetRocketMaxAmmo.*Rocket\\.MaxAmmo"
---

<objective>
Расширить C#-код HUD для отображения ракетного боезапаса и таймера перезарядки: HudData ViewModel, HudVisual View, ObservableBridgeSystem bridge, GameScreen интеграция, юнит-тесты.

Purpose: Полная MVVM цепочка ECS -> HUD для ракет, по паттерну лазера
Output: Компилируемый код с зелёными тестами, готовый для привязки к UI элементам сцены (Plan 02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hud/15-CONTEXT.md
@.planning/phases/15-hud/15-RESEARCH.md

@Assets/Scripts/View/HudVisual.cs
@Assets/Scripts/Bridge/ObservableBridgeSystem.cs
@Assets/Scripts/Application/Screens/GameScreen.cs
@Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
@Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
@Assets/Scripts/ECS/Components/RocketAmmoData.cs
@Assets/Scripts/Configs/GameData.cs

<interfaces>
<!-- Существующие контракты, используемые в этом плане -->

From Assets/Scripts/ECS/Components/RocketAmmoData.cs:
```csharp
public struct RocketAmmoData : IComponentData
{
    public int MaxAmmo;
    public float ReloadDurationSec;
    public int CurrentAmmo;
    public float ReloadRemaining;
    public bool Shooting;
    public float2 Direction;
    public float2 ShootPosition;
}
```

From Assets/Scripts/View/HudVisual.cs (текущее состояние):
```csharp
public sealed class HudData : AbstractViewModel
{
    public readonly ReactiveValue<string> Coordinates = new();
    public readonly ReactiveValue<string> RotationAngle = new();
    public readonly ReactiveValue<string> Speed = new();
    public readonly ReactiveValue<string> LaserShootCount = new();
    public readonly ReactiveValue<string> LaserReloadTime = new();
    public readonly ReactiveValue<bool> IsLaserReloadTimeVisible = new();
}
```

From Assets/Scripts/Bridge/ObservableBridgeSystem.cs (паттерн Query строка 51-53):
```csharp
foreach (var (move, rotate, thrust, laser) in
         SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, RefRO<ThrustData>, RefRO<LaserData>>()
             .WithAll<ShipTag>())
```

From Assets/Scripts/Configs/GameData.cs (RocketData struct):
```csharp
public struct RocketData
{
    public GameObject Prefab;
    public float Speed;
    public float LifeTimeSec;
    public float TurnRateDegPerSec;
    public int MaxAmmo;
    public float ReloadDurationSec;
    public int Score;
}
```

From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs (CreateShipEntity уже содержит RocketAmmoData):
```csharp
protected Entity CreateShipEntity(
    float2 position = default, float speed = 0f,
    int rocketMaxAmmo = 3, float rocketReloadSec = 5f)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HudData ViewModel + HudVisual View -- добавить rocket поля и биндинги</name>
  <files>Assets/Scripts/View/HudVisual.cs</files>
  <read_first>
    - Assets/Scripts/View/HudVisual.cs
  </read_first>
  <behavior>
    - HudData содержит поле RocketAmmoCount типа ReactiveValue&lt;string&gt;
    - HudData содержит поле RocketReloadTime типа ReactiveValue&lt;string&gt;
    - HudData содержит поле IsRocketReloadTimeVisible типа ReactiveValue&lt;bool&gt;
    - HudVisual содержит [SerializeField] private TMP_Text _rocketAmmoCount
    - HudVisual содержит [SerializeField] private TMP_Text _rocketReloadTime
    - HudVisual.OnConnected() привязывает ViewModel.RocketAmmoCount к _rocketAmmoCount
    - HudVisual.OnConnected() привязывает ViewModel.RocketReloadTime к _rocketReloadTime
    - HudVisual.OnConnected() привязывает ViewModel.IsRocketReloadTimeVisible к _rocketReloadTime.gameObject
  </behavior>
  <action>
Открыть Assets/Scripts/View/HudVisual.cs. Реализация по решениям D-01, D-05, D-06:

В класс HudData добавить три поля после IsLaserReloadTimeVisible (строка 14):
```csharp
public readonly ReactiveValue<string> RocketAmmoCount = new();
public readonly ReactiveValue<string> RocketReloadTime = new();
public readonly ReactiveValue<bool> IsRocketReloadTimeVisible = new();
```

В класс HudVisual добавить два SerializeField после _laserReloadTime (строка 23):
```csharp
[SerializeField] private TMP_Text _rocketAmmoCount = default;
[SerializeField] private TMP_Text _rocketReloadTime = default;
```

В метод OnConnected() добавить три Bind после строки 32:
```csharp
Bind.From(ViewModel.RocketAmmoCount).To(_rocketAmmoCount);
Bind.From(ViewModel.RocketReloadTime).To(_rocketReloadTime);
Bind.From(ViewModel.IsRocketReloadTimeVisible).To(_rocketReloadTime.gameObject);
```
  </action>
  <verify>
    <automated>grep -n "RocketAmmoCount\|RocketReloadTime\|IsRocketReloadTimeVisible\|_rocketAmmoCount\|_rocketReloadTime" Assets/Scripts/View/HudVisual.cs</automated>
  </verify>
  <acceptance_criteria>
    - HudVisual.cs содержит `public readonly ReactiveValue<string> RocketAmmoCount = new();`
    - HudVisual.cs содержит `public readonly ReactiveValue<string> RocketReloadTime = new();`
    - HudVisual.cs содержит `public readonly ReactiveValue<bool> IsRocketReloadTimeVisible = new();`
    - HudVisual.cs содержит `[SerializeField] private TMP_Text _rocketAmmoCount = default;`
    - HudVisual.cs содержит `[SerializeField] private TMP_Text _rocketReloadTime = default;`
    - HudVisual.cs содержит `Bind.From(ViewModel.RocketAmmoCount).To(_rocketAmmoCount);`
    - HudVisual.cs содержит `Bind.From(ViewModel.RocketReloadTime).To(_rocketReloadTime);`
    - HudVisual.cs содержит `Bind.From(ViewModel.IsRocketReloadTimeVisible).To(_rocketReloadTime.gameObject);`
  </acceptance_criteria>
  <done>HudData содержит 3 ReactiveValue поля для ракет, HudVisual содержит 2 SerializeField и 3 Bind в OnConnected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ObservableBridgeSystem + GameScreen + тесты -- расширить bridge и интеграцию</name>
  <files>
    Assets/Scripts/Bridge/ObservableBridgeSystem.cs,
    Assets/Scripts/Application/Screens/GameScreen.cs,
    Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
  </files>
  <read_first>
    - Assets/Scripts/Bridge/ObservableBridgeSystem.cs
    - Assets/Scripts/Application/Screens/GameScreen.cs
    - Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
    - Assets/Scripts/ECS/Components/RocketAmmoData.cs
  </read_first>
  <behavior>
    - Test PushesRocketData_ToHudData: при CurrentAmmo=2, MaxAmmo=3 -- hudData.RocketAmmoCount.Value == "Rockets: 2", hudData.IsRocketReloadTimeVisible.Value == true
    - Test PushesRocketReloadVisibility_HiddenWhenFull: при CurrentAmmo=3, MaxAmmo=3 -- hudData.IsRocketReloadTimeVisible.Value == false
    - Test PushesRocketReloadTime_ToHudData: при ReloadRemaining=4.0f -- hudData.RocketReloadTime.Value содержит "Reload rocket:" и "sec"
    - Все существующие тесты ObservableBridgeSystem проходят без изменений (Query расширен, но CreateShipEntity уже создаёт RocketAmmoData)
  </behavior>
  <action>
**ObservableBridgeSystem.cs** (решения D-07, D-08, D-09):

1. Добавить `using SelStrom.Asteroids.ECS;` если отсутствует (RocketAmmoData в namespace SelStrom.Asteroids.ECS). Проверить -- уже есть на строке 3.

2. Добавить приватное поле после `_laserMaxShoots` (строка 15):
```csharp
private int _rocketMaxAmmo;
```

3. Добавить публичный setter после `SetLaserMaxShoots` (после строки 34):
```csharp
public void SetRocketMaxAmmo(int maxAmmo)
{
    _rocketMaxAmmo = maxAmmo;
}
```

4. Расширить Query в OnUpdate() -- строки 51-53 заменить на:
```csharp
foreach (var (move, rotate, thrust, laser, rocketAmmo) in
         SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, RefRO<ThrustData>, RefRO<LaserData>, RefRO<RocketAmmoData>>()
             .WithAll<ShipTag>())
```

5. В блоке `if (_hudData != null)` после строки 73 (после IsLaserReloadTimeVisible) добавить:
```csharp
var rocketCurrentAmmo = rocketAmmo.ValueRO.CurrentAmmo;
_hudData.RocketAmmoCount.Value = $"Rockets: {rocketCurrentAmmo.ToString()}";
_hudData.RocketReloadTime.Value =
    $"Reload rocket: {TimeSpan.FromSeconds((int)rocketAmmo.ValueRO.ReloadRemaining):%s} sec";
_hudData.IsRocketReloadTimeVisible.Value = rocketCurrentAmmo < _rocketMaxAmmo;
```

**GameScreen.cs** (решение D-10):

В метод ActivateHud() после строки 66 (`bridge.SetLaserMaxShoots(...)`) добавить:
```csharp
bridge.SetRocketMaxAmmo(_configs.Rocket.MaxAmmo);
```

**ObservableBridgeSystemTests.cs** -- добавить 3 теста в конец класса:

ВАЖНО: `CreateFullShipEntity` вызывает `CreateShipEntity`, который УЖЕ добавляет `RocketAmmoData` с дефолтными значениями (maxAmmo=3, reloadSec=5f, currentAmmo=3, reloadRemaining=5f). Поэтому для тестов rocket нужно после `CreateFullShipEntity` перезаписать `RocketAmmoData` через `m_Manager.SetComponentData`.

Тест 1 -- PushesRocketData_ToHudData:
```csharp
[Test]
public void PushesRocketData_ToHudData()
{
    var hudData = new HudData();
    _system.SetHudData(hudData);
    _system.SetRocketMaxAmmo(3);

    var entity = CreateFullShipEntity(
        position: float2.zero,
        speed: 0f,
        direction: float2.zero,
        rotation: new float2(1f, 0f),
        thrustActive: false,
        laserCurrentShoots: 3,
        laserMaxShoots: 3,
        reloadRemaining: 0f);
    m_Manager.SetComponentData(entity, new RocketAmmoData
    {
        CurrentAmmo = 2,
        MaxAmmo = 3,
        ReloadRemaining = 4.0f,
        ReloadDurationSec = 5.0f
    });

    _system.Update();

    Assert.AreEqual("Rockets: 2", hudData.RocketAmmoCount.Value);
    Assert.IsTrue(hudData.IsRocketReloadTimeVisible.Value,
        "IsRocketReloadTimeVisible should be true when ammo < maxAmmo");
}
```

Тест 2 -- PushesRocketReloadVisibility_HiddenWhenFull:
```csharp
[Test]
public void PushesRocketReloadVisibility_HiddenWhenFull()
{
    var hudData = new HudData();
    _system.SetHudData(hudData);
    _system.SetRocketMaxAmmo(3);

    var entity = CreateFullShipEntity(
        position: float2.zero,
        speed: 0f,
        direction: float2.zero,
        rotation: new float2(1f, 0f),
        thrustActive: false,
        laserCurrentShoots: 3,
        laserMaxShoots: 3,
        reloadRemaining: 0f);
    m_Manager.SetComponentData(entity, new RocketAmmoData
    {
        CurrentAmmo = 3,
        MaxAmmo = 3,
        ReloadRemaining = 0f,
        ReloadDurationSec = 5.0f
    });

    _system.Update();

    Assert.IsFalse(hudData.IsRocketReloadTimeVisible.Value,
        "IsRocketReloadTimeVisible should be false when ammo == maxAmmo");
}
```

Тест 3 -- PushesRocketReloadTime_ToHudData:
```csharp
[Test]
public void PushesRocketReloadTime_ToHudData()
{
    var hudData = new HudData();
    _system.SetHudData(hudData);
    _system.SetRocketMaxAmmo(3);

    var entity = CreateFullShipEntity(
        position: float2.zero,
        speed: 0f,
        direction: float2.zero,
        rotation: new float2(1f, 0f),
        thrustActive: false,
        laserCurrentShoots: 3,
        laserMaxShoots: 3,
        reloadRemaining: 0f);
    m_Manager.SetComponentData(entity, new RocketAmmoData
    {
        CurrentAmmo = 1,
        MaxAmmo = 3,
        ReloadRemaining = 4.7f,
        ReloadDurationSec = 5.0f
    });

    _system.Update();

    Assert.IsTrue(hudData.RocketReloadTime.Value.Contains("Reload rocket:"),
        "RocketReloadTime should contain 'Reload rocket:'");
    Assert.IsTrue(hudData.RocketReloadTime.Value.Contains("sec"),
        "RocketReloadTime should contain 'sec'");
}
```
  </action>
  <verify>
    <automated>grep -n "SetRocketMaxAmmo\|_rocketMaxAmmo\|RocketAmmoData\|PushesRocket" Assets/Scripts/Bridge/ObservableBridgeSystem.cs Assets/Scripts/Application/Screens/GameScreen.cs Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - ObservableBridgeSystem.cs содержит `private int _rocketMaxAmmo;`
    - ObservableBridgeSystem.cs содержит `public void SetRocketMaxAmmo(int maxAmmo)`
    - ObservableBridgeSystem.cs содержит `RefRO<RocketAmmoData>` в Query
    - ObservableBridgeSystem.cs содержит `_hudData.RocketAmmoCount.Value = $"Rockets: {rocketCurrentAmmo.ToString()}";`
    - ObservableBridgeSystem.cs содержит `_hudData.IsRocketReloadTimeVisible.Value = rocketCurrentAmmo < _rocketMaxAmmo;`
    - GameScreen.cs содержит `bridge.SetRocketMaxAmmo(_configs.Rocket.MaxAmmo);`
    - ObservableBridgeSystemTests.cs содержит метод `PushesRocketData_ToHudData`
    - ObservableBridgeSystemTests.cs содержит метод `PushesRocketReloadVisibility_HiddenWhenFull`
    - ObservableBridgeSystemTests.cs содержит метод `PushesRocketReloadTime_ToHudData`
  </acceptance_criteria>
  <done>ObservableBridgeSystem читает RocketAmmoData и пишет в HudData, GameScreen передаёт MaxAmmo, 3 новых теста написаны, все существующие тесты проходят</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECS -> HUD | Данные из ECS компонентов отображаются в UI без валидации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I (Information Disclosure) | HudData | accept | HUD показывает только игровые данные (боезапас, таймер) -- нет PII |
| T-15-02 | T (Tampering) | RocketAmmoData | accept | ECS данные read-only через RefRO, клиентская игра без серверной логики |
</threat_model>

<verification>
1. `grep -rn "RocketAmmoCount\|RocketReloadTime\|IsRocketReloadTimeVisible" Assets/Scripts/View/HudVisual.cs` -- 8 совпадений (3 в HudData + 2 SerializeField + 3 Bind)
2. `grep -rn "SetRocketMaxAmmo\|_rocketMaxAmmo" Assets/Scripts/Bridge/ObservableBridgeSystem.cs` -- 3 совпадения (field + setter + usage)
3. `grep -rn "SetRocketMaxAmmo" Assets/Scripts/Application/Screens/GameScreen.cs` -- 1 совпадение
4. `grep -c "PushesRocket" Assets/Tests/EditMode/ECS/ObservableBridgeSystemTests.cs` -- 3 теста
</verification>

<success_criteria>
- HudData содержит RocketAmmoCount, RocketReloadTime, IsRocketReloadTimeVisible
- HudVisual содержит _rocketAmmoCount, _rocketReloadTime SerializeField и 3 Bind
- ObservableBridgeSystem Query включает RefRO&lt;RocketAmmoData&gt; и пишет данные в HudData
- GameScreen.ActivateHud() вызывает bridge.SetRocketMaxAmmo()
- 3 новых теста в ObservableBridgeSystemTests
- Код компилируется без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/15-hud/15-01-SUMMARY.md`
</output>
