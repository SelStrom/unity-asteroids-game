---
phase: 12-bridge-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - Assets/Tests/EditMode/ECS/RocketLifecycleTests.cs
autonomous: false
requirements: [TEST-02]
must_haves:
  truths:
    - "Интеграционные тесты покрывают полный lifecycle ракеты: спавн -> компоненты -> DeadTag -> cleanup"
    - "Тесты проверяют что GameObjectRef корректно привязывается и уничтожается"
    - "Тесты проверяют синхронизацию rotation из Direction через GameObjectSyncSystem"
  artifacts:
    - path: "Assets/Tests/EditMode/ECS/RocketLifecycleTests.cs"
      provides: "Интеграционные тесты lifecycle ракеты"
      contains: "class RocketLifecycleTests"
  key_links:
    - from: "RocketLifecycleTests.cs"
      to: "GameObjectSyncSystem + DeadEntityCleanupSystem"
      via: "Managed systems через World.CreateSystemManaged"
      pattern: "CreateSystemManaged"
---

<objective>
Интеграционные тесты полного lifecycle ракеты: от спавна entity с GameObjectRef до cleanup через DeadEntityCleanupSystem.

Purpose: TEST-02 требует интеграционные тесты lifecycle. Тесты доказывают что ракета корректно проходит весь путь: создание -> синхронизация позиции/вращения -> пометка DeadTag -> уничтожение entity и вызов callback.
Output: RocketLifecycleTests.cs с набором тестов lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bridge-lifecycle/12-CONTEXT.md
@.planning/phases/12-bridge-lifecycle/12-01-SUMMARY.md
@.planning/phases/12-bridge-lifecycle/12-02-SUMMARY.md

<interfaces>
<!-- Тестовый fixture и системы -->

From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs:
```csharp
public class AsteroidsEcsTestFixture
{
    protected World World;
    protected EntityManager m_Manager;

    public virtual void SetUp() { World = new World("Test"); m_Manager = World.EntityManager; }
    public virtual void TearDown() { if (World != null && World.IsCreated) { World.Dispose(); } World = null; }

    protected Entity CreateRocketEntity(
        float2 position, float speed, float2 direction, float lifeTime,
        float turnRateDegPerSec = 180f) { ... }

    protected Entity CreateAsteroidEntity(
        float2 position, float speed, float2 direction, int age, int score = 100) { ... }

    protected Entity CreateRocketShootEventSingleton() { ... }
    protected Entity CreateScoreDataSingleton(int value = 0) { ... }
    protected Entity CreateGameAreaSingleton(float2 size) { ... }
}
```

From Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs:
```csharp
public struct DeadEntityInfo
{
    public GameObject GameObject;
    public int Age;
    public float Speed;
}

[UpdateInGroup(typeof(LateSimulationSystemGroup))]
public partial class DeadEntityCleanupSystem : SystemBase
{
    public void SetOnDeadEntityCallback(Action<DeadEntityInfo> callback) { ... }
    protected override void OnUpdate() { ... }
}
```

From Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs (после плана 01):
```csharp
[UpdateInGroup(typeof(PresentationSystemGroup))]
public partial class GameObjectSyncSystem : SystemBase
{
    // Ветка 1: MoveData + RotateData + GameObjectRef
    // Ветка 2: MoveData + GameObjectRef, .WithNone<RotateData>().WithNone<RocketTag>()
    // Ветка 3: MoveData + GameObjectRef, .WithAll<RocketTag>().WithNone<RotateData>() -- rotation из Direction
}
```

From Assets/Scripts/ECS/Components/Tags/RocketTag.cs:
```csharp
public struct RocketTag : IComponentData { }
```

From Assets/Scripts/ECS/Components/DeadTag.cs:
```csharp
public struct DeadTag : IComponentData { }
```

From Assets/Scripts/ECS/GameObjectRef.cs:
```csharp
public class GameObjectRef : IComponentData
{
    public Transform Transform;
    public GameObject GameObject;
}
```

From Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs (паттерн тестирования):
```csharp
public class DeadEntityCleanupSystemTests : AsteroidsEcsTestFixture
{
    private DeadEntityCleanupSystem _cleanupSystem;
    private List<DeadEntityInfo> _callbackResults;
    private List<GameObject> _testObjects;

    public override void SetUp()
    {
        base.SetUp();
        _cleanupSystem = World.CreateSystemManaged<DeadEntityCleanupSystem>();
        _callbackResults = new List<DeadEntityInfo>();
        _cleanupSystem.SetOnDeadEntityCallback(info => _callbackResults.Add(info));
        _testObjects = new List<GameObject>();
    }

    public override void TearDown()
    {
        foreach (var go in _testObjects) { if (go != null) { Object.DestroyImmediate(go); } }
        _testObjects.Clear();
        base.TearDown();
    }

    private GameObject CreateTestGameObject(string name = "test")
    {
        var go = new GameObject(name);
        _testObjects.Add(go);
        return go;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Интеграционные тесты lifecycle ракеты (D-13, D-14, TEST-02)</name>
  <files>Assets/Tests/EditMode/ECS/RocketLifecycleTests.cs</files>
  <read_first>
    - Assets/Tests/EditMode/ECS/DeadEntityCleanupSystemTests.cs
    - Assets/Tests/EditMode/ECS/GameObjectSyncSystemTests.cs
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
    - Assets/Scripts/Bridge/DeadEntityCleanupSystem.cs
    - Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs
  </read_first>
  <action>
Создать `Assets/Tests/EditMode/ECS/RocketLifecycleTests.cs`. Класс наследует `AsteroidsEcsTestFixture`. В SetUp создавать обе managed-системы: `GameObjectSyncSystem` и `DeadEntityCleanupSystem`. Хранить `List<GameObject> _testObjects` для cleanup, `List<DeadEntityInfo> _callbackResults` для проверки callback.

Тесты:

**Test 1: RocketLifecycle_SpawnEntityWithComponents**
- Создать entity через `CreateRocketEntity(float2.zero, 8f, new float2(1f, 0f), 5f, 180f)`.
- Assert: entity существует, имеет `RocketTag`, `MoveData`, `LifeTimeData`, `RocketTargetData`.
- Assert: НЕ имеет `RotateData` (D-02).

**Test 2: RocketLifecycle_GameObjectRefSync**
- Создать entity через `CreateRocketEntity(new float2(5f, 3f), 8f, new float2(0f, 1f), 5f)`.
- Создать `new GameObject("Rocket")`, добавить `GameObjectRef` через `m_Manager.AddComponentObject`.
- Вызвать `_syncSystem.Update()`.
- Assert: position.x == 5f, position.y == 3f.
- Assert: eulerAngles.z == 90f (direction (0,1) = 90 градусов).

**Test 3: RocketLifecycle_DeadTagTriggersCleanup**
- Создать entity через `CreateRocketEntity(...)`.
- Создать `new GameObject("Rocket")`, добавить `GameObjectRef`.
- Добавить `DeadTag` на entity.
- Вызвать `_cleanupSystem.Update()`.
- Assert: entity больше не существует (`m_Manager.Exists(entity)` == false).
- Assert: `_callbackResults.Count == 1`.
- Assert: `_callbackResults[0].GameObject == go`.

**Test 4: RocketLifecycle_FullCycle_SpawnSyncDead**
- Создать entity через `CreateRocketEntity(new float2(1f, 2f), 8f, new float2(1f, 0f), 5f)`.
- Создать `new GameObject("Rocket")`, добавить `GameObjectRef`.
- Шаг 1: Вызвать `_syncSystem.Update()`. Assert: position == (1, 2), eulerAngles.z == 0 (direction (1,0)).
- Шаг 2: Изменить MoveData: position=(3, 4), direction=(0, 1). Вызвать `_syncSystem.Update()`. Assert: position == (3, 4), eulerAngles.z == 90.
- Шаг 3: Добавить `DeadTag`. Вызвать `_cleanupSystem.Update()`. Assert: entity не существует, callback вызван.

**Test 5: RocketLifecycle_NoRotateData_NeverAdded**
- Создать entity через `CreateRocketEntity(...)`.
- Assert: `m_Manager.HasComponent<RotateData>(entity)` == false.
- Это подтверждает D-02 -- ракета никогда не получает RotateData.

Полный код тестового класса:

```csharp
using System.Collections.Generic;
using NUnit.Framework;
using SelStrom.Asteroids.ECS;
using Unity.Entities;
using Unity.Mathematics;
using UnityEngine;

namespace SelStrom.Asteroids.Tests.EditMode.ECS
{
    public class RocketLifecycleTests : AsteroidsEcsTestFixture
    {
        private GameObjectSyncSystem _syncSystem;
        private DeadEntityCleanupSystem _cleanupSystem;
        private List<DeadEntityInfo> _callbackResults;
        private List<GameObject> _testObjects;

        public override void SetUp()
        {
            base.SetUp();
            _syncSystem = World.CreateSystemManaged<GameObjectSyncSystem>();
            _cleanupSystem = World.CreateSystemManaged<DeadEntityCleanupSystem>();
            _callbackResults = new List<DeadEntityInfo>();
            _cleanupSystem.SetOnDeadEntityCallback(info => _callbackResults.Add(info));
            _testObjects = new List<GameObject>();
        }

        public override void TearDown()
        {
            foreach (var go in _testObjects)
            {
                if (go != null)
                {
                    Object.DestroyImmediate(go);
                }
            }

            _testObjects.Clear();
            base.TearDown();
        }

        private GameObject CreateTestGameObject(string name = "test")
        {
            var go = new GameObject(name);
            _testObjects.Add(go);
            return go;
        }

        [Test]
        public void RocketLifecycle_SpawnEntityWithComponents()
        {
            var entity = CreateRocketEntity(float2.zero, 8f, new float2(1f, 0f), 5f, 180f);

            Assert.IsTrue(m_Manager.Exists(entity));
            Assert.IsTrue(m_Manager.HasComponent<RocketTag>(entity));
            Assert.IsTrue(m_Manager.HasComponent<MoveData>(entity));
            Assert.IsTrue(m_Manager.HasComponent<LifeTimeData>(entity));
            Assert.IsTrue(m_Manager.HasComponent<RocketTargetData>(entity));
            Assert.IsFalse(m_Manager.HasComponent<RotateData>(entity));
        }

        [Test]
        public void RocketLifecycle_GameObjectRefSync()
        {
            var entity = CreateRocketEntity(new float2(5f, 3f), 8f, new float2(0f, 1f), 5f);
            var go = CreateTestGameObject("Rocket");
            m_Manager.AddComponentObject(entity, new GameObjectRef
            {
                Transform = go.transform,
                GameObject = go
            });

            _syncSystem.Update();

            Assert.AreEqual(5f, go.transform.position.x, 0.001f);
            Assert.AreEqual(3f, go.transform.position.y, 0.001f);
            Assert.AreEqual(90f, go.transform.eulerAngles.z, 0.1f);
        }

        [Test]
        public void RocketLifecycle_DeadTagTriggersCleanup()
        {
            var entity = CreateRocketEntity(float2.zero, 8f, new float2(1f, 0f), 5f);
            var go = CreateTestGameObject("Rocket");
            m_Manager.AddComponentObject(entity, new GameObjectRef
            {
                Transform = go.transform,
                GameObject = go
            });

            m_Manager.AddComponentData(entity, new DeadTag());
            _cleanupSystem.Update();

            Assert.IsFalse(m_Manager.Exists(entity));
            Assert.AreEqual(1, _callbackResults.Count);
            Assert.AreEqual(go, _callbackResults[0].GameObject);
        }

        [Test]
        public void RocketLifecycle_FullCycle_SpawnSyncDead()
        {
            var entity = CreateRocketEntity(new float2(1f, 2f), 8f, new float2(1f, 0f), 5f);
            var go = CreateTestGameObject("Rocket");
            m_Manager.AddComponentObject(entity, new GameObjectRef
            {
                Transform = go.transform,
                GameObject = go
            });

            // Шаг 1: начальная синхронизация
            _syncSystem.Update();
            Assert.AreEqual(1f, go.transform.position.x, 0.001f);
            Assert.AreEqual(2f, go.transform.position.y, 0.001f);
            Assert.AreEqual(0f, go.transform.eulerAngles.z, 0.1f);

            // Шаг 2: обновление позиции и направления
            m_Manager.SetComponentData(entity, new MoveData
            {
                Position = new float2(3f, 4f),
                Speed = 8f,
                Direction = new float2(0f, 1f)
            });
            _syncSystem.Update();
            Assert.AreEqual(3f, go.transform.position.x, 0.001f);
            Assert.AreEqual(4f, go.transform.position.y, 0.001f);
            Assert.AreEqual(90f, go.transform.eulerAngles.z, 0.1f);

            // Шаг 3: уничтожение
            m_Manager.AddComponentData(entity, new DeadTag());
            _cleanupSystem.Update();
            Assert.IsFalse(m_Manager.Exists(entity));
            Assert.AreEqual(1, _callbackResults.Count);
        }

        [Test]
        public void RocketLifecycle_NoRotateData_NeverAdded()
        {
            var entity = CreateRocketEntity(float2.zero, 8f, new float2(1f, 0f), 5f);

            Assert.IsFalse(m_Manager.HasComponent<RotateData>(entity));
        }
    }
}
```
  </action>
  <verify>
    <automated>unity -runTests -testPlatform EditMode -testFilter RocketLifecycle</automated>
  </verify>
  <acceptance_criteria>
    - Файл Assets/Tests/EditMode/ECS/RocketLifecycleTests.cs существует
    - Содержит `class RocketLifecycleTests : AsteroidsEcsTestFixture`
    - Содержит `RocketLifecycle_SpawnEntityWithComponents`
    - Содержит `RocketLifecycle_GameObjectRefSync`
    - Содержит `RocketLifecycle_DeadTagTriggersCleanup`
    - Содержит `RocketLifecycle_FullCycle_SpawnSyncDead`
    - Содержит `RocketLifecycle_NoRotateData_NeverAdded`
    - Содержит `World.CreateSystemManaged<GameObjectSyncSystem>()`
    - Содержит `World.CreateSystemManaged<DeadEntityCleanupSystem>()`
    - 5 тестов проходят
  </acceptance_criteria>
  <done>5 интеграционных тестов lifecycle ракеты зелёные: проверка компонентов, синхронизация позиции и rotation, cleanup через DeadTag, полный цикл спавн->синк->смерть, отсутствие RotateData.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Визуальная проверка компиляции всех файлов</name>
  <files>-</files>
  <action>
Это checkpoint -- пользователь визуально проверяет результат всей фазы 12 в Unity Editor.
Полный bridge-слой ракеты: GameObjectSyncSystem (3 ветки), RocketShootEvent, RocketVisual, EntitiesCatalog.CreateRocket, ShootEventProcessorSystem.ProcessRocketEvents, RocketData в GameData, интеграционные тесты lifecycle.
  </action>
  <what-built>
    Полный bridge-слой ракеты: GameObjectSyncSystem (3 ветки), RocketShootEvent, RocketVisual, EntitiesCatalog.CreateRocket, ShootEventProcessorSystem.ProcessRocketEvents, RocketData в GameData, интеграционные тесты lifecycle.
  </what-built>
  <how-to-verify>
    1. Откройте Unity Editor
    2. Дождитесь компиляции (Console без ошибок)
    3. Проверьте что GameData ScriptableObject (Assets/Configs/) имеет новую секцию "Rocket" с полем Prefab
    4. Запустите тесты: Window -> General -> Test Runner -> EditMode -> Run All
    5. Все тесты GameObjectSyncSystem (8 шт) и RocketLifecycle (5 шт) зелёные
    6. Существующие тесты не сломаны
  </how-to-verify>
  <verify>Пользователь подтверждает компиляцию и тесты в Unity Editor</verify>
  <done>Unity Editor компилирует без ошибок, все тесты зелёные, GameData имеет секцию Rocket</done>
  <resume-signal>Напишите "approved" или опишите проблемы</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет trust boundaries -- внутриигровые тесты.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| -- | -- | -- | accept | Тестовый код, не затрагивает продакшен-безопасность |
</threat_model>

<verification>
- Все 5 тестов RocketLifecycle зелёные
- Все существующие тесты не сломаны
- Unity Editor компилирует без ошибок
</verification>

<success_criteria>
- TEST-02 выполнен: интеграционные тесты покрывают спавн -> синхронизацию -> коллизию (DeadTag) -> уничтожение
- Полный набор из 5 тестов подтверждает lifecycle
- Визуальная проверка компиляции в Unity Editor
</success_criteria>

<output>
After completion, create `.planning/phases/12-bridge-lifecycle/12-03-SUMMARY.md`
</output>
