---
phase: 12-bridge-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - Assets/Scripts/View/RocketVisual.cs
  - Assets/Scripts/Configs/GameData.cs
  - Assets/Scripts/Application/EntitiesCatalog.cs
  - Assets/Scripts/Bridge/ShootEventProcessorSystem.cs
  - Assets/Scripts/Application/Application.cs
  - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "RocketVisual MonoBehaviour существует и пробрасывает коллизии через ViewModel"
    - "EntitiesCatalog.CreateRocket() создаёт ракету с ViewModel, Visual, GameObjectRef и CollisionBridge"
    - "ShootEventProcessorSystem обрабатывает RocketShootEvent и вызывает CreateRocket"
    - "RocketShootEvent buffer singleton создаётся в Application при старте игры"
  artifacts:
    - path: "Assets/Scripts/View/RocketVisual.cs"
      provides: "RocketViewModel + RocketVisual (аналог BulletVisual)"
      contains: "class RocketVisual : AbstractWidgetView<RocketViewModel>"
    - path: "Assets/Scripts/Application/EntitiesCatalog.cs"
      provides: "CreateRocket() + EntityType.Rocket"
      contains: "public void CreateRocket"
    - path: "Assets/Scripts/Bridge/ShootEventProcessorSystem.cs"
      provides: "Обработка RocketShootEvent"
      contains: "ProcessRocketEvents"
    - path: "Assets/Scripts/Configs/GameData.cs"
      provides: "RocketData struct с полем Prefab"
      contains: "struct RocketData"
    - path: "Assets/Scripts/Application/Application.cs"
      provides: "RocketShootEvent buffer singleton"
      contains: "AddBuffer<RocketShootEvent>"
  key_links:
    - from: "ShootEventProcessorSystem.cs"
      to: "EntitiesCatalog.CreateRocket"
      via: "ProcessRocketEvents -> _catalog.CreateRocket()"
      pattern: "_catalog\\.CreateRocket"
    - from: "EntitiesCatalog.cs"
      to: "EntityFactory.CreateRocket + GameObjectRef"
      via: "CreateRocket вызывает EntityFactory и добавляет GameObjectRef"
      pattern: "EntityFactory\\.CreateRocket"
---

<objective>
Визуальное представление ракеты: RocketVisual, фабричный метод CreateRocket, обработка RocketShootEvent и конфигурация RocketData.

Purpose: Ракета должна отображаться в сцене (VIS-01) и создаваться через событийную систему (D-07, D-08). Это полный bridge между ECS и GameObject для ракеты.
Output: RocketVisual.cs, обновлённые EntitiesCatalog, ShootEventProcessorSystem, GameData, Application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bridge-lifecycle/12-CONTEXT.md
@.planning/phases/12-bridge-lifecycle/12-01-SUMMARY.md

<interfaces>
<!-- Паттерны из кодовой базы для точного следования -->

From Assets/Scripts/View/BulletVisual.cs (точный шаблон для RocketVisual):
```csharp
namespace SelStrom.Asteroids
{
    public class BulletViewModel : AbstractViewModel
    {
        public readonly ReactiveValue<Action<Collision2D>> OnCollision = new();
    }

    public class BulletVisual : AbstractWidgetView<BulletViewModel>, IEntityView
    {
        [SerializeField] private Collider2D _collider = default;

        protected override void OnConnected()
        {
            _collider.enabled = true;
        }

        private void OnCollisionEnter2D(Collision2D col)
        {
            ViewModel.OnCollision.Value?.Invoke(col);
        }
    }
}
```

From Assets/Scripts/Application/EntitiesCatalog.cs -- CreateBullet (шаблон для CreateRocket):
```csharp
public void CreateBullet(GameData.BulletData data, GameObject prefab, Vector2 position, Vector2 direction)
{
    var viewModel = new BulletViewModel();
    var bindings = new EventBindingContext();
    bindings.InvokeAll();

    var view = _viewFactory.Get<BulletVisual>(prefab);
    view.Connect(viewModel);

    var entity = EntityFactory.CreateBullet(
        _entityManager, new float2(position.x, position.y), data.Speed,
        new float2(direction.x, direction.y), data.LifeTimeSeconds, isPlayer);
    _entityManager.AddComponentObject(entity, new GameObjectRef
    {
        Transform = view.transform,
        GameObject = view.gameObject
    });
    _collisionBridge.RegisterMapping(view.gameObject, entity);

    viewModel.OnCollision.Value = col =>
    {
        _collisionBridge.ReportCollision(view.gameObject, col.gameObject);
    };

    AddToCatalog(view.gameObject, entity, EntityType.Bullet, bindings);
}
```

From Assets/Scripts/Application/EntitiesCatalog.cs -- EntityType enum:
```csharp
public enum EntityType
{
    Ship,
    Asteroid,
    Bullet,
    UfoBig,
    Ufo
}
```

From Assets/Scripts/Configs/GameData.cs -- BulletData struct (шаблон для RocketData):
```csharp
[Serializable]
public struct BulletData
{
    public GameObject Prefab;
    public GameObject EnemyPrefab;
    public int LifeTimeSeconds;
    public float Speed;
}
```

From Assets/Scripts/ECS/EntityFactory.cs -- CreateRocket (уже существует из Phase 10):
```csharp
public static Entity CreateRocket(
    EntityManager em, float2 position, float speed,
    float2 direction, float lifeTime, float turnRateDegPerSec)
{
    var entity = em.CreateEntity();
    em.AddComponentData(entity, new RocketTag());
    em.AddComponentData(entity, new MoveData { Position = position, Speed = speed, Direction = direction });
    em.AddComponentData(entity, new LifeTimeData { TimeRemaining = lifeTime });
    em.AddComponentData(entity, new RocketTargetData { Target = Entity.Null, TurnRateDegPerSec = turnRateDegPerSec });
    return entity;
}
```

From Assets/Scripts/Bridge/ShootEventProcessorSystem.cs -- ProcessGunEvents (шаблон):
```csharp
private readonly List<GunShootEvent> _pendingGunEvents = new();

private void ProcessGunEvents()
{
    _pendingGunEvents.Clear();
    foreach (var buffer in SystemAPI.Query<DynamicBuffer<GunShootEvent>>())
    {
        if (buffer.Length == 0) { continue; }
        for (int i = 0; i < buffer.Length; i++) { _pendingGunEvents.Add(buffer[i]); }
        buffer.Clear();
    }
    for (int i = 0; i < _pendingGunEvents.Count; i++)
    {
        var evt = _pendingGunEvents[i];
        var position = new Vector2(evt.Position.x, evt.Position.y);
        var direction = new Vector2(evt.Direction.x, evt.Direction.y);
        var prefab = evt.IsPlayer ? _configs.Bullet.Prefab : _configs.Bullet.EnemyPrefab;
        _catalog.CreateBullet(_configs.Bullet, prefab, position, direction);
    }
}
```

From Assets/Scripts/Application/Application.cs строки 144-168 -- паттерн создания ShootEvent buffer singleton:
```csharp
// GunShootEvent buffer singleton
var gunQuery = _entityManager.CreateEntityQuery(typeof(GunShootEvent));
if (gunQuery.CalculateEntityCount() == 0)
{
    var gunEventEntity = _entityManager.CreateEntity();
    _entityManager.AddBuffer<GunShootEvent>(gunEventEntity);
}
else
{
    var existingEntity = gunQuery.GetSingletonEntity();
    _entityManager.GetBuffer<GunShootEvent>(existingEntity).Clear();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RocketVisual, RocketData, EntityType.Rocket, CreateRocket (D-04, D-05, D-06, D-10, D-11, D-12, VIS-01)</name>
  <files>Assets/Scripts/View/RocketVisual.cs, Assets/Scripts/Configs/GameData.cs, Assets/Scripts/Application/EntitiesCatalog.cs</files>
  <read_first>
    - Assets/Scripts/View/BulletVisual.cs
    - Assets/Scripts/Configs/GameData.cs
    - Assets/Scripts/Application/EntitiesCatalog.cs
    - Assets/Scripts/ECS/EntityFactory.cs
    - Assets/Scripts/Bridge/CollisionBridge.cs
    - Assets/Scripts/View/Base/IEntityView.cs
  </read_first>
  <action>
1. Создать `Assets/Scripts/View/RocketVisual.cs` по точному шаблону BulletVisual:

```csharp
using System;
using Shtl.Mvvm;
using UnityEngine;

namespace SelStrom.Asteroids
{
    public class RocketViewModel : AbstractViewModel
    {
        public readonly ReactiveValue<Action<Collision2D>> OnCollision = new();
    }

    public class RocketVisual : AbstractWidgetView<RocketViewModel>, IEntityView
    {
        [SerializeField] private Collider2D _collider = default;

        protected override void OnConnected()
        {
            _collider.enabled = true;
        }

        private void OnCollisionEnter2D(Collision2D col)
        {
            ViewModel.OnCollision.Value?.Invoke(col);
        }
    }
}
```

2. В `Assets/Scripts/Configs/GameData.cs` добавить struct `RocketData` и поле:
   - Добавить перед закрывающей скобкой класса GameData:
```csharp
[Serializable]
public struct RocketData
{
    public GameObject Prefab;
}
```
   - Добавить поле в класс GameData после `public ShipData Ship;`:
```csharp
[Space]
public RocketData Rocket;
```

3. В `Assets/Scripts/Application/EntitiesCatalog.cs`:
   - Добавить `Rocket` в enum `EntityType` (после `Ufo`):
```csharp
public enum EntityType
{
    Ship,
    Asteroid,
    Bullet,
    UfoBig,
    Ufo,
    Rocket
}
```
   - Добавить метод `CreateRocket(Vector2 position, Vector2 direction)` по шаблону CreateBullet. Временные значения speed=8f, lifeTime=5f, turnRate=180f (Phase 14 вынесет в конфиг):

```csharp
public void CreateRocket(Vector2 position, Vector2 direction)
{
    var viewModel = new RocketViewModel();
    var bindings = new EventBindingContext();
    bindings.InvokeAll();

    var view = _viewFactory.Get<RocketVisual>(_configs.Rocket.Prefab);
    view.Connect(viewModel);

    // TODO Phase 14: вынести speed, lifeTime, turnRateDegPerSec в RocketData конфиг
    var entity = EntityFactory.CreateRocket(
        _entityManager,
        new float2(position.x, position.y),
        speed: 8f,
        new float2(direction.x, direction.y),
        lifeTime: 5f,
        turnRateDegPerSec: 180f
    );
    _entityManager.AddComponentObject(entity, new GameObjectRef
    {
        Transform = view.transform,
        GameObject = view.gameObject
    });
    _collisionBridge.RegisterMapping(view.gameObject, entity);

    viewModel.OnCollision.Value = col =>
    {
        _collisionBridge.ReportCollision(view.gameObject, col.gameObject);
    };

    AddToCatalog(view.gameObject, entity, EntityType.Rocket, bindings);
}
```

Добавить using для float2 если отсутствует: `using Unity.Mathematics;`
  </action>
  <verify>
    <automated>grep -n "class RocketVisual" Assets/Scripts/View/RocketVisual.cs && grep -n "CreateRocket" Assets/Scripts/Application/EntitiesCatalog.cs && grep -n "Rocket" Assets/Scripts/Configs/GameData.cs</automated>
  </verify>
  <acceptance_criteria>
    - Assets/Scripts/View/RocketVisual.cs существует
    - RocketVisual.cs содержит `class RocketViewModel : AbstractViewModel`
    - RocketVisual.cs содержит `class RocketVisual : AbstractWidgetView<RocketViewModel>, IEntityView`
    - RocketVisual.cs содержит `OnCollisionEnter2D`
    - GameData.cs содержит `public struct RocketData`
    - GameData.cs содержит `public RocketData Rocket;`
    - EntitiesCatalog.cs содержит `Rocket` в enum EntityType
    - EntitiesCatalog.cs содержит `public void CreateRocket(Vector2 position, Vector2 direction)`
    - EntitiesCatalog.cs содержит `EntityFactory.CreateRocket(`
    - EntitiesCatalog.cs содержит `_collisionBridge.RegisterMapping(view.gameObject, entity)`
    - EntitiesCatalog.cs содержит `AddToCatalog(view.gameObject, entity, EntityType.Rocket, bindings)`
    - EntitiesCatalog.cs содержит `new GameObjectRef` в CreateRocket
  </acceptance_criteria>
  <done>RocketVisual и RocketViewModel созданы по шаблону BulletVisual. RocketData добавлена в GameData. CreateRocket добавлен в EntitiesCatalog с GameObjectRef и CollisionBridge. EntityType.Rocket добавлен в enum.</done>
</task>

<task type="auto">
  <name>Task 2: ShootEventProcessorSystem + RocketShootEvent singleton + test fixture (D-08, D-09)</name>
  <files>Assets/Scripts/Bridge/ShootEventProcessorSystem.cs, Assets/Scripts/Application/Application.cs, Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs</files>
  <read_first>
    - Assets/Scripts/Bridge/ShootEventProcessorSystem.cs
    - Assets/Scripts/Application/Application.cs
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
    - Assets/Scripts/ECS/Components/RocketShootEvent.cs
  </read_first>
  <action>
1. В `Assets/Scripts/Bridge/ShootEventProcessorSystem.cs`:
   - Добавить `using SelStrom.Asteroids.ECS;` если отсутствует (RocketShootEvent в этом namespace).
   - Добавить приватный список: `private readonly List<RocketShootEvent> _pendingRocketEvents = new();`
   - В `OnUpdate()` добавить вызов `ProcessRocketEvents();` после `ProcessLaserEvents();`:
```csharp
protected override void OnUpdate()
{
    if (_catalog == null || _configs == null)
    {
        return;
    }

    ProcessGunEvents();
    ProcessLaserEvents();
    ProcessRocketEvents();
}
```
   - Добавить метод `ProcessRocketEvents()` по точному шаблону ProcessGunEvents:
```csharp
private void ProcessRocketEvents()
{
    _pendingRocketEvents.Clear();

    foreach (var buffer in SystemAPI.Query<DynamicBuffer<RocketShootEvent>>())
    {
        if (buffer.Length == 0)
        {
            continue;
        }

        for (int i = 0; i < buffer.Length; i++)
        {
            _pendingRocketEvents.Add(buffer[i]);
        }

        buffer.Clear();
    }

    for (int i = 0; i < _pendingRocketEvents.Count; i++)
    {
        var evt = _pendingRocketEvents[i];
        var position = new Vector2(evt.Position.x, evt.Position.y);
        var direction = new Vector2(evt.Direction.x, evt.Direction.y);
        _catalog.CreateRocket(position, direction);
    }
}
```

2. В `Assets/Scripts/Application/Application.cs` добавить создание RocketShootEvent buffer singleton по паттерну GunShootEvent/LaserShootEvent. Найти блок после `// LaserShootEvent buffer singleton` (строки ~157-168) и ПОСЛЕ него добавить:
```csharp
// RocketShootEvent buffer singleton
var rocketQuery = _entityManager.CreateEntityQuery(typeof(RocketShootEvent));
if (rocketQuery.CalculateEntityCount() == 0)
{
    var rocketEventEntity = _entityManager.CreateEntity();
    _entityManager.AddBuffer<RocketShootEvent>(rocketEventEntity);
}
else
{
    var existingEntity = rocketQuery.GetSingletonEntity();
    _entityManager.GetBuffer<RocketShootEvent>(existingEntity).Clear();
}
```
Добавить `using SelStrom.Asteroids.ECS;` если отсутствует.

3. В `Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs` добавить helper-метод для создания RocketShootEvent singleton (по паттерну CreateGunShootEventSingleton/CreateLaserShootEventSingleton):
```csharp
protected Entity CreateRocketShootEventSingleton()
{
    var entity = m_Manager.CreateEntity();
    m_Manager.AddBuffer<RocketShootEvent>(entity);
    return entity;
}
```
  </action>
  <verify>
    <automated>grep -n "ProcessRocketEvents" Assets/Scripts/Bridge/ShootEventProcessorSystem.cs && grep -n "RocketShootEvent" Assets/Scripts/Application/Application.cs && grep -n "CreateRocketShootEventSingleton" Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs</automated>
  </verify>
  <acceptance_criteria>
    - ShootEventProcessorSystem.cs содержит `ProcessRocketEvents();` в OnUpdate
    - ShootEventProcessorSystem.cs содержит `private void ProcessRocketEvents()`
    - ShootEventProcessorSystem.cs содержит `_pendingRocketEvents` список
    - ShootEventProcessorSystem.cs содержит `SystemAPI.Query<DynamicBuffer<RocketShootEvent>>()`
    - ShootEventProcessorSystem.cs содержит `_catalog.CreateRocket(position, direction)`
    - Application.cs содержит `AddBuffer<RocketShootEvent>`
    - Application.cs содержит `// RocketShootEvent buffer singleton`
    - AsteroidsEcsTestFixture.cs содержит `protected Entity CreateRocketShootEventSingleton()`
    - AsteroidsEcsTestFixture.cs содержит `m_Manager.AddBuffer<RocketShootEvent>(entity)`
  </acceptance_criteria>
  <done>ShootEventProcessorSystem обрабатывает RocketShootEvent и вызывает CreateRocket. Application создаёт RocketShootEvent buffer singleton. Тестовый fixture имеет helper для создания singleton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет trust boundaries -- внутриигровой bridge-слой.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| -- | -- | -- | accept | Фаза не затрагивает сеть, аутентификацию или пользовательские данные |
</threat_model>

<verification>
- RocketVisual.cs компилируется и следует паттерну BulletVisual
- EntitiesCatalog.CreateRocket() создаёт entity с GameObjectRef
- ShootEventProcessorSystem обрабатывает RocketShootEvent
- Application создаёт RocketShootEvent buffer singleton
- Все существующие тесты проходят
</verification>

<success_criteria>
- Полный bridge между ECS ракетой и GameObject: событие -> обработка -> создание визуала с GameObjectRef и CollisionBridge
- RocketData конфиг с ссылкой на префаб
- Тестовый fixture готов для интеграционных тестов плана 03
</success_criteria>

<output>
After completion, create `.planning/phases/12-bridge-lifecycle/12-02-SUMMARY.md`
</output>
