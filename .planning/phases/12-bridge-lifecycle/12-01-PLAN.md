---
phase: 12-bridge-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs
  - Assets/Scripts/ECS/Components/RocketShootEvent.cs
  - Assets/Tests/EditMode/ECS/GameObjectSyncSystemTests.cs
autonomous: true
requirements: [VIS-03]
must_haves:
  truths:
    - "Ракета с RocketTag синхронизирует позицию и вращение из MoveData.Direction"
    - "Пули и астероиды (без RocketTag) синхронизируют только позицию, как раньше"
    - "RocketShootEvent существует как IBufferElementData для событийной модели"
  artifacts:
    - path: "Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs"
      provides: "Третья ветка для RocketTag: позиция + rotation из Direction"
      contains: "WithAll<RocketTag>"
    - path: "Assets/Scripts/ECS/Components/RocketShootEvent.cs"
      provides: "Событие запуска ракеты"
      contains: "struct RocketShootEvent : IBufferElementData"
    - path: "Assets/Tests/EditMode/ECS/GameObjectSyncSystemTests.cs"
      provides: "Тест синхронизации rotation ракеты по Direction"
      contains: "SyncsPositionAndRotationFromDirection_ForRocketEntity"
  key_links:
    - from: "GameObjectSyncSystem.cs"
      to: "RocketTag + MoveData + GameObjectRef"
      via: "SystemAPI.Query с WithAll<RocketTag>"
      pattern: "WithAll<RocketTag>"
---

<objective>
ECS-инфраструктура для визуальной синхронизации ракеты: третья ветка GameObjectSyncSystem для rotation по Direction и компонент RocketShootEvent.

Purpose: Ракета должна вращаться по направлению полёта (VIS-03), а не игнорировать rotation как пуля. RocketShootEvent нужен для триггера спавна визуала в следующем плане.
Output: Обновлённый GameObjectSyncSystem с 3 ветками, новый RocketShootEvent, тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bridge-lifecycle/12-CONTEXT.md

<interfaces>
<!-- Ключевые типы и контракты. Исполнитель использует их напрямую. -->

From Assets/Scripts/ECS/Components/Tags/RocketTag.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct RocketTag : IComponentData { }
}
```

From Assets/Scripts/ECS/Components/MoveData.cs:
```csharp
public struct MoveData : IComponentData
{
    public float2 Position;
    public float Speed;
    public float2 Direction;
}
```

From Assets/Scripts/ECS/Components/RotateData.cs:
```csharp
public struct RotateData : IComponentData
{
    public float2 Rotation;
    public float TargetDirection;
}
```

From Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs (ТЕКУЩЕЕ СОСТОЯНИЕ -- 2 ветки):
```csharp
[UpdateInGroup(typeof(PresentationSystemGroup))]
public partial class GameObjectSyncSystem : SystemBase
{
    protected override void OnUpdate()
    {
        // Ветка 1: MoveData + RotateData + GameObjectRef (корабль, UFO)
        foreach (var (move, rotate, goRef) in
                 SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, GameObjectRef>())
        {
            var pos = move.ValueRO.Position;
            goRef.Transform.position = new Vector3(pos.x, pos.y, goRef.Transform.position.z);
            var rot = rotate.ValueRO.Rotation;
            var angle = math.atan2(rot.y, rot.x) * Mathf.Rad2Deg;
            goRef.Transform.rotation = Quaternion.Euler(0f, 0f, angle);
        }

        // Ветка 2: MoveData + GameObjectRef, без RotateData (астероиды, пули)
        foreach (var (move, goRef) in
                 SystemAPI.Query<RefRO<MoveData>, GameObjectRef>()
                     .WithNone<RotateData>())
        {
            var pos = move.ValueRO.Position;
            goRef.Transform.position = new Vector3(pos.x, pos.y, goRef.Transform.position.z);
        }
    }
}
```

From Assets/Scripts/ECS/Components/GunShootEvent.cs (паттерн для RocketShootEvent):
```csharp
public struct GunShootEvent : IBufferElementData
{
    public Entity ShooterEntity;
    public float2 Position;
    public float2 Direction;
    public bool IsPlayer;
}
```

From Assets/Scripts/ECS/Components/LaserShootEvent.cs:
```csharp
public struct LaserShootEvent : IBufferElementData
{
    public Entity ShooterEntity;
    public float2 Position;
    public float2 Direction;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Третья ветка GameObjectSyncSystem для RocketTag (D-01, D-02, D-03, VIS-03)</name>
  <files>Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs, Assets/Tests/EditMode/ECS/GameObjectSyncSystemTests.cs</files>
  <read_first>
    - Assets/Scripts/ECS/Systems/GameObjectSyncSystem.cs
    - Assets/Tests/EditMode/ECS/GameObjectSyncSystemTests.cs
    - Assets/Scripts/ECS/Components/Tags/RocketTag.cs
    - Assets/Scripts/ECS/Components/MoveData.cs
  </read_first>
  <behavior>
    - Test 1 (SyncsPositionAndRotationFromDirection_ForRocketEntity): Создать entity с RocketTag + MoveData(Position=(5,3), Direction=(0,1)) + GameObjectRef. После Update() проверить position=(5,3) и eulerAngles.z=90 (atan2(1,0) = 90 градусов).
    - Test 2 (RocketEntity_DoesNotAffectPositionOnlyBranch): Создать entity с RocketTag + MoveData + GameObjectRef (без RotateData). Также создать обычную entity без RocketTag и без RotateData. После Update() обычная entity должна иметь rotation == Quaternion.identity (только позиция), а ракета должна иметь rotation по Direction.
    - Test 3 (RocketRotation_ZeroDegrees_ForRightDirection): Ракета с Direction=(1,0) должна иметь eulerAngles.z=0.
    - Test 4 (RocketRotation_180Degrees_ForLeftDirection): Ракета с Direction=(-1,0) должна иметь eulerAngles.z=180.
  </behavior>
  <action>
1. В `GameObjectSyncSystemTests.cs` добавить 4 теста (описаны в behavior). Каждый тест создаёт entity через `m_Manager.CreateEntity()`, добавляет `RocketTag`, `MoveData`, `GameObjectRef`, вызывает `_system.Update()`, проверяет position и rotation через Assert.AreEqual.

2. Запустить тесты -- они должны УПАСТЬ, потому что вторая ветка GameObjectSyncSystem не устанавливает rotation для ракет.

3. В `GameObjectSyncSystem.cs` модифицировать `OnUpdate()`:
   - Ветка 1 (без изменений): `SystemAPI.Query<RefRO<MoveData>, RefRO<RotateData>, GameObjectRef>()` -- корабль, UFO.
   - Ветка 2 (ИЗМЕНИТЬ): добавить `.WithNone<RocketTag>()` чтобы исключить ракеты. Было: `.WithNone<RotateData>()`. Стало: `.WithNone<RotateData>().WithNone<RocketTag>()`.
   - Ветка 3 (НОВАЯ): `SystemAPI.Query<RefRO<MoveData>, GameObjectRef>().WithAll<RocketTag>().WithNone<RotateData>()` -- ракеты. Внутри:
     ```csharp
     var pos = move.ValueRO.Position;
     goRef.Transform.position = new Vector3(pos.x, pos.y, goRef.Transform.position.z);
     var dir = move.ValueRO.Direction;
     var angle = math.atan2(dir.y, dir.x) * Mathf.Rad2Deg;
     goRef.Transform.rotation = Quaternion.Euler(0f, 0f, angle);
     ```

4. Запустить тесты -- все должны ПРОЙТИ. Существующие тесты тоже должны пройти (SyncsPositionOnly_ForEntityWithoutRotateData не имеет RocketTag, значит попадёт во вторую ветку как раньше).
  </action>
  <verify>
    <automated>unity -runTests -testPlatform EditMode -testFilter GameObjectSyncSystem</automated>
  </verify>
  <acceptance_criteria>
    - GameObjectSyncSystem.cs содержит `WithAll<RocketTag>()` в третьей ветке
    - GameObjectSyncSystem.cs содержит `WithNone<RocketTag>()` во второй ветке
    - GameObjectSyncSystem.cs содержит `math.atan2(dir.y, dir.x) * Mathf.Rad2Deg` в третьей ветке
    - GameObjectSyncSystemTests.cs содержит `SyncsPositionAndRotationFromDirection_ForRocketEntity`
    - GameObjectSyncSystemTests.cs содержит `RocketEntity_DoesNotAffectPositionOnlyBranch`
    - Все тесты GameObjectSyncSystem проходят (8 тестов: 4 старых + 4 новых)
  </acceptance_criteria>
  <done>GameObjectSyncSystem имеет 3 ветки: (1) MoveData+RotateData, (2) MoveData без RotateData и без RocketTag, (3) MoveData+RocketTag -- rotation из Direction. Все 8 тестов зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: RocketShootEvent компонент (D-07)</name>
  <files>Assets/Scripts/ECS/Components/RocketShootEvent.cs</files>
  <read_first>
    - Assets/Scripts/ECS/Components/GunShootEvent.cs
    - Assets/Scripts/ECS/Components/LaserShootEvent.cs
  </read_first>
  <action>
Создать файл `Assets/Scripts/ECS/Components/RocketShootEvent.cs` по паттерну GunShootEvent/LaserShootEvent:

```csharp
using Unity.Entities;
using Unity.Mathematics;

namespace SelStrom.Asteroids.ECS
{
    public struct RocketShootEvent : IBufferElementData
    {
        public Entity ShooterEntity;
        public float2 Position;
        public float2 Direction;
    }
}
```

Поля: `ShooterEntity` (entity корабля), `Position` (позиция запуска), `Direction` (направление запуска). Без `IsPlayer` -- ракеты только у игрока.
  </action>
  <verify>
    <automated>grep -n "struct RocketShootEvent : IBufferElementData" Assets/Scripts/ECS/Components/RocketShootEvent.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл Assets/Scripts/ECS/Components/RocketShootEvent.cs существует
    - Содержит `public struct RocketShootEvent : IBufferElementData`
    - Содержит `public Entity ShooterEntity`
    - Содержит `public float2 Position`
    - Содержит `public float2 Direction`
    - Namespace: `SelStrom.Asteroids.ECS`
  </acceptance_criteria>
  <done>RocketShootEvent.cs создан как IBufferElementData с полями ShooterEntity, Position, Direction в namespace SelStrom.Asteroids.ECS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет trust boundaries -- все изменения внутриигровые (ECS-системы синхронизации).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| -- | -- | -- | accept | Фаза не затрагивает сеть, аутентификацию, ввод пользователя или хранилище данных |
</threat_model>

<verification>
- Все тесты GameObjectSyncSystem зелёные (включая 4 новых для RocketTag)
- Существующие тесты не сломаны (вторая ветка корректно исключает RocketTag)
- RocketShootEvent.cs компилируется
</verification>

<success_criteria>
- GameObjectSyncSystem.cs имеет 3 ветки синхронизации
- Ракеты синхронизируют rotation из MoveData.Direction через math.atan2
- Пули и астероиды по-прежнему синхронизируют только позицию
- RocketShootEvent готов для использования в плане 02
</success_criteria>

<output>
After completion, create `.planning/phases/12-bridge-lifecycle/12-01-SUMMARY.md`
</output>
