---
phase: 11-collision-scoring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
  - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03

must_haves:
  truths:
    - "Ракета получает DeadTag при столкновении с астероидом"
    - "Ракета получает DeadTag при столкновении с UFO (любой тип)"
    - "Враг получает DeadTag при столкновении с ракетой"
    - "Очки начисляются по ScoreValue врага при столкновении с ракетой"
    - "Ракета НЕ взаимодействует с кораблем"
    - "Ракета НЕ взаимодействует с пулей игрока"
    - "Зеркальный порядок entities не влияет на результат"
  artifacts:
    - path: "Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs"
      provides: "IsRocket helper + Rocket+Enemy ветки в ProcessCollision"
      contains: "IsRocket"
    - path: "Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs"
      provides: "7-8 тестов коллизий ракеты"
      contains: "RocketHitsAsteroid"
  key_links:
    - from: "EcsCollisionHandlerSystem.ProcessCollision"
      to: "IsRocket helper"
      via: "вызов IsRocket(ref em, entity)"
      pattern: "IsRocket\\(ref em"
    - from: "EcsCollisionHandlerSystem.ProcessCollision"
      to: "MarkDead + AddScore"
      via: "переиспользование существующих helpers"
      pattern: "MarkDead.*AddScore"
---

<objective>
TDD: Коллизия ракеты с врагами -- DeadTag и начисление очков

Purpose: Ракета взаимодействует с игровым миром. При столкновении с любым врагом (астероид, UFO, UfoBig) оба entity получают DeadTag, очки начисляются по ScoreValue врага. Ракета НЕ коллидирует с кораблём, пулями или другими ракетами.

Output: Расширенная система коллизий с полным TDD-покрытием ракетных столкновений.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-collision-scoring/11-CONTEXT.md
@.planning/phases/11-collision-scoring/11-RESEARCH.md

<interfaces>
<!-- Система коллизий -- точка модификации -->

From Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs:
```csharp
// ProcessCollision -- порядок веток:
// 1. PlayerBullet + Enemy (lines 58-73)
// 2. [СЮДА] Rocket + Enemy
// 3. EnemyBullet + Ship (lines 76-88)
// 4. Ship + Enemy (lines 90-101)
// 5. Asteroid + UfoAny (lines 103-116)

// Существующие helpers:
private bool IsPlayerBullet(ref EntityManager em, Entity entity);
private bool IsEnemyBullet(ref EntityManager em, Entity entity);
private bool IsShip(ref EntityManager em, Entity entity);
private bool IsEnemy(ref EntityManager em, Entity entity); // AsteroidTag || UfoBigTag || UfoTag
private bool IsAsteroid(ref EntityManager em, Entity entity);
private bool IsUfoAny(ref EntityManager em, Entity entity);
private void MarkDead(ref EntityManager em, Entity entity); // Проверяет !HasComponent<DeadTag>
private void AddScore(ref EntityManager em, Entity enemyEntity, ref ScoreData scoreData); // ScoreValue.Score
```

From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs:
```csharp
protected Entity CreateRocketEntity(
    float2 position, float speed, float2 direction, float lifeTime,
    float turnRateDegPerSec = 180f);
// Создает entity с: RocketTag, MoveData, LifeTimeData, RocketTargetData

protected Entity CreateShipEntity(float2 position = default, float speed = 0f, ...);
protected Entity CreateAsteroidEntity(float2 position, float speed, float2 direction, int age, int score = 100);
protected Entity CreateBulletEntity(float2 position, float speed, float2 direction, float lifeTime, bool isPlayer);
protected Entity CreateUfoBigEntity(float2 position, float speed, float2 direction, int score = 200);
protected Entity CreateUfoEntity(float2 position, float speed, float2 direction, int score = 500);
```

From Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs:
```csharp
// SetUp: _scoreEntity = CreateScoreDataSingleton(0); _collisionBufferEntity = CreateCollisionEventSingleton();
private void AddCollisionEvent(Entity entityA, Entity entityB);
private void RunSystem(); // CreateAndGetSystem<EcsCollisionHandlerSystem>() + OnUpdate
```

From Assets/Scripts/ECS/Components/Tags/RocketTag.cs:
```csharp
namespace SelStrom.Asteroids.ECS
{
    public struct RocketTag : IComponentData { }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Написать falling тесты коллизий ракеты</name>
  <files>Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs</files>
  <read_first>
    - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
    - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
  </read_first>
  <behavior>
    - RocketHitsAsteroid_BothGetDeadTag: ракета + астероид -> оба DeadTag
    - RocketHitsAsteroid_ScoreIncreased: ракета + астероид (score:100) -> Score == 100
    - RocketHitsUfo_BothDeadAndScoreIncreased: ракета + Ufo (score:500) -> оба DeadTag, Score == 500
    - RocketHitsUfoBig_BothDeadAndScoreIncreased: ракета + UfoBig (score:200) -> оба DeadTag, Score == 200
    - RocketHitsAsteroid_ReversedOrder_BothGetDeadTag: AddCollisionEvent(asteroid, rocket) -> оба DeadTag
    - RocketHitsShip_NoDeadTag: ракета + Ship -> НИ ОДИН не получает DeadTag
    - RocketHitsPlayerBullet_NoDeadTag: ракета + PlayerBullet -> НИ ОДИН не получает DeadTag
  </behavior>
  <action>
Добавить 7 тестовых методов в конец класса `CollisionHandlerTests` (перед закрывающими скобками), следуя паттерну существующих тестов (per D-08, D-09).

Все тесты используют `CreateRocketEntity(float2.zero, 15f, new float2(1f, 0f), 5f)` из fixture.

1. `RocketHitsAsteroid_BothGetDeadTag` -- создать ракету и астероид (score:100), `AddCollisionEvent(rocket, asteroid)`, `RunSystem()`, Assert DeadTag на обоих (per D-01, D-03, D-06).

2. `RocketHitsAsteroid_ScoreIncreased` -- аналогично, но Assert `scoreData.Value == 100` (per D-04, COLL-01).

3. `RocketHitsUfo_BothDeadAndScoreIncreased` -- ракета + `CreateUfoEntity(score:500)`, Assert DeadTag на обоих + Score == 500 (per COLL-02).

4. `RocketHitsUfoBig_BothDeadAndScoreIncreased` -- ракета + `CreateUfoBigEntity(score:200)`, Assert DeadTag на обоих + Score == 200 (per COLL-02).

5. `RocketHitsAsteroid_ReversedOrder_BothGetDeadTag` -- `AddCollisionEvent(asteroid, rocket)` (reversed), Assert DeadTag на обоих (per D-06, зеркальная ветка).

6. `RocketHitsShip_NoDeadTag` -- ракета + `CreateShipEntity`, `AddCollisionEvent(rocket, ship)`, Assert `IsFalse(HasComponent<DeadTag>)` на обоих (per D-05).

7. `RocketHitsPlayerBullet_NoDeadTag` -- ракета + `CreateBulletEntity(isPlayer:true)`, Assert `IsFalse(HasComponent<DeadTag>)` на обоих (per D-05).

Сообщение коммита: `test(11-01): add failing tests for rocket collision and scoring`
  </action>
  <verify>
    <automated>Все 7 новых тестов ДОЛЖНЫ упасть (Assert.IsTrue fails на DeadTag). Существующие 9 тестов проходят без изменений.</automated>
  </verify>
  <acceptance_criteria>
    - CollisionHandlerTests.cs содержит метод `RocketHitsAsteroid_BothGetDeadTag`
    - CollisionHandlerTests.cs содержит метод `RocketHitsAsteroid_ScoreIncreased`
    - CollisionHandlerTests.cs содержит метод `RocketHitsUfo_BothDeadAndScoreIncreased`
    - CollisionHandlerTests.cs содержит метод `RocketHitsUfoBig_BothDeadAndScoreIncreased`
    - CollisionHandlerTests.cs содержит метод `RocketHitsAsteroid_ReversedOrder_BothGetDeadTag`
    - CollisionHandlerTests.cs содержит метод `RocketHitsShip_NoDeadTag`
    - CollisionHandlerTests.cs содержит метод `RocketHitsPlayerBullet_NoDeadTag`
    - Каждый тест имеет атрибут [Test]
    - Тесты используют CreateRocketEntity из fixture (не создают entity вручную)
    - Негативные тесты используют Assert.IsFalse для проверки отсутствия DeadTag
  </acceptance_criteria>
  <done>7 новых тестов написаны, компилируются, ПРОВАЛИВАЮТСЯ (RED). Существующие тесты не сломаны.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Реализовать IsRocket helper и ветки Rocket+Enemy в ProcessCollision</name>
  <files>Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs</files>
  <read_first>
    - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
    - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
  </read_first>
  <behavior>
    - IsRocket возвращает true для entity с RocketTag
    - ProcessCollision: Rocket+Enemy -> MarkDead обоих + AddScore от enemy
    - ProcessCollision: зеркальная ветка Enemy+Rocket -> тот же результат
    - Rocket+Ship, Rocket+Bullet -- не матчат ни одну ветку, ничего не происходит
  </behavior>
  <action>
В файле `EcsCollisionHandlerSystem.cs` добавить:

1. Helper `IsRocket` ПОСЛЕ `IsEnemyBullet` (line ~127), перед `IsShip` (per D-03):
```csharp
private bool IsRocket(ref EntityManager em, Entity entity)
{
    return em.HasComponent<RocketTag>(entity);
}
```

2. Две зеркальные ветки в `ProcessCollision`, ПОСЛЕ PlayerBullet+Enemy (line 73), ДО EnemyBullet+Ship (line 76) (per D-01, D-02):
```csharp
// Rocket + Enemy (Asteroid/Ufo/UfoBig)
if (IsRocket(ref em, entityA) && IsEnemy(ref em, entityB))
{
    MarkDead(ref em, entityA);
    MarkDead(ref em, entityB);
    AddScore(ref em, entityB, ref scoreData);
    return;
}

if (IsRocket(ref em, entityB) && IsEnemy(ref em, entityA))
{
    MarkDead(ref em, entityB);
    MarkDead(ref em, entityA);
    AddScore(ref em, entityA, ref scoreData);
    return;
}
```

Ветки переиспользуют `IsEnemy`, `MarkDead`, `AddScore` без дублирования логики. `IsEnemy` уже покрывает AsteroidTag, UfoBigTag, UfoTag. `AddScore` читает ScoreValue с enemy entity (per D-04). Дробление астероидов обрабатывается downstream через DeadTag (per D-07). Ракета не имеет ScoreValue, очки только с enemy.

Сообщение коммита: `feat(11-01): implement rocket collision with enemies`
  </action>
  <verify>
    <automated>Все 16 тестов (9 существующих + 7 новых) проходят.</automated>
  </verify>
  <acceptance_criteria>
    - EcsCollisionHandlerSystem.cs содержит метод `IsRocket`
    - EcsCollisionHandlerSystem.cs содержит строку `IsRocket(ref em, entityA) && IsEnemy(ref em, entityB)`
    - EcsCollisionHandlerSystem.cs содержит строку `IsRocket(ref em, entityB) && IsEnemy(ref em, entityA)`
    - Ветки Rocket+Enemy расположены ПОСЛЕ PlayerBullet+Enemy и ДО EnemyBullet+Ship
    - Ветки используют MarkDead для обоих entities (ракета + враг)
    - Ветки используют AddScore с enemy entity (не с ракетой)
    - Нет дублирования кода -- используются существующие helpers IsEnemy, MarkDead, AddScore
    - `using SelStrom.Asteroids.ECS` уже есть (RocketTag в том же namespace)
  </acceptance_criteria>
  <done>Все 16 тестов проходят (GREEN). IsRocket helper и 2 зеркальные ветки Rocket+Enemy добавлены в ProcessCollision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CollisionEventData buffer -> ProcessCollision | Entity IDs из буфера коллизий -- внутренний ECS pipeline, доверенный |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | CollisionEventData | accept | Внутренний ECS-буфер, не доступен извне. Entity IDs валидируются через HasComponent перед действием |
| T-11-02 | Denial of Service | ProcessCollision | accept | Линейная сложность O(N) по числу событий коллизий. N ограничено числом entity в сцене (~50 max) |
| T-11-03 | Elevation of Privilege | AddScore | mitigate | ScoreValue читается с enemy entity, не с ракеты. Ракета не имеет ScoreValue -- невозможно инжектировать произвольные очки через ракету |
</threat_model>

<verification>
1. Все 16 тестов в CollisionHandlerTests проходят (9 существующих + 7 новых)
2. `grep -c "IsRocket" Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs` возвращает >= 3 (определение + 2 вызова)
3. `grep -c "RocketHits" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs` возвращает >= 7
</verification>

<success_criteria>
- Ракета получает DeadTag при столкновении с любым врагом (COLL-03)
- Астероид получает DeadTag при столкновении с ракетой, очки начисляются (COLL-01)
- UFO/UfoBig получает DeadTag при столкновении с ракетой, очки начисляются (COLL-02)
- Зеркальный порядок entities работает корректно
- Ракета НЕ коллидирует с кораблём и пулями (D-05)
- Код не дублирует логику, переиспользует существующие helpers
</success_criteria>

<output>
After completion, create `.planning/phases/11-collision-scoring/11-01-SUMMARY.md`
</output>
