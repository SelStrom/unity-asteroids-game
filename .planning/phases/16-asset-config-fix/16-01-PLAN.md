---
phase: 16-asset-config-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Media/configs/GameData.asset
  - Assets/Media/prefabs/rocket.prefab
autonomous: false
gap_closure: true
requirements: [CONF-01]

must_haves:
  truths:
    - "Score ракеты в GameData.asset равен 50"
    - "Rocket префаб содержит Trail child с ParticleSystem, привязанным к _trailEffect"
  artifacts:
    - path: "Assets/Media/configs/GameData.asset"
      provides: "Корректное значение Score=50 для RocketData"
      contains: "Score: 50"
    - path: "Assets/Media/prefabs/rocket.prefab"
      provides: "Trail ParticleSystem на Rocket префабе"
  key_links:
    - from: "Assets/Media/configs/GameData.asset"
      to: "EntitiesCatalog.CreateRocket"
      via: "GameData.Rocket.Score читается при создании entity"
      pattern: "Score"
    - from: "Assets/Media/prefabs/rocket.prefab"
      to: "Assets/Scripts/View/RocketVisual.cs"
      via: "_trailEffect SerializeField привязка к Trail ParticleSystem"
      pattern: "_trailEffect"
---

<objective>
Исправить два asset-gap'а из аудита milestone v1.2.0:
1. Score=0 в GameData.asset для RocketData (должно быть 50)
2. Rocket префаб не настроен -- trail ParticleSystem отсутствует до запуска Editor-скрипта

Purpose: Закрыть CONF-01 requirement -- все параметры ракеты корректно заданы в ScriptableObject
Output: GameData.asset с Score=50, Rocket префаб с настроенным trail
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-config-visual-polish/14-02-SUMMARY.md

@Assets/Media/configs/GameData.asset
@Assets/Editor/RocketPrefabSetup.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Исправить Score=0 на Score=50 в GameData.asset</name>
  <files>Assets/Media/configs/GameData.asset</files>
  <read_first>
    - Assets/Media/configs/GameData.asset
  </read_first>
  <action>
    Отредактировать Assets/Media/configs/GameData.asset -- изменить строку `Score: 0` на `Score: 50` в секции Rocket (строка 48).

    Это YAML-файл Unity, прямое редактирование безопасно для скалярных значений.

    Конкретное изменение:
    - Найти строку `    Score: 0` (после ReloadDurationSec: 5 в секции Rocket)
    - Заменить на `    Score: 50`

    Не трогать никакие другие значения. Отступ -- 4 пробела (сохранить как есть).

    Per CONF-01: "Все параметры ракеты задаются через ScriptableObject (скорость, turn rate, боезапас, перезарядка, время жизни, очки)"
  </action>
  <verify>
    <automated>grep -n "Score: 50" Assets/Media/configs/GameData.asset</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Score: 50" Assets/Media/configs/GameData.asset` возвращает ровно одну строку
    - `grep "Score: 0" Assets/Media/configs/GameData.asset` не возвращает строк (нет нуля)
    - Остальные значения Rocket секции не изменены (Speed=8, LifeTimeSec=5, TurnRateDegPerSec=180, MaxAmmo=3, ReloadDurationSec=5)
  </acceptance_criteria>
  <done>GameData.asset содержит Score=50 для RocketData</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Запустить Editor-скрипт и верифицировать trail + Score в Unity</name>
  <files>Assets/Media/prefabs/rocket.prefab</files>
  <read_first>
    - Assets/Editor/RocketPrefabSetup.cs
    - Assets/Media/prefabs/rocket.prefab
  </read_first>
  <action>
    Пользователь запускает существующий Editor-скрипт RocketPrefabSetup.cs (создан в Phase 14) через меню Tools > Setup Rocket (All). Скрипт:
    1. Создает дочерний объект Trail с ParticleSystem на rocket.prefab
    2. Настраивает ParticleSystem: World space, playOnAwake=false, startLifetime=0.4, emission=40, fade to transparent
    3. Привязывает _trailEffect SerializeField на RocketVisual к созданному ParticleSystem
    4. Устанавливает все значения конфига ракеты (включая Score=50)
  </action>
  <what-built>
    Task 1 исправил Score=50 в GameData.asset. Editor-скрипт RocketPrefabSetup.cs уже существует (создан в Phase 14) и содержит полную настройку trail ParticleSystem + привязку _trailEffect.
  </what-built>
  <how-to-verify>
    1. Открыть Unity Editor
    2. Дождаться компиляции (если требуется)
    3. Меню: Tools > Setup Rocket (All)
    4. Проверить Console -- должно быть:
       - "Rocket trail ParticleSystem created and configured successfully!" (или "updated")
       - "_trailEffect bound to Trail ParticleSystem"
       - "Rocket config values set: Speed=8, LifeTimeSec=5, TurnRateDegPerSec=180, MaxAmmo=3, ReloadDurationSec=5, Score=50"
       - "=== Rocket setup complete! ==="
    5. Открыть Assets/Media/prefabs/rocket.prefab в Inspector:
       - Должен быть дочерний объект "Trail" с ParticleSystem
       - RocketVisual._trailEffect должен ссылаться на этот ParticleSystem
    6. Проверить GameData.asset в Inspector:
       - Rocket.Score должен быть 50
    7. Запустить Play Mode и выстрелить ракетой (R) -- за ракетой должен тянуться белый инверсионный след
  </how-to-verify>
  <verify>
    <automated>grep -n "Score: 50" Assets/Media/configs/GameData.asset</automated>
  </verify>
  <acceptance_criteria>
    - В Unity Inspector: rocket.prefab имеет дочерний объект Trail с ParticleSystem
    - RocketVisual._trailEffect ссылается на Trail ParticleSystem (не None)
    - GameData.asset Inspector: Rocket.Score = 50
    - Play Mode: за ракетой тянется белый инверсионный след
  </acceptance_criteria>
  <done>Rocket префаб содержит настроенный trail ParticleSystem, Score=50 подтвержден в Inspector</done>
  <resume-signal>Ввести "approved" если trail работает и Score=50 в инспекторе, или описать проблемы</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет trust boundaries -- изменяются только локальные конфигурационные ассеты.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | GameData.asset | accept | Локальный ассет, контроль через git |
</threat_model>

<verification>
- `grep "Score: 50" Assets/Media/configs/GameData.asset` возвращает строку
- В Unity Inspector: Rocket.Score = 50
- В Unity Inspector: rocket.prefab имеет Trail child с ParticleSystem
- Play Mode: за ракетой тянется инверсионный след
</verification>

<success_criteria>
1. Score=50 в GameData.asset для RocketData (а не 0)
2. Rocket префаб имеет корректно настроенный trail ParticleSystem (после запуска Editor-скрипта)
3. _trailEffect привязан к ParticleSystem на префабе
</success_criteria>

<output>
After completion, create `.planning/phases/16-asset-config-fix/16-01-SUMMARY.md`
</output>
