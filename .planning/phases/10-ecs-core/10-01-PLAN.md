---
phase: 10-ecs-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/ECS/Components/Tags/RocketTag.cs
  - Assets/Scripts/ECS/Components/RocketTargetData.cs
  - Assets/Scripts/ECS/Components/RocketAmmoData.cs
  - Assets/Scripts/ECS/EntityFactory.cs
  - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
  - Assets/Tests/EditMode/ECS/EntityFactoryTests.cs
autonomous: true
requirements: [ROCK-04, ROCK-05]
must_haves:
  truths:
    - "RocketTag, RocketTargetData, RocketAmmoData компилируются как IComponentData struct"
    - "EntityFactory.CreateRocket создаёт entity с RocketTag + MoveData + LifeTimeData + RocketTargetData"
    - "EntityFactory.CreateShip добавляет RocketAmmoData с начальным боезапасом = MaxAmmo"
    - "Тестовый хелпер CreateRocketEntity доступен в AsteroidsEcsTestFixture"
  artifacts:
    - path: "Assets/Scripts/ECS/Components/Tags/RocketTag.cs"
      provides: "IComponentData маркер ракеты"
      contains: "public struct RocketTag : IComponentData"
    - path: "Assets/Scripts/ECS/Components/RocketTargetData.cs"
      provides: "Данные цели ракеты + turn rate"
      contains: "public struct RocketTargetData : IComponentData"
    - path: "Assets/Scripts/ECS/Components/RocketAmmoData.cs"
      provides: "Боезапас ракет на Ship entity"
      contains: "public struct RocketAmmoData : IComponentData"
    - path: "Assets/Scripts/ECS/EntityFactory.cs"
      provides: "CreateRocket + расширенный CreateShip"
      exports: ["CreateRocket", "CreateShip"]
    - path: "Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs"
      provides: "Хелпер CreateRocketEntity для тестов"
      contains: "protected Entity CreateRocketEntity"
  key_links:
    - from: "EntityFactory.CreateRocket"
      to: "RocketTag, MoveData, LifeTimeData, RocketTargetData"
      via: "AddComponentData"
      pattern: "em\\.AddComponentData.*RocketTag"
    - from: "EntityFactory.CreateShip"
      to: "RocketAmmoData"
      via: "AddComponentData"
      pattern: "em\\.AddComponentData.*RocketAmmoData"
---

<objective>
Создать ECS-компоненты ракеты и расширить EntityFactory.

Purpose: Фундамент данных для систем наведения (Plan 02) и перезарядки (Plan 03). Без компонентов и фабричных методов системы не могут создавать и обрабатывать entity ракет.
Output: 3 новых IComponentData, расширенный EntityFactory, тестовый хелпер.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ecs-core/10-CONTEXT.md
@.planning/phases/10-ecs-core/10-RESEARCH.md

@Assets/Scripts/ECS/Components/Tags/BulletTag.cs
@Assets/Scripts/ECS/Components/Tags/DeadTag.cs
@Assets/Scripts/ECS/Components/MoveData.cs
@Assets/Scripts/ECS/Components/LifeTimeData.cs
@Assets/Scripts/ECS/Components/GunData.cs
@Assets/Scripts/ECS/Components/LaserData.cs
@Assets/Scripts/ECS/EntityFactory.cs
@Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
@Assets/Tests/EditMode/ECS/EntityFactoryTests.cs

<interfaces>
<!-- Существующие паттерны компонентов -->

From Assets/Scripts/ECS/Components/Tags/BulletTag.cs:
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct BulletTag : IComponentData { }
}
```

From Assets/Scripts/ECS/Components/GunData.cs:
```csharp
public struct GunData : IComponentData
{
    public int MaxShoots;
    public float ReloadDurationSec;
    public int CurrentShoots;
    public float ReloadRemaining;
    public bool Shooting;
    public float2 Direction;
    public float2 ShootPosition;
}
```

From Assets/Scripts/ECS/Components/LaserData.cs:
```csharp
public struct LaserData : IComponentData
{
    public int MaxShoots;
    public float UpdateDurationSec;
    public int CurrentShoots;
    public float ReloadRemaining;
    public bool Shooting;
    public float2 Direction;
    public float2 ShootPosition;
}
```

From Assets/Scripts/ECS/EntityFactory.cs -- CreateBullet (паттерн для CreateRocket):
```csharp
public static Entity CreateBullet(
    EntityManager em,
    float2 position, float speed, float2 direction,
    float lifeTime, bool isPlayer)
```

From Assets/Scripts/ECS/EntityFactory.cs -- CreateShip (будет расширен):
```csharp
public static Entity CreateShip(
    EntityManager em,
    float2 position, float moveSpeed,
    float thrustAcceleration, float thrustMaxSpeed,
    int gunMaxShoots, float gunReloadSec,
    int laserMaxShoots, float laserReloadSec)
```

From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs -- CreateBulletEntity (паттерн для CreateRocketEntity):
```csharp
protected Entity CreateBulletEntity(
    float2 position, float speed, float2 direction, float lifeTime, bool isPlayer)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать ECS-компоненты ракеты (RocketTag, RocketTargetData, RocketAmmoData)</name>
  <files>
    Assets/Scripts/ECS/Components/Tags/RocketTag.cs,
    Assets/Scripts/ECS/Components/RocketTargetData.cs,
    Assets/Scripts/ECS/Components/RocketAmmoData.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/Components/Tags/BulletTag.cs,
    Assets/Scripts/ECS/Components/Tags/DeadTag.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Components/LaserData.cs
  </read_first>
  <action>
Создать 3 новых файла по существующим паттернам:

**RocketTag.cs** (по паттерну BulletTag.cs -- per D-08):
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct RocketTag : IComponentData { }
}
```

**RocketTargetData.cs** (per D-09, D-02 -- хранит Entity цели + turn rate):
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct RocketTargetData : IComponentData
    {
        public Entity Target;
        public float TurnRateDegPerSec;
    }
}
```
TurnRateDegPerSec -- поле инстанса (не константа), передаётся через EntityFactory.CreateRocket. Позволяет разный turn rate для разных ракет в будущем.

**RocketAmmoData.cs** (per D-10, D-12 -- живёт на Ship entity, аналогично GunData/LaserData):
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    public struct RocketAmmoData : IComponentData
    {
        public int MaxAmmo;
        public float ReloadDurationSec;
        public int CurrentAmmo;
        public float ReloadRemaining;
    }
}
```
Поля аналогичны LaserData (MaxShoots/MaxAmmo, CurrentShoots/CurrentAmmo, ReloadDurationSec, ReloadRemaining) но без Shooting/Direction/ShootPosition -- ракете они не нужны (запуск обрабатывается в Phase 13).
  </action>
  <verify>
    <automated>grep -r "public struct RocketTag : IComponentData" Assets/Scripts/ECS/Components/Tags/RocketTag.cs && grep -r "public struct RocketTargetData : IComponentData" Assets/Scripts/ECS/Components/RocketTargetData.cs && grep -r "public struct RocketAmmoData : IComponentData" Assets/Scripts/ECS/Components/RocketAmmoData.cs && grep "TurnRateDegPerSec" Assets/Scripts/ECS/Components/RocketTargetData.cs && grep "CurrentAmmo" Assets/Scripts/ECS/Components/RocketAmmoData.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл RocketTag.cs содержит `public struct RocketTag : IComponentData { }`
    - Файл RocketTargetData.cs содержит `public Entity Target;` и `public float TurnRateDegPerSec;`
    - Файл RocketAmmoData.cs содержит `public int MaxAmmo;`, `public int CurrentAmmo;`, `public float ReloadDurationSec;`, `public float ReloadRemaining;`
    - Все файлы в namespace `SelStrom.Asteroids.ECS`
    - Нет логики в struct (только поля)
  </acceptance_criteria>
  <done>3 IComponentData struct существуют, компилируются, следуют существующим naming conventions</done>
</task>

<task type="auto">
  <name>Task 2: Расширить EntityFactory и тестовую fixture</name>
  <files>
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    Assets/Tests/EditMode/ECS/EntityFactoryTests.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/EntityFactory.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    Assets/Tests/EditMode/ECS/EntityFactoryTests.cs
  </read_first>
  <action>
**EntityFactory.cs -- добавить CreateRocket (per D-07, D-08, D-09, ROCK-04):**

Добавить метод после CreateUfo. Параметры: EntityManager em, float2 position, float speed, float2 direction, float lifeTime, float turnRateDegPerSec. Метод создаёт entity с RocketTag, MoveData, LifeTimeData, RocketTargetData (Target = Entity.Null):

```csharp
public static Entity CreateRocket(
    EntityManager em,
    float2 position,
    float speed,
    float2 direction,
    float lifeTime,
    float turnRateDegPerSec)
{
    var entity = em.CreateEntity();
    em.AddComponentData(entity, new RocketTag());
    em.AddComponentData(entity, new MoveData
    {
        Position = position,
        Speed = speed,
        Direction = direction
    });
    em.AddComponentData(entity, new LifeTimeData
    {
        TimeRemaining = lifeTime
    });
    em.AddComponentData(entity, new RocketTargetData
    {
        Target = Entity.Null,
        TurnRateDegPerSec = turnRateDegPerSec
    });
    return entity;
}
```

**EntityFactory.cs -- расширить CreateShip (per D-10, D-12, ROCK-05):**

Добавить параметры `int rocketMaxAmmo, float rocketReloadSec` в конец сигнатуры CreateShip. Добавить `em.AddComponentData(entity, new RocketAmmoData { ... })` перед return. Начальные значения: CurrentAmmo = rocketMaxAmmo, ReloadRemaining = rocketReloadSec.

**AsteroidsEcsTestFixture.cs -- добавить хелпер CreateRocketEntity:**

Добавить после CreateUfoEntity:
```csharp
protected Entity CreateRocketEntity(
    float2 position, float speed, float2 direction, float lifeTime,
    float turnRateDegPerSec = 180f)
{
    var entity = m_Manager.CreateEntity();
    m_Manager.AddComponentData(entity, new RocketTag());
    m_Manager.AddComponentData(entity, new MoveData
    {
        Position = position,
        Speed = speed,
        Direction = direction
    });
    m_Manager.AddComponentData(entity, new LifeTimeData
    {
        TimeRemaining = lifeTime
    });
    m_Manager.AddComponentData(entity, new RocketTargetData
    {
        Target = Entity.Null,
        TurnRateDegPerSec = turnRateDegPerSec
    });
    return entity;
}
```

**AsteroidsEcsTestFixture.cs -- обновить CreateShipEntity:**

Добавить параметр `int rocketMaxAmmo = 3, float rocketReloadSec = 5f` с дефолтами. Добавить `m_Manager.AddComponentData(entity, new RocketAmmoData { MaxAmmo = rocketMaxAmmo, ReloadDurationSec = rocketReloadSec, CurrentAmmo = rocketMaxAmmo, ReloadRemaining = rocketReloadSec });`.

**EntityFactoryTests.cs -- добавить тест CreateRocket_CreatesEntityWithAllComponents:**

Тест вызывает EntityFactory.CreateRocket, проверяет наличие RocketTag, MoveData, LifeTimeData, RocketTargetData. Проверяет что RocketTargetData.Target == Entity.Null.

Также добавить тест CreateShip_HasRocketAmmoData -- проверяет наличие RocketAmmoData на ship entity и что CurrentAmmo == MaxAmmo.
  </action>
  <verify>
    <automated>grep "CreateRocket" Assets/Scripts/ECS/EntityFactory.cs && grep "RocketAmmoData" Assets/Scripts/ECS/EntityFactory.cs && grep "CreateRocketEntity" Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs && grep "CreateRocket_CreatesEntityWithAllComponents" Assets/Tests/EditMode/ECS/EntityFactoryTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - EntityFactory.cs содержит `public static Entity CreateRocket(` с 6 параметрами
    - EntityFactory.CreateShip имеет параметры `int rocketMaxAmmo, float rocketReloadSec` и вызывает `em.AddComponentData(entity, new RocketAmmoData`
    - AsteroidsEcsTestFixture.cs содержит `protected Entity CreateRocketEntity(`
    - AsteroidsEcsTestFixture.CreateShipEntity содержит `new RocketAmmoData`
    - EntityFactoryTests.cs содержит тест `CreateRocket_CreatesEntityWithAllComponents`
    - EntityFactoryTests.cs содержит тест `CreateShip_HasRocketAmmoData`
  </acceptance_criteria>
  <done>EntityFactory создаёт ракету со всеми компонентами и корабль с RocketAmmoData; тестовый хелпер готов для Plan 02 и Plan 03; тесты фабрики проверяют корректность создания entity</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет применимых trust boundary для данного плана. Все компоненты -- чистые data struct без сетевого взаимодействия, пользовательского ввода строк или хранения данных. EntityFactory -- вызывается только из внутреннего кода.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | N/A | IComponentData structs | accept | Чистые данные без логики, нет attack surface |
</threat_model>

<verification>
- grep подтверждает наличие всех 3 компонентов и 2 фабричных методов
- EntityFactoryTests проверяют создание entity с корректным набором компонентов
- Все существующие тесты проходят (CreateShip с новыми параметрами имеет дефолты в тестовом хелпере)
</verification>

<success_criteria>
1. 3 новых IComponentData struct компилируются: RocketTag, RocketTargetData, RocketAmmoData
2. EntityFactory.CreateRocket создаёт entity с RocketTag + MoveData + LifeTimeData + RocketTargetData(Target=Entity.Null)
3. EntityFactory.CreateShip добавляет RocketAmmoData с CurrentAmmo = MaxAmmo
4. CreateRocketEntity хелпер доступен в AsteroidsEcsTestFixture
5. EntityFactoryTests покрывают CreateRocket и расширенный CreateShip
</success_criteria>

<output>
After completion, create `.planning/phases/10-ecs-core/10-01-SUMMARY.md`
</output>
