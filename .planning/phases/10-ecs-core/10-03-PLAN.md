---
phase: 10-ecs-core
plan: 03
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs
  - Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs
autonomous: true
requirements: [ROCK-06, TEST-01]
must_haves:
  truths:
    - "Боезапас ракет восстанавливается по одной за период перезарядки (инкрементально)"
    - "Перезарядка не превышает MaxAmmo"
    - "Перезарядка не работает при полном боезапасе"
    - "Таймер перезарядки сбрасывается к ReloadDurationSec после каждого восстановления"
  artifacts:
    - path: "Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs"
      provides: "Система инкрементальной перезарядки боезапаса ракет"
      contains: "public partial struct EcsRocketAmmoSystem : ISystem"
    - path: "Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs"
      provides: "Юнит-тесты перезарядки боезапаса"
      contains: "public class RocketAmmoSystemTests"
  key_links:
    - from: "EcsRocketAmmoSystem"
      to: "RocketAmmoData.CurrentAmmo"
      via: "RefRW<RocketAmmoData> инкрементация"
      pattern: "CurrentAmmo.*\\+= 1"
    - from: "EcsRocketAmmoSystem"
      to: "RocketAmmoData.ReloadRemaining"
      via: "RefRW<RocketAmmoData> декремент таймера"
      pattern: "ReloadRemaining.*-=.*deltaTime"
---

<objective>
Реализовать систему перезарядки боезапаса ракет (EcsRocketAmmoSystem) через TDD.

Purpose: Инкрементальная перезарядка (одна ракета за период) по паттерну EcsLaserSystem. Боезапас живёт на Ship entity (per D-12).
Output: Рабочая система перезарядки с полным набором EditMode-тестов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ecs-core/10-CONTEXT.md
@.planning/phases/10-ecs-core/10-RESEARCH.md
@.planning/phases/10-ecs-core/10-01-SUMMARY.md

@Assets/Scripts/ECS/Components/RocketAmmoData.cs
@Assets/Scripts/ECS/Systems/EcsLaserSystem.cs
@Assets/Scripts/ECS/Systems/EcsGunSystem.cs
@Assets/Tests/EditMode/ECS/EcsLaserSystemTests.cs
@Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs

<interfaces>
<!-- Компоненты из Plan 01 -->

From Assets/Scripts/ECS/Components/RocketAmmoData.cs:
```csharp
public struct RocketAmmoData : IComponentData
{
    public int MaxAmmo;
    public float ReloadDurationSec;
    public int CurrentAmmo;
    public float ReloadRemaining;
}
```

<!-- Паттерн перезарядки из LaserSystem -->
From Assets/Scripts/ECS/Systems/EcsLaserSystem.cs:
```csharp
[UpdateAfter(typeof(EcsGunSystem))]
public partial struct EcsLaserSystem : ISystem
{
    // ...
    foreach (var (laser, entity) in SystemAPI.Query<RefRW<LaserData>>().WithEntityAccess())
    {
        if (laser.ValueRO.CurrentShoots < laser.ValueRO.MaxShoots)
        {
            laser.ValueRW.ReloadRemaining -= deltaTime;
            if (laser.ValueRO.ReloadRemaining <= 0)
            {
                laser.ValueRW.ReloadRemaining = laser.ValueRO.UpdateDurationSec;
                laser.ValueRW.CurrentShoots += 1;
            }
        }
    }
}
```

<!-- Паттерн тестирования ISystem -->
From Assets/Tests/EditMode/ECS/EcsLaserSystemTests.cs:
```csharp
[SetUp]
public override void SetUp()
{
    base.SetUp();
    _systemHandle = World.CreateSystem<EcsLaserSystem>();
    _singletonEntity = CreateLaserShootEventSingleton();
}

private void RunSystem(float deltaTime = 1.0f)
{
    World.PushTime(new TimeData(deltaTime, deltaTime));
    _systemHandle.Update(World.Unmanaged);
    World.PopTime();
}
```
</interfaces>
</context>

<tasks>

<feature>
  <name>EcsRocketAmmoSystem -- инкрементальная перезарядка боезапаса ракет</name>
  <files>Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs, Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs</files>
  <behavior>
    - CurrentAmmo < MaxAmmo: ReloadRemaining уменьшается на deltaTime (per D-13)
    - ReloadRemaining <= 0: CurrentAmmo += 1, ReloadRemaining сбрасывается к ReloadDurationSec (per D-11)
    - CurrentAmmo == MaxAmmo: ReloadRemaining не уменьшается, CurrentAmmo не меняется
    - Множественные тики: каждый период добавляется ровно 1 ракета (не все сразу)
    - Частичная перезарядка: при deltaTime < ReloadRemaining -- только уменьшение таймера
  </behavior>
  <implementation>
EcsRocketAmmoSystem -- partial struct : ISystem (Burst-совместима, только RefRW, без managed-вызовов).

Атрибут: `[UpdateAfter(typeof(EcsLaserSystem))]` -- после лазера, аналогичный паттерн.

Логика идентична EcsLaserSystem для перезарядки (CurrentShoots -> CurrentAmmo, MaxShoots -> MaxAmmo, UpdateDurationSec -> ReloadDurationSec), но БЕЗ стрельбы (Shooting/events -- не нужны, запуск ракеты обрабатывается в Phase 13).
  </implementation>
</feature>

<task type="auto" tdd="true">
  <name>Task 1: RED -- написать failing-тесты для системы перезарядки</name>
  <files>Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs</files>
  <read_first>
    Assets/Tests/EditMode/ECS/EcsLaserSystemTests.cs,
    Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs,
    Assets/Scripts/ECS/Components/RocketAmmoData.cs
  </read_first>
  <behavior>
    - Test: Reload_IncrementsCurrentAmmo_ByOne -- CurrentAmmo=1, MaxAmmo=3, ReloadRemaining=0.1, dt=1.0 -> CurrentAmmo=2, ReloadRemaining=ReloadDurationSec
    - Test: Reload_DoesNotExceedMaxAmmo -- CurrentAmmo=3, MaxAmmo=3 -> CurrentAmmo=3, ReloadRemaining не изменился
    - Test: Reload_TimerDecreases_WhenNotFull -- CurrentAmmo=1, MaxAmmo=3, ReloadRemaining=2.0, dt=0.5 -> ReloadRemaining=1.5, CurrentAmmo=1
    - Test: Reload_ResetsTimer_AfterReload -- CurrentAmmo=0, MaxAmmo=3, ReloadDurationSec=5.0, ReloadRemaining=0.1, dt=1.0 -> ReloadRemaining=5.0
    - Test: Reload_MultipleEntities_IndependentTimers -- два Ship entity с разными таймерами, оба перезаряжаются независимо
  </behavior>
  <action>
Создать файл `Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs`.

Namespace: `SelStrom.Asteroids.Tests.EditMode.ECS`.
Класс наследует `AsteroidsEcsTestFixture`.

SetUp по паттерну EcsLaserSystemTests (ISystem, НЕ SystemBase):
```csharp
private SystemHandle _systemHandle;

[SetUp]
public override void SetUp()
{
    base.SetUp();
    _systemHandle = World.CreateSystem<EcsRocketAmmoSystem>();
}

private void RunSystem(float deltaTime = 1.0f)
{
    World.PushTime(new TimeData(deltaTime, deltaTime));
    _systemHandle.Update(World.Unmanaged);
    World.PopTime();
}
```

В тестах создавать entity напрямую через m_Manager.CreateEntity() + AddComponentData(new RocketAmmoData { ... }), без ShipTag -- система работает только с RocketAmmoData, тег не нужен для query.

Написать все 5 тестов из behavior. На этом шаге создать заглушку системы:
```csharp
// Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    [UpdateAfter(typeof(EcsLaserSystem))]
    public partial struct EcsRocketAmmoSystem : ISystem
    {
        public void OnCreate(ref SystemState state)
        {
        }

        public void OnDestroy(ref SystemState state)
        {
        }

        public void OnUpdate(ref SystemState state)
        {
        }
    }
}
```
Тесты должны компилироваться и FAIL (RED).
  </action>
  <verify>
    <automated>grep -c "\[Test\]" Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - Файл RocketAmmoSystemTests.cs содержит минимум 5 методов с атрибутом [Test]
    - Файл EcsRocketAmmoSystem.cs существует как заглушка с пустым OnUpdate
    - Класс RocketAmmoSystemTests наследует AsteroidsEcsTestFixture
    - SetUp использует World.CreateSystem (паттерн ISystem)
    - Тесты создают entity с RocketAmmoData через m_Manager
  </acceptance_criteria>
  <done>5+ тестов написаны, компилируются, все FAIL (RED фаза TDD)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN + REFACTOR -- реализовать EcsRocketAmmoSystem</name>
  <files>Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs</files>
  <read_first>
    Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs,
    Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs,
    Assets/Scripts/ECS/Systems/EcsLaserSystem.cs,
    Assets/Scripts/ECS/Components/RocketAmmoData.cs
  </read_first>
  <action>
Заменить заглушку полной реализацией EcsRocketAmmoSystem.

Файл: `Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs`
Namespace: `SelStrom.Asteroids.ECS`
Struct: `public partial struct EcsRocketAmmoSystem : ISystem` (ISystem -- Burst-совместима, только RefRW без managed-вызовов)
Атрибут: `[UpdateAfter(typeof(EcsLaserSystem))]`

**Полная реализация:**
```csharp
using Unity.Entities;

namespace SelStrom.Asteroids.ECS
{
    [UpdateAfter(typeof(EcsLaserSystem))]
    public partial struct EcsRocketAmmoSystem : ISystem
    {
        public void OnCreate(ref SystemState state)
        {
        }

        public void OnDestroy(ref SystemState state)
        {
        }

        public void OnUpdate(ref SystemState state)
        {
            var deltaTime = SystemAPI.Time.DeltaTime;

            foreach (var ammo in SystemAPI.Query<RefRW<RocketAmmoData>>())
            {
                if (ammo.ValueRO.CurrentAmmo < ammo.ValueRO.MaxAmmo)
                {
                    ammo.ValueRW.ReloadRemaining -= deltaTime;
                    if (ammo.ValueRO.ReloadRemaining <= 0)
                    {
                        ammo.ValueRW.ReloadRemaining = ammo.ValueRO.ReloadDurationSec;
                        ammo.ValueRW.CurrentAmmo += 1;
                    }
                }
            }
        }
    }
}
```

Логика точно по паттерну EcsLaserSystem (per D-11, D-13):
1. Если CurrentAmmo < MaxAmmo -- уменьшить ReloadRemaining на deltaTime
2. Если ReloadRemaining <= 0 -- восстановить 1 ракету (CurrentAmmo += 1), сбросить таймер к ReloadDurationSec
3. Если CurrentAmmo == MaxAmmo -- ничего не делать

БЕЗ стрельбы (нет Shooting поля, нет event buffer) -- запуск ракеты будет в Phase 13.

Запустить тесты. Все 5+ тестов должны быть GREEN.
  </action>
  <verify>
    <automated>grep "CurrentAmmo" Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs && grep "ReloadRemaining" Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs && grep "UpdateAfter.*EcsLaserSystem" Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs</automated>
  </verify>
  <acceptance_criteria>
    - EcsRocketAmmoSystem.cs содержит `public partial struct EcsRocketAmmoSystem : ISystem`
    - Содержит `[UpdateAfter(typeof(EcsLaserSystem))]`
    - OnUpdate итерирует по `RefRW<RocketAmmoData>`
    - Условие `ammo.ValueRO.CurrentAmmo < ammo.ValueRO.MaxAmmo` присутствует
    - При ReloadRemaining <= 0: `CurrentAmmo += 1` и `ReloadRemaining = ReloadDurationSec`
    - Нет Shooting поля, нет event buffer
    - Все тесты в RocketAmmoSystemTests GREEN
  </acceptance_criteria>
  <done>EcsRocketAmmoSystem полностью реализована: инкрементальная перезарядка, ограничение MaxAmmo, сброс таймера. Все тесты GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет применимых trust boundary. Система перезарядки работает полностью локально с ECS-данными. Нет внешнего ввода, сети или хранения.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | N/A | EcsRocketAmmoSystem | accept | Чистая игровая логика, нет attack surface. Integer overflow теоретически возможен но не достижим (MaxAmmo < 100). |
</threat_model>

<verification>
- Все 5+ тестов в RocketAmmoSystemTests GREEN
- grep подтверждает паттерн инкрементальной перезарядки (CurrentAmmo += 1)
- Система -- ISystem struct (Burst-совместима)
</verification>

<success_criteria>
1. EcsRocketAmmoSystem инкрементально перезаряжает боезапас (ROCK-06, D-11, D-13)
2. Перезарядка не превышает MaxAmmo
3. Система -- ISystem (не SystemBase), Burst-совместима
4. Все тесты GREEN (TEST-01 для ammo)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ecs-core/10-03-SUMMARY.md`
</output>
