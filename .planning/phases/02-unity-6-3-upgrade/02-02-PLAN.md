---
phase: 02-unity-6-3-upgrade
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs
  - Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs
  - Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs
  - Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs
autonomous: false
requirements: [UPG-05]

must_haves:
  truths:
    - "EditMode тесты проверяют доступность ключевых типов после апгрейда (TMP, InputSystem, UGS)"
    - "EditMode тесты подтверждают отсутствие deprecated API в коде проекта"
    - "PlayMode smoke-тест загружает сцену Main и находит корабль"
    - "Игра запускается в Editor и воспроизводит геймплей 1:1"
  artifacts:
    - path: "Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs"
      provides: "Тесты отсутствия deprecated API"
      contains: "class UpgradeValidationTests"
    - path: "Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs"
      provides: "Тесты TMP-интеграции в Unity 6.3"
      contains: "class TmpIntegrationTests"
    - path: "Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs"
      provides: "Тесты доступности ключевых типов из пакетов"
      contains: "class PackageCompatibilityTests"
    - path: "Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs"
      provides: "PlayMode smoke-тест загрузки сцены"
      contains: "class GameplaySmokeTests"
  key_links:
    - from: "Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs"
      to: "TMPro namespace (Unity.TextMeshPro assembly)"
      via: "using TMPro + Type.GetType reflection"
      pattern: "typeof.*TMP_Text"
    - from: "Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs"
      to: "Assets/Scenes/Main.unity"
      via: "SceneManager.LoadScene"
      pattern: "LoadScene.*Main"
---

<objective>
Написать тест сюит для верификации Unity 6.3 апгрейда: EditMode тесты (deprecated API, TMP-интеграция, совместимость пакетов) + PlayMode smoke-тест загрузки сцены. Затем ручная верификация полного геймплея 1:1.

Purpose: Автоматизированная и ручная верификация, что апгрейд не сломал функциональность. Тестовый фундамент для регрессионной защиты в Phase 3+.
Output: 4 тестовых файла + подтверждение геймплея 1:1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unity-6-3-upgrade/02-CONTEXT.md
@.planning/phases/02-unity-6-3-upgrade/02-RESEARCH.md
@.planning/phases/02-unity-6-3-upgrade/02-01-SUMMARY.md

<interfaces>
<!-- Существующий паттерн TMP-тестов из Phase 1 -->

From Assets/Tests/EditMode/ShtlMvvm/TmpCompatibilityTests.cs (ПАТТЕРН):
```csharp
// Reflection-based тесты, проверяют доступность типов без создания Unity-объектов
// namespace: SelStrom.Asteroids.Tests.EditMode
// Паттерн: [Test] + Type.GetType("TMPro.TMP_Text, Unity.TextMeshPro") + Assert.IsNotNull
```

From Assets/Tests/EditMode/EditModeTests.asmdef:
```json
{
  "name": "EditModeTests",
  "rootNamespace": "SelStrom.Asteroids.Tests.EditMode",
  "references": ["Asteroids", "Conf", "Shtl.Mvvm", "GUID:6055be8ebefd69e48b49212b09b47b2f"],
  "includePlatforms": ["Editor"],
  "overrideReferences": true,
  "precompiledReferences": ["nunit.framework.dll"],
  "defineConstraints": ["UNITY_INCLUDE_TESTS"]
}
```

From Assets/Tests/PlayMode/PlayModeTests.asmdef:
```json
{
  "name": "PlayModeTests",
  "rootNamespace": "SelStrom.Asteroids.Tests.PlayMode",
  "references": ["Asteroids", "Conf", "Shtl.Mvvm", "GUID:6055be8ebefd69e48b49212b09b47b2f"],
  "overrideReferences": true,
  "precompiledReferences": ["nunit.framework.dll"],
  "defineConstraints": ["UNITY_INCLUDE_TESTS"]
}
```

<!-- Ключевые типы для проверки -->
- `TMPro.TMP_Text` -- базовый TMP текст (assembly: Unity.TextMeshPro)
- `TMPro.TMP_InputField` -- поле ввода (используется в ScoreVisual.cs)
- `TMPro.TextMeshProUGUI` -- TMP для uGUI Canvas
- `UnityEngine.InputSystem.InputAction` -- Input System (assembly: Unity.InputSystem)
- `Unity.Services.Authentication.IAuthenticationService` -- UGS Auth
- `Unity.Services.Leaderboards.ILeaderboardsService` -- UGS Leaderboards
- `SelStrom.Asteroids.ApplicationEntry` -- точка входа игры
- `Model.Components.MoveComponent` -- ECS-компонент движения

<!-- Файлы с using TMPro (не должны сломаться после апгрейда) -->
- Assets/Scripts/View/HudVisual.cs -- TMP_Text x5
- Assets/Scripts/View/Components/GuiText.cs -- TMP_Text x1
- Assets/Scripts/View/ScoreVisual.cs -- TMP_Text x2, TMP_InputField x1
- Assets/Scripts/View/LeaderboardEntryVisual.cs -- TMP_Text x3
- Assets/Editor/LeaderboardPrefabCreator.cs -- TMP_Text, TextMeshProUGUI
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать EditMode тесты верификации апгрейда</name>
  <files>
    Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs
    Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs
    Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs
  </files>
  <read_first>
    Assets/Tests/EditMode/ShtlMvvm/TmpCompatibilityTests.cs
    Assets/Tests/EditMode/EditModeTests.asmdef
    Assets/Scripts/View/HudVisual.cs
    Assets/Scripts/View/ScoreVisual.cs
    Assets/Scripts/View/LeaderboardEntryVisual.cs
    Assets/Scripts/View/Components/GuiText.cs
  </read_first>
  <action>
    Per D-01 и D-02: Создать три файла EditMode тестов в `Assets/Tests/EditMode/Upgrade/`. Namespace: `SelStrom.Asteroids.Tests.EditMode`. Следовать паттерну из `TmpCompatibilityTests.cs` (reflection-based, без создания Unity-объектов).

    **Файл 1: `UpgradeValidationTests.cs`** -- проверка отсутствия deprecated API:
    ```csharp
    using System.IO;
    using System.Linq;
    using NUnit.Framework;

    namespace SelStrom.Asteroids.Tests.EditMode
    {
        public class UpgradeValidationTests
        {
            // Тест 1: Ни один .cs файл в Assets/Scripts/ не содержит FindObjectsOfType
            [Test]
            public void NoDeprecatedFindObjectsOfType()
            {
                // Читать все .cs файлы из Assets/Scripts/
                // Assert: ни один не содержит строку "FindObjectsOfType" или "FindObjectOfType"
            }

            // Тест 2: Ни один .cs файл не использует SendMessage/BroadcastMessage
            [Test]
            public void NoDeprecatedSendMessage()
            {
                // Assert: ни один .cs в Assets/Scripts/ не содержит "SendMessage(" или "BroadcastMessage("
            }

            // Тест 3: Все asmdef файлы не содержат строковых ссылок "Unity.TextMeshPro"
            [Test]
            public void NoStringTmpReferencesInAsmdef()
            {
                // Читать все .asmdef файлы в Assets/
                // Assert: ни один не содержит строку "\"Unity.TextMeshPro\""
                // GUID-ссылки допустимы
            }
        }
    }
    ```

    **Файл 2: `TmpIntegrationTests.cs`** -- расширенная проверка TMP в Unity 6.3:
    ```csharp
    using System;
    using NUnit.Framework;

    namespace SelStrom.Asteroids.Tests.EditMode
    {
        public class TmpIntegrationTests
        {
            // Тест 1: TMP_InputField доступен (используется в ScoreVisual.cs)
            [Test]
            public void TmpInputFieldTypeExists()
            {
                var type = Type.GetType("TMPro.TMP_InputField, Unity.TextMeshPro");
                Assert.IsNotNull(type, "TMP_InputField должен быть доступен через Unity.TextMeshPro assembly");
            }

            // Тест 2: TextMeshProUGUI доступен (используется в LeaderboardPrefabCreator.cs)
            [Test]
            public void TextMeshProUguiTypeExists()
            {
                var type = Type.GetType("TMPro.TextMeshProUGUI, Unity.TextMeshPro");
                Assert.IsNotNull(type, "TextMeshProUGUI должен быть доступен через Unity.TextMeshPro assembly");
            }

            // Тест 3: TMP_FontAsset доступен (шрифты должны работать)
            [Test]
            public void TmpFontAssetTypeExists()
            {
                var type = Type.GetType("TMPro.TMP_FontAsset, Unity.TextMeshPro");
                Assert.IsNotNull(type, "TMP_FontAsset должен быть доступен для рендеринга шрифтов");
            }
        }
    }
    ```

    **Файл 3: `PackageCompatibilityTests.cs`** -- проверка типов из ключевых пакетов:
    ```csharp
    using System;
    using NUnit.Framework;

    namespace SelStrom.Asteroids.Tests.EditMode
    {
        public class PackageCompatibilityTests
        {
            // Тест 1: InputAction из InputSystem доступен
            [Test]
            public void InputSystemTypeExists()
            {
                var type = Type.GetType("UnityEngine.InputSystem.InputAction, Unity.InputSystem");
                Assert.IsNotNull(type, "InputAction из InputSystem пакета должен быть доступен");
            }

            // Тест 2: UGS Authentication типы доступны
            [Test]
            public void AuthenticationServiceTypeExists()
            {
                var type = Type.GetType(
                    "Unity.Services.Authentication.IAuthenticationService, Unity.Services.Authentication");
                Assert.IsNotNull(type, "IAuthenticationService из UGS Auth должен быть доступен");
            }

            // Тест 3: UGS Leaderboards типы доступны
            [Test]
            public void LeaderboardsServiceTypeExists()
            {
                var type = Type.GetType(
                    "Unity.Services.Leaderboards.ILeaderboardsService, Unity.Services.Leaderboards");
                Assert.IsNotNull(type, "ILeaderboardsService из UGS Leaderboards должен быть доступен");
            }

            // Тест 4: Основные игровые типы доступны
            [Test]
            public void CoreGameTypesExist()
            {
                Assert.IsNotNull(typeof(SelStrom.Asteroids.ApplicationEntry),
                    "ApplicationEntry должен компилироваться");
                Assert.IsNotNull(typeof(Model.Components.MoveComponent),
                    "MoveComponent должен компилироваться");
            }
        }
    }
    ```

    Создать директорию `Assets/Tests/EditMode/Upgrade/` если не существует. Не создавать .meta файлы -- Unity сгенерирует их автоматически.

    ВАЖНО (D-02): Эти тесты НЕ дублируют Phase 4 (TST-01..TST-09 -- это тесты ECS-компонентов и систем). Эти тесты проверяют инфраструктурную совместимость после апгрейда движка.
  </action>
  <verify>
    <automated>
      cd /Users/selstrom/work/projects/asteroids && \
      test -f Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs && echo "UpgradeValidation: exists" && \
      test -f Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs && echo "TmpIntegration: exists" && \
      test -f Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs && echo "PackageCompat: exists" && \
      grep -q "class UpgradeValidationTests" Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs && \
      grep -q "class TmpIntegrationTests" Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs && \
      grep -q "class PackageCompatibilityTests" Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs && \
      echo "All test classes found"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs` существует и содержит `class UpgradeValidationTests`
    - `Assets/Tests/EditMode/Upgrade/UpgradeValidationTests.cs` содержит `NoDeprecatedFindObjectsOfType` и `NoStringTmpReferencesInAsmdef`
    - `Assets/Tests/EditMode/Upgrade/TmpIntegrationTests.cs` существует и содержит `TmpInputFieldTypeExists`
    - `Assets/Tests/EditMode/Upgrade/PackageCompatibilityTests.cs` существует и содержит `InputSystemTypeExists` и `CoreGameTypesExist`
    - Все файлы в namespace `SelStrom.Asteroids.Tests.EditMode`
    - Код использует `{}` для всех if/else/for (per CLAUDE.md)
  </acceptance_criteria>
  <done>3 файла EditMode тестов созданы: UpgradeValidationTests (deprecated API), TmpIntegrationTests (TMP-типы), PackageCompatibilityTests (пакеты + игровые типы)</done>
</task>

<task type="auto">
  <name>Task 2: Написать PlayMode smoke-тест загрузки сцены</name>
  <files>
    Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs
  </files>
  <read_first>
    Assets/Tests/PlayMode/PlayModeTests.asmdef
    Assets/Scripts/Application/ApplicationEntry.cs
    Assets/Scripts/View/ShipVisual.cs
  </read_first>
  <action>
    Per D-01 и D-02: Создать PlayMode smoke-тест в `Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs`. Namespace: `SelStrom.Asteroids.Tests.PlayMode`.

    Этот тест НЕ дублирует TST-12 (полный игровой цикл старт -> игра -> конец). Smoke-тест проверяет только базовую загрузку сцены и наличие ключевых объектов после апгрейда.

    ```csharp
    using System.Collections;
    using NUnit.Framework;
    using UnityEngine;
    using UnityEngine.SceneManagement;
    using UnityEngine.TestTools;

    namespace SelStrom.Asteroids.Tests.PlayMode
    {
        public class GameplaySmokeTests
        {
            [UnitySetUp]
            public IEnumerator SetUp()
            {
                SceneManager.LoadScene("Main");
                yield return null; // ждем 1 кадр для инициализации
                yield return null; // ещё 1 кадр для Awake/Start
            }

            // Тест 1: Сцена Main загружается без ошибок
            [UnityTest]
            public IEnumerator SceneLoadsSuccessfully()
            {
                var scene = SceneManager.GetActiveScene();
                Assert.AreEqual("Main", scene.name, "Сцена Main должна быть активной");
                Assert.IsTrue(scene.isLoaded, "Сцена должна быть загружена");
                yield return null;
            }

            // Тест 2: ApplicationEntry существует в сцене
            [UnityTest]
            public IEnumerator ApplicationEntryExists()
            {
                var entry = Object.FindFirstObjectByType<ApplicationEntry>();
                Assert.IsNotNull(entry, "ApplicationEntry должен присутствовать в сцене Main");
                yield return null;
            }

            // Тест 3: Camera существует и настроена
            [UnityTest]
            public IEnumerator CameraExists()
            {
                var camera = Camera.main;
                Assert.IsNotNull(camera, "Main Camera должна существовать");
                Assert.IsTrue(camera.orthographic, "Камера должна быть ортографической (2D игра)");
                yield return null;
            }
        }
    }
    ```

    ВАЖНО: Использовать `Object.FindFirstObjectByType<T>()` (Unity 6.3 API), НЕ `FindObjectOfType<T>()`.

    Создать директорию `Assets/Tests/PlayMode/Upgrade/` если не существует.

    В PlayMode asmdef сцена Main должна быть добавлена в Build Settings для тестов. Если PlayMode тест не находит сцену -- нужно убедиться, что `Main` добавлена. Executor должен проверить это.
  </action>
  <verify>
    <automated>
      cd /Users/selstrom/work/projects/asteroids && \
      test -f Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs && echo "GameplaySmoke: exists" && \
      grep -q "class GameplaySmokeTests" Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs && \
      grep -q "FindFirstObjectByType" Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs && \
      grep -q "UnityTest" Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs && \
      echo "PlayMode test structure: OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Tests/PlayMode/Upgrade/GameplaySmokeTests.cs` существует и содержит `class GameplaySmokeTests`
    - Файл содержит `[UnityTest]` атрибуты (не `[Test]` -- PlayMode требует корутин)
    - Файл использует `FindFirstObjectByType` (не deprecated `FindObjectOfType`)
    - Файл загружает сцену "Main" через `SceneManager.LoadScene`
    - Файл в namespace `SelStrom.Asteroids.Tests.PlayMode`
    - Код использует `{}` для всех if/else (per CLAUDE.md)
  </acceptance_criteria>
  <done>PlayMode smoke-тест создан: загрузка сцены Main, проверка ApplicationEntry и Camera</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Запуск тестов и верификация геймплея 1:1</name>
  <files>none (верификация)</files>
  <action>
    Человек запускает тесты в Unity Test Runner (EditMode + PlayMode) и проверяет геймплей вручную: Title Screen, управление кораблем, астероиды, НЛО, HUD, коллизии, End Game экран, TMP-тексты.
  </action>
  <what-built>
    Тест сюит верификации апгрейда: 3 EditMode теста (UpgradeValidation, TmpIntegration, PackageCompatibility) + 1 PlayMode smoke-тест (GameplaySmoke).
  </what-built>
  <how-to-verify>
    **Часть A: Запуск тестов**
    1. Переключитесь в Unity Editor
    2. Дождитесь компиляции (если есть ошибки -- опишите их)
    3. Откройте Test Runner: Window > General > Test Runner
    4. Вкладка EditMode > Run All
    5. Проверьте: все EditMode тесты зеленые (включая ShtlMvvm/TmpCompatibilityTests из Phase 1 и новые Upgrade/ тесты)
    6. Вкладка PlayMode > Run All
    7. Проверьте: GameplaySmokeTests зеленые (сцена загружается, ApplicationEntry найден, Camera ортографическая)
    8. Если какие-то тесты красные -- перечислите failed тесты и сообщения об ошибках

    **Часть B: Ручная верификация геймплея 1:1 (UPG-05)**
    1. Нажмите Play в Editor
    2. На Title Screen нажмите Start (или Space)
    3. Проверьте корабль: W -- ускорение, A/D -- поворот, Space -- стрельба, Q -- лазер
    4. Проверьте астероиды: появляются, двигаются, дробятся при попадании на 2 меньших
    5. Проверьте НЛО: появляются периодически, стреляют в сторону корабля
    6. Проверьте HUD: координаты, скорость, угол, заряды лазера, очки -- отображаются и обновляются
    7. Проверьте коллизии: столкновение с астероидом/пулей -> конец игры
    8. Проверьте End Game экран: очки, поле ввода имени, кнопка Restart
    9. Проверьте TMP-текст: все надписи читаемы, шрифты не сломаны (нет "Missing Font")
    10. Если есть визуальные артефакты или нерабочие механики -- опишите
  </how-to-verify>
  <verify>Человек подтверждает: все тесты зеленые, геймплей 1:1</verify>
  <done>Все тесты проходят (EditMode + PlayMode), геймплей 1:1 подтвержден ручной верификацией</done>
  <resume-signal>Напишите "approved" и результаты тестов (N passed / M failed). Если есть проблемы -- опишите подробно.</resume-signal>
</task>

</tasks>

<verification>
- EditMode тесты: UpgradeValidation, TmpIntegration, PackageCompatibility -- все зеленые
- PlayMode тесты: GameplaySmoke -- все зеленые
- Ручная верификация: геймплей 1:1 подтвержден человеком
- TMP-тексты отображаются корректно
- Все игровые механики работают: движение, стрельба, астероиды, НЛО, HUD, лидерборд
</verification>

<success_criteria>
- Все EditMode тесты проходят (0 failed)
- Все PlayMode тесты проходят (0 failed)
- Геймплей 1:1 подтвержден ручной верификацией (UPG-05)
- TMP-тексты читаемы, шрифты назначены
</success_criteria>

<output>
After completion, create `.planning/phases/02-unity-6-3-upgrade/02-02-SUMMARY.md`
</output>
