---
phase: 08-ufo-asteroid-collision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
  - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
autonomous: true
requirements:
  - COL-01
  - COL-02
  - COL-03

must_haves:
  truths:
    - "UFO (UfoTag) и астероид (AsteroidTag) оба получают DeadTag при столкновении"
    - "UfoBig (UfoBigTag) и астероид (AsteroidTag) оба получают DeadTag при столкновении"
    - "Столкновение UFO+Asteroid не начисляет очки (это не убийство игроком)"
    - "Существующие коллизии (PlayerBullet+Enemy, EnemyBullet+Ship, Ship+Enemy) продолжают работать"
  artifacts:
    - path: "Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs"
      provides: "Обработка UFO+Asteroid коллизий в ProcessCollision"
      contains: "IsAsteroid"
    - path: "Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs"
      provides: "Регрессионные тесты UFO+Asteroid коллизий"
      contains: "AsteroidHitsUfo"
  key_links:
    - from: "EcsCollisionHandlerSystem.ProcessCollision"
      to: "MarkDead"
      via: "IsAsteroid + IsUfo/IsUfoBig check"
      pattern: "IsAsteroid.*IsUfo|IsUfo.*IsAsteroid"
---

<objective>
Добавить обработку коллизии UFO+Asteroid в EcsCollisionHandlerSystem и покрыть регрессионными тестами.

Purpose: В классическом Asteroids UFO и астероиды уничтожают друг друга при столкновении. Текущая реализация EcsCollisionHandlerSystem не обрабатывает эту пару.
Output: Обновленная система коллизий + 4 новых теста
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
@Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
@Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs

<interfaces>
<!-- Существующие хелперы в EcsCollisionHandlerSystem -->
From Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs:
```csharp
private bool IsPlayerBullet(ref EntityManager em, Entity entity)
private bool IsEnemyBullet(ref EntityManager em, Entity entity)
private bool IsShip(ref EntityManager em, Entity entity)
private bool IsEnemy(ref EntityManager em, Entity entity)  // AsteroidTag || UfoBigTag || UfoTag
private void MarkDead(ref EntityManager em, Entity entity)
private void AddScore(ref EntityManager em, Entity enemyEntity, ref ScoreData scoreData)
```

<!-- Тестовая фикстура -->
From Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs:
```csharp
protected Entity CreateAsteroidEntity(float2 position, float speed, float2 direction, int age, int score = 100)
protected Entity CreateUfoBigEntity(float2 position, float speed, float2 direction, int score = 200)
protected Entity CreateUfoEntity(float2 position, float speed, float2 direction, int score = 500)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Регрессионные тесты UFO+Asteroid коллизий</name>
  <files>Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs</files>
  <read_first>
    - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
    - Assets/Tests/EditMode/ECS/AsteroidsEcsTestFixture.cs
    - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
  </read_first>
  <behavior>
    - AsteroidHitsUfo_BothGetDeadTag: Asteroid + Ufo коллизия -> оба получают DeadTag
    - AsteroidHitsUfoBig_BothGetDeadTag: Asteroid + UfoBig коллизия -> оба получают DeadTag
    - AsteroidHitsUfo_ScoreNotChanged: Asteroid + Ufo коллизия -> score остается 0 (никто из игроков не убил)
    - AsteroidHitsUfo_ReversedOrder_BothGetDeadTag: Ufo + Asteroid (порядок entityA/entityB наоборот) -> оба получают DeadTag
  </behavior>
  <action>
    Добавить 4 теста в класс CollisionHandlerTests:

    1. `AsteroidHitsUfo_BothGetDeadTag`:
       - Создать asteroid через `CreateAsteroidEntity(new float2(5f, 0f), 3f, new float2(-1f, 0f), 3, score: 100)`
       - Создать ufo через `CreateUfoEntity(new float2(0f, 0f), 2f, new float2(1f, 0f), score: 500)`
       - `AddCollisionEvent(asteroid, ufo)` -> `RunSystem()`
       - Assert: оба имеют DeadTag

    2. `AsteroidHitsUfoBig_BothGetDeadTag`:
       - Создать asteroid через `CreateAsteroidEntity(new float2(5f, 0f), 3f, new float2(-1f, 0f), 3, score: 100)`
       - Создать ufoBig через `CreateUfoBigEntity(new float2(0f, 0f), 2f, new float2(1f, 0f), score: 200)`
       - `AddCollisionEvent(asteroid, ufoBig)` -> `RunSystem()`
       - Assert: оба имеют DeadTag

    3. `AsteroidHitsUfo_ScoreNotChanged`:
       - Создать asteroid + ufo аналогично тесту 1
       - `AddCollisionEvent(asteroid, ufo)` -> `RunSystem()`
       - Assert: `scoreData.Value == 0` (столкновение без участия игрока не дает очков)

    4. `AsteroidHitsUfo_ReversedOrder_BothGetDeadTag`:
       - Создать asteroid + ufo аналогично тесту 1
       - `AddCollisionEvent(ufo, asteroid)` -> `RunSystem()` (обратный порядок entityA/B)
       - Assert: оба имеют DeadTag

    Все тесты ДОЛЖНЫ УПАСТЬ (RED) до реализации в Task 2.
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "AsteroidHitsUfo\|AsteroidHitsUfoBig" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "AsteroidHitsUfo_BothGetDeadTag" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs возвращает совпадение
    - grep "AsteroidHitsUfoBig_BothGetDeadTag" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs возвращает совпадение
    - grep "AsteroidHitsUfo_ScoreNotChanged" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs возвращает совпадение
    - grep "AsteroidHitsUfo_ReversedOrder_BothGetDeadTag" Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs возвращает совпадение
    - Каждый тест метод содержит Assert.IsTrue или Assert.AreEqual
  </acceptance_criteria>
  <done>4 новых теста добавлены в CollisionHandlerTests, покрывают Asteroid+Ufo, Asteroid+UfoBig, отсутствие очков, обратный порядок entities</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализация UFO+Asteroid коллизии в ProcessCollision</name>
  <files>Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs</files>
  <read_first>
    - Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs
    - Assets/Tests/EditMode/ECS/CollisionHandlerTests.cs
  </read_first>
  <behavior>
    - Asteroid + Ufo -> оба MarkDead, без начисления очков
    - Asteroid + UfoBig -> оба MarkDead, без начисления очков
    - Обратный порядок (Ufo + Asteroid) тоже обрабатывается
    - Существующие коллизии (PlayerBullet+Enemy, EnemyBullet+Ship, Ship+Enemy) не сломаны
  </behavior>
  <action>
    1. Добавить два приватных хелпера в EcsCollisionHandlerSystem:

    ```csharp
    private bool IsAsteroid(ref EntityManager em, Entity entity)
    {
        return em.HasComponent<AsteroidTag>(entity);
    }

    private bool IsUfoAny(ref EntityManager em, Entity entity)
    {
        return em.HasComponent<UfoBigTag>(entity)
               || em.HasComponent<UfoTag>(entity);
    }
    ```

    2. Добавить в конец метода ProcessCollision (ПОСЛЕ всех существующих проверок, ПЕРЕД закрывающей скобкой) два новых блока:

    ```csharp
    // Asteroid + UFO (любой тип)
    if (IsAsteroid(ref em, entityA) && IsUfoAny(ref em, entityB))
    {
        MarkDead(ref em, entityA);
        MarkDead(ref em, entityB);
        return;
    }

    if (IsAsteroid(ref em, entityB) && IsUfoAny(ref em, entityA))
    {
        MarkDead(ref em, entityB);
        MarkDead(ref em, entityA);
        return;
    }
    ```

    Очки НЕ начисляются -- это столкновение без участия игрока.

    3. Убедиться, что новый код размещен ПОСЛЕ проверок Ship+Enemy, чтобы не перехватить PlayerBullet+Enemy (PlayerBullet не имеет AsteroidTag, поэтому конфликта нет, но порядок важен для читаемости).
  </action>
  <verify>
    <automated>cd /Users/selstrom/work/projects/asteroids && grep -c "IsAsteroid\|IsUfoAny" Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "IsAsteroid" Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs возвращает совпадение
    - grep "IsUfoAny" Assets/Scripts/ECS/Systems/EcsCollisionHandlerSystem.cs возвращает совпадение
    - grep "MarkDead.*entityA" ... содержит вызов для asteroid+ufo пары
    - Файл содержит комментарий "Asteroid + UFO"
    - Все существующие тесты + 4 новых проходят (через Unity Editor test runner)
  </acceptance_criteria>
  <done>ProcessCollision обрабатывает пары Asteroid+UfoTag и Asteroid+UfoBigTag, оба получают DeadTag, очки не начисляются, все тесты зеленые</done>
</task>

</tasks>

<verification>
1. Все 4 новых теста проходят зеленым (AsteroidHitsUfo_BothGetDeadTag, AsteroidHitsUfoBig_BothGetDeadTag, AsteroidHitsUfo_ScoreNotChanged, AsteroidHitsUfo_ReversedOrder_BothGetDeadTag)
2. Все существующие 5 тестов продолжают проходить (PlayerBulletHitsAsteroid, PlayerBulletHitsUfoBig, EnemyBulletHitsShip, ShipHitsAsteroid, NoCollisionEvents)
3. grep "IsAsteroid" и "IsUfoAny" в EcsCollisionHandlerSystem.cs находят определения хелперов
</verification>

<success_criteria>
- EcsCollisionHandlerSystem.ProcessCollision обрабатывает Asteroid+Ufo и Asteroid+UfoBig пары
- Оба entity получают DeadTag, очки не начисляются
- 9 тестов в CollisionHandlerTests проходят (5 существующих + 4 новых)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ufo-asteroid-collision/08-01-SUMMARY.md`
</output>
