---
phase: 13-input-game-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - Assets/Scripts/Application/Game.cs
autonomous: true
requirements:
  - ROCK-01

must_haves:
  truths:
    - "Нажатие R во время игры устанавливает Shooting=true на ship entity с корректными Position/Direction"
    - "Ракета не запускается при CurrentAmmo <= 0 (guard в Game.OnRocket)"
    - "OnRocket подписан в Start и отписан в Stop"
    - "RocketShootEvent буфер очищается при рестарте в ClearEcsEventBuffers"
  artifacts:
    - path: "Assets/Scripts/Application/Game.cs"
      provides: "OnRocket handler, Start/Stop subscriptions, ClearEcsEventBuffers"
      contains: "OnRocket"
  key_links:
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/ECS/Components/RocketAmmoData.cs"
      via: "GetComponentData<RocketAmmoData> + SetComponentData"
      pattern: "GetComponentData<RocketAmmoData>"
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/Input/PlayerInput.cs"
      via: "OnRocketAction += OnRocket"
      pattern: "OnRocketAction.*OnRocket"
    - from: "Assets/Scripts/Application/Game.cs"
      to: "Assets/Scripts/ECS/Components/RocketShootEvent.cs"
      via: "ClearEcsEventBuffers query + buffer.Clear()"
      pattern: "RocketShootEvent"
---

<objective>
Подключить обработчик OnRocket в Game.cs -- замкнуть цепочку Input -> ECS -> Visual.

Purpose: Финальная интеграция: при нажатии R игрок запускает ракету, при рестарте буферы очищаются. После этого плана полный пайплайн работает end-to-end.
Output: Рабочий Game.OnRocket(), подписки Start/Stop, очистка буфера при рестарте.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-input-game-integration/13-CONTEXT.md
@.planning/phases/13-input-game-integration/13-RESEARCH.md
@.planning/phases/13-input-game-integration/13-01-SUMMARY.md

<interfaces>
<!-- Расширенный RocketAmmoData из Plan 01 -->
From Assets/Scripts/ECS/Components/RocketAmmoData.cs (после Plan 01):
```csharp
public struct RocketAmmoData : IComponentData
{
    public int MaxAmmo;
    public float ReloadDurationSec;
    public int CurrentAmmo;
    public float ReloadRemaining;
    public bool Shooting;
    public float2 Direction;
    public float2 ShootPosition;
}
```

<!-- Паттерн OnAttack/OnLaser из Game.cs -->
From Assets/Scripts/Application/Game.cs:
```csharp
private void OnAttack()
{
    if (TryGetShipEntity(out var entity))
    {
        var gunData = _entityManager.GetComponentData<ECS.GunData>(entity);
        var rotateData = _entityManager.GetComponentData<RotateData>(entity);
        var moveData = _entityManager.GetComponentData<MoveData>(entity);
        gunData.Shooting = true;
        gunData.Direction = rotateData.Rotation;
        gunData.ShootPosition = moveData.Position;
        _entityManager.SetComponentData(entity, gunData);
    }
}
```

<!-- Паттерн ClearEcsEventBuffers -->
From Assets/Scripts/Application/Game.cs:
```csharp
private void ClearEcsEventBuffers()
{
    var gunQuery = _entityManager.CreateEntityQuery(typeof(GunShootEvent));
    if (gunQuery.CalculateEntityCount() > 0)
    {
        var entities = gunQuery.ToEntityArray(Unity.Collections.Allocator.Temp);
        for (int i = 0; i < entities.Length; i++)
        {
            _entityManager.GetBuffer<GunShootEvent>(entities[i]).Clear();
        }
        entities.Dispose();
    }
    // ... laser, collision аналогично
}
```

<!-- Start/Stop подписки -->
From Assets/Scripts/Application/Game.cs:
```csharp
public void Start()
{
    // ...
    _playerInput.OnAttackAction += OnAttack;
    _playerInput.OnLaserAction += OnLaser;
    // ...
}
private void Stop()
{
    // ...
    _playerInput.OnAttackAction -= OnAttack;
    _playerInput.OnLaserAction -= OnLaser;
    // ...
}
```

From Assets/Scripts/ECS/Components/RocketShootEvent.cs:
```csharp
public struct RocketShootEvent : IBufferElementData
{
    public Entity ShooterEntity;
    public float2 Position;
    public float2 Direction;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Game.OnRocket handler + Start/Stop подписки + ClearEcsEventBuffers (D-06, D-09, D-10, D-12)</name>
  <files>Assets/Scripts/Application/Game.cs</files>
  <read_first>
    Assets/Scripts/Application/Game.cs,
    Assets/Scripts/ECS/Components/RocketAmmoData.cs,
    Assets/Scripts/ECS/Components/RocketShootEvent.cs,
    Assets/Scripts/Input/PlayerInput.cs
  </read_first>
  <action>
В `Assets/Scripts/Application/Game.cs` внести три изменения:

**1. Добавить метод `OnRocket()`** после метода `OnLaser()` (строка ~275):
```csharp
private void OnRocket()
{
    if (TryGetShipEntity(out var entity))
    {
        var rocketAmmo = _entityManager.GetComponentData<RocketAmmoData>(entity);
        if (rocketAmmo.CurrentAmmo <= 0)
        {
            return;
        }

        var rotateData = _entityManager.GetComponentData<RotateData>(entity);
        var moveData = _entityManager.GetComponentData<MoveData>(entity);

        rocketAmmo.Shooting = true;
        rocketAmmo.Direction = rotateData.Rotation;
        rocketAmmo.ShootPosition = moveData.Position;
        _entityManager.SetComponentData(entity, rocketAmmo);
    }
}
```
Guard `CurrentAmmo <= 0` добавлен (Claude's Discretion из CONTEXT.md) -- ракеты перезаряжаются инкрементально и могут быть на нуле долго, guard предотвращает лишний SetComponentData при зажатой R.

**2. Подписки в Start/Stop:**
- В методе `Start()` после строки `_playerInput.OnLaserAction += OnLaser;` добавить:
  `_playerInput.OnRocketAction += OnRocket;`
- В методе `Stop()` после строки `_playerInput.OnLaserAction -= OnLaser;` добавить:
  `_playerInput.OnRocketAction -= OnRocket;`

**3. Очистка RocketShootEvent в ClearEcsEventBuffers:**
После блока очистки `LaserShootEvent` (строки ~115-125) и перед блоком `CollisionEventData`, добавить аналогичный блок:
```csharp
var rocketQuery = _entityManager.CreateEntityQuery(typeof(RocketShootEvent));
if (rocketQuery.CalculateEntityCount() > 0)
{
    var entities = rocketQuery.ToEntityArray(Unity.Collections.Allocator.Temp);
    for (int i = 0; i < entities.Length; i++)
    {
        _entityManager.GetBuffer<RocketShootEvent>(entities[i]).Clear();
    }

    entities.Dispose();
}
```
  </action>
  <verify>
    <automated>grep -c "OnRocket" Assets/Scripts/Application/Game.cs && grep -c "RocketShootEvent" Assets/Scripts/Application/Game.cs && grep -c "OnRocketAction" Assets/Scripts/Application/Game.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Game.cs` содержит `private void OnRocket()`
    - `Game.cs` содержит `GetComponentData<RocketAmmoData>(entity)` внутри OnRocket
    - `Game.cs` содержит `if (rocketAmmo.CurrentAmmo <= 0)` -- guard на пустой боезапас
    - `Game.cs` содержит `rocketAmmo.Shooting = true;`
    - `Game.cs` содержит `_playerInput.OnRocketAction += OnRocket;` в Start()
    - `Game.cs` содержит `_playerInput.OnRocketAction -= OnRocket;` в Stop()
    - `Game.cs` содержит `CreateEntityQuery(typeof(RocketShootEvent))` в ClearEcsEventBuffers
    - `Game.cs` содержит `GetBuffer<RocketShootEvent>` с `.Clear()` вызовом
  </acceptance_criteria>
  <done>Game.OnRocket() читает RocketAmmoData, устанавливает Shooting=true с Position/Direction, подписка в Start/отписка в Stop, буфер RocketShootEvent очищается при рестарте</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PlayerInput -> Game | Event от Input System передаётся в Game handler |
| Game -> ECS | Game модифицирует компоненты через EntityManager |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Spoofing | Game.OnRocket | accept | Локальный input, нет сетевого вектора атаки |
| T-13-04 | Tampering | RocketAmmoData via SetComponentData | accept | Guard CurrentAmmo<=0 предотвращает стрельбу без боезапаса, но это game logic а не security |
</threat_model>

<verification>
- `Game.cs` содержит OnRocket handler с guard на CurrentAmmo
- Start() подписывается на OnRocketAction
- Stop() отписывается от OnRocketAction
- ClearEcsEventBuffers() очищает буфер RocketShootEvent
- Полный пайплайн: R -> PlayerInput.OnRocketAction -> Game.OnRocket -> RocketAmmoData.Shooting=true -> EcsRocketAmmoSystem -> RocketShootEvent -> ShootEventProcessorSystem -> CreateRocket
</verification>

<success_criteria>
Нажатие R во время игры запускает ракету из позиции корабля, ракета не запускается при пустом боезапасе, рестарт корректно очищает буферы ракетных событий
</success_criteria>

<output>
After completion, create `.planning/phases/13-input-game-integration/13-02-SUMMARY.md`
</output>
