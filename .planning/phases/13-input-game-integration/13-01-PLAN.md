---
phase: 13-input-game-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Input/player_actions.inputactions
  - Assets/Scripts/Input/Generated/PlayerActions.cs
  - Assets/Scripts/Input/PlayerInput.cs
  - Assets/Scripts/ECS/Components/RocketAmmoData.cs
  - Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs
autonomous: true
requirements:
  - ROCK-01

must_haves:
  truths:
    - "Input action Rocket с биндингом Keyboard/r существует в player_actions.inputactions"
    - "PlayerInput генерирует событие OnRocketAction при нажатии R"
    - "RocketAmmoData содержит поля Shooting, Direction, ShootPosition"
    - "EcsRocketAmmoSystem генерирует RocketShootEvent при Shooting=true и CurrentAmmo>0"
    - "EcsRocketAmmoSystem безусловно сбрасывает Shooting=false после обработки"
  artifacts:
    - path: "Assets/Input/player_actions.inputactions"
      provides: "Rocket action definition"
      contains: "Rocket"
    - path: "Assets/Scripts/Input/PlayerInput.cs"
      provides: "OnRocketAction event и обработчик"
      contains: "OnRocketAction"
    - path: "Assets/Scripts/ECS/Components/RocketAmmoData.cs"
      provides: "Shooting/Direction/ShootPosition поля"
      contains: "Shooting"
    - path: "Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs"
      provides: "Логика стрельбы и генерации RocketShootEvent"
      contains: "RocketShootEvent"
  key_links:
    - from: "Assets/Scripts/Input/PlayerInput.cs"
      to: "Assets/Input/player_actions.inputactions"
      via: "_playerControls.Rocket.performed"
      pattern: "_playerControls\\.Rocket\\.performed"
    - from: "Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs"
      to: "Assets/Scripts/ECS/Components/RocketShootEvent.cs"
      via: "rocketEvents.Add(new RocketShootEvent"
      pattern: "rocketEvents\\.Add\\(new RocketShootEvent"
---

<objective>
Подключить кнопку R к Input System и подготовить ECS-пайплайн стрельбы ракетами.

Purpose: Создать инфраструктурный слой для запуска ракет -- от InputAction до генерации RocketShootEvent в ECS. Это фундамент для Game.cs интеграции в Plan 02.
Output: Рабочий Input action Rocket, расширенный RocketAmmoData, EcsRocketAmmoSystem с логикой стрельбы.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-input-game-integration/13-CONTEXT.md
@.planning/phases/13-input-game-integration/13-RESEARCH.md

<interfaces>
<!-- Паттерн GunData -- эталон для расширения RocketAmmoData -->
From Assets/Scripts/ECS/Components/GunData.cs:
```csharp
public struct GunData : IComponentData
{
    public int MaxShoots;
    public float ReloadDurationSec;
    public int CurrentShoots;
    public float ReloadRemaining;
    public bool Shooting;
    public float2 Direction;
    public float2 ShootPosition;
}
```

From Assets/Scripts/ECS/Components/RocketShootEvent.cs:
```csharp
public struct RocketShootEvent : IBufferElementData
{
    public Entity ShooterEntity;
    public float2 Position;
    public float2 Direction;
}
```

<!-- Паттерн EcsGunSystem -- эталон для доработки EcsRocketAmmoSystem -->
From Assets/Scripts/ECS/Systems/EcsGunSystem.cs:
```csharp
public void OnCreate(ref SystemState state)
{
    state.RequireForUpdate<GunShootEvent>();
}
public void OnUpdate(ref SystemState state)
{
    var deltaTime = SystemAPI.Time.DeltaTime;
    var gunEvents = SystemAPI.GetSingletonBuffer<GunShootEvent>();
    foreach (var (gun, entity) in SystemAPI.Query<RefRW<GunData>>().WithEntityAccess())
    {
        // reload logic...
        if (gun.ValueRO.Shooting && gun.ValueRO.CurrentShoots > 0)
        {
            gun.ValueRW.CurrentShoots--;
            gunEvents.Add(new GunShootEvent { ... });
        }
        gun.ValueRW.Shooting = false;
    }
}
```

From Assets/Scripts/Input/PlayerInput.cs:
```csharp
public event Action OnLaserAction;
// В конструкторе:
_playerControls.Laser.performed += OnLaser;
// Обработчик:
[PublicAPI]
private void OnLaser(InputAction.CallbackContext _)
{
    OnLaserAction?.Invoke();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Input action Rocket + PlayerInput event (D-01, D-02, D-03, D-04)</name>
  <files>
    Assets/Input/player_actions.inputactions,
    Assets/Scripts/Input/Generated/PlayerActions.cs,
    Assets/Scripts/Input/PlayerInput.cs
  </files>
  <read_first>
    Assets/Input/player_actions.inputactions,
    Assets/Scripts/Input/PlayerInput.cs,
    Assets/Scripts/Input/Generated/PlayerActions.cs,
    Assets/Scripts/ECS/Components/GunData.cs
  </read_first>
  <action>
1. В `Assets/Input/player_actions.inputactions` добавить новый action `Rocket` в массив `actions` (после `Laser`, перед `Back`):
```json
{
    "name": "Rocket",
    "type": "Button",
    "id": "a7e3f1b2-4c5d-6e7f-8a9b-0c1d2e3f4a5b",
    "expectedControlType": "",
    "processors": "",
    "interactions": "",
    "initialStateCheck": false
}
```
И добавить биндинг в массив `bindings` (после биндинга Laser `<Keyboard>/q`, перед биндингом Back `<Keyboard>/escape`):
```json
{
    "name": "",
    "id": "b8f4g2c3-5d6e-7f8a-9b0c-1d2e3f4a5b6c",
    "path": "<Keyboard>/r",
    "interactions": "",
    "processors": "",
    "groups": "",
    "action": "Rocket",
    "isComposite": false,
    "isPartOfComposite": false
}
```

2. Перегенерировать `Assets/Scripts/Input/Generated/PlayerActions.cs`. Это автогенерируемый файл Unity Input System. Добавить свойство `Rocket` в класс `PlayerControlsActions` по паттерну существующих свойств (`Attack`, `Laser` и т.д.). Конкретно: найти паттерн определения свойства `Laser` (поле `private InputAction m_PlayerControls_Laser` + свойство `public InputAction Laser`) и добавить аналогичную пару для `Rocket`. Также добавить `m_PlayerControls_Rocket = ...FindAction("Rocket", ...)` в конструктор `PlayerControlsActions`, и `m_Wrapper.m_PlayerControls_Rocket` в Get/Enable/Disable по паттерну Laser.

3. В `Assets/Scripts/Input/PlayerInput.cs`:
   - Добавить `public event Action OnRocketAction;` после `public event Action OnLaserAction;`
   - В конструкторе добавить `_playerControls.Rocket.performed += OnRocket;` после строки `_playerControls.Laser.performed += OnLaser;`
   - Добавить обработчик по паттерну OnLaser:
```csharp
[PublicAPI]
private void OnRocket(InputAction.CallbackContext _)
{
    OnRocketAction?.Invoke();
}
```
  </action>
  <verify>
    <automated>grep -c "Rocket" Assets/Input/player_actions.inputactions && grep -c "OnRocketAction" Assets/Scripts/Input/PlayerInput.cs && grep -c "Rocket" Assets/Scripts/Input/Generated/PlayerActions.cs</automated>
  </verify>
  <acceptance_criteria>
    - `player_actions.inputactions` содержит `"name": "Rocket"` в actions и `"path": "<Keyboard>/r"` в bindings
    - `PlayerInput.cs` содержит `public event Action OnRocketAction;`
    - `PlayerInput.cs` содержит `_playerControls.Rocket.performed += OnRocket;`
    - `PlayerInput.cs` содержит `private void OnRocket(InputAction.CallbackContext _)`
    - `PlayerActions.cs` содержит `public InputAction Rocket`
  </acceptance_criteria>
  <done>Action Rocket с биндингом R определен в InputActions, PlayerActions.cs перегенерирован, PlayerInput пробрасывает OnRocketAction event</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RocketAmmoData + EcsRocketAmmoSystem shooting logic (D-05, D-07)</name>
  <files>
    Assets/Scripts/ECS/Components/RocketAmmoData.cs,
    Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs,
    Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs
  </files>
  <read_first>
    Assets/Scripts/ECS/Components/RocketAmmoData.cs,
    Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs,
    Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs,
    Assets/Scripts/ECS/Components/GunData.cs,
    Assets/Scripts/ECS/Systems/EcsGunSystem.cs,
    Assets/Scripts/ECS/Components/RocketShootEvent.cs
  </read_first>
  <behavior>
    - Test: Shooting=true + CurrentAmmo>0 -> RocketShootEvent добавлен в буфер, CurrentAmmo уменьшен на 1
    - Test: Shooting=true + CurrentAmmo==0 -> нет RocketShootEvent, CurrentAmmo остается 0
    - Test: Shooting flag сбрасывается в false безусловно (даже при CurrentAmmo==0)
    - Test: RocketShootEvent содержит корректные ShooterEntity, Position, Direction из RocketAmmoData
    - Test: Перезарядка продолжает работать корректно после добавления shooting логики
  </behavior>
  <action>
1. Расширить `Assets/Scripts/ECS/Components/RocketAmmoData.cs` -- добавить три поля после `ReloadRemaining`:
```csharp
using Unity.Entities;
using Unity.Mathematics;

namespace SelStrom.Asteroids.ECS
{
    public struct RocketAmmoData : IComponentData
    {
        public int MaxAmmo;
        public float ReloadDurationSec;
        public int CurrentAmmo;
        public float ReloadRemaining;
        public bool Shooting;
        public float2 Direction;
        public float2 ShootPosition;
    }
}
```

2. Обновить `Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs`:
   - В `OnCreate`: добавить `state.RequireForUpdate<RocketShootEvent>();` (по паттерну EcsGunSystem)
   - В `OnUpdate`: заменить `SystemAPI.Query<RefRW<RocketAmmoData>>()` на `SystemAPI.Query<RefRW<RocketAmmoData>>().WithEntityAccess()` для получения entity
   - Добавить `var rocketEvents = SystemAPI.GetSingletonBuffer<RocketShootEvent>();` в начало OnUpdate
   - После блока перезарядки добавить блок стрельбы:
```csharp
if (ammo.ValueRO.Shooting && ammo.ValueRO.CurrentAmmo > 0)
{
    ammo.ValueRW.CurrentAmmo--;
    rocketEvents.Add(new RocketShootEvent
    {
        ShooterEntity = entity,
        Position = ammo.ValueRO.ShootPosition,
        Direction = ammo.ValueRO.Direction
    });
}

ammo.ValueRW.Shooting = false;
```
   ВАЖНО: `ammo.ValueRW.Shooting = false;` ВЫНЕСЕН за пределы if-блока -- сбрасывается безусловно (Pitfall 5 из RESEARCH.md)

3. В `Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs` добавить тесты стрельбы. Тестам нужен singleton buffer RocketShootEvent -- создать singleton entity с буфером в SetUp или в каждом тесте:
```csharp
private Entity CreateRocketEventSingleton()
{
    var singletonEntity = m_Manager.CreateEntity();
    m_Manager.AddBuffer<RocketShootEvent>(singletonEntity);
    return singletonEntity;
}
```

Тесты:
- `Shoot_WithAmmo_CreatesRocketShootEvent`: entity с `Shooting=true, CurrentAmmo=2, ShootPosition=(1,2), Direction=(0,1)` -> после RunSystem -> буфер содержит 1 event с корректными Position/Direction/ShooterEntity, CurrentAmmo==1
- `Shoot_WithoutAmmo_NoEvent`: entity с `Shooting=true, CurrentAmmo=0` -> после RunSystem -> буфер пуст, CurrentAmmo==0
- `Shoot_ResetsShootingFlag_Unconditionally`: entity с `Shooting=true, CurrentAmmo=0` -> после RunSystem -> `Shooting==false`
- `Shoot_WithAmmo_ResetsShootingFlag`: entity с `Shooting=true, CurrentAmmo=2` -> после RunSystem -> `Shooting==false`
- `Reload_StillWorks_WithShootingFields`: entity с `CurrentAmmo=1, MaxAmmo=3, ReloadRemaining=0.1f, Shooting=false` -> после RunSystem -> `CurrentAmmo==2` (регрессия)
  </action>
  <verify>
    <automated>grep -c "RocketShootEvent" Assets/Scripts/ECS/Systems/EcsRocketAmmoSystem.cs && grep -c "Shoot_" Assets/Tests/EditMode/ECS/RocketAmmoSystemTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - `RocketAmmoData.cs` содержит `public bool Shooting;` и `public float2 Direction;` и `public float2 ShootPosition;`
    - `RocketAmmoData.cs` содержит `using Unity.Mathematics;`
    - `EcsRocketAmmoSystem.cs` содержит `state.RequireForUpdate<RocketShootEvent>();` в OnCreate
    - `EcsRocketAmmoSystem.cs` содержит `.WithEntityAccess()` в foreach query
    - `EcsRocketAmmoSystem.cs` содержит `rocketEvents.Add(new RocketShootEvent` внутри if блока
    - `EcsRocketAmmoSystem.cs` содержит `ammo.ValueRW.Shooting = false;` ВНЕ if блока (безусловный сброс)
    - `RocketAmmoSystemTests.cs` содержит методы `Shoot_WithAmmo_CreatesRocketShootEvent`, `Shoot_WithoutAmmo_NoEvent`, `Shoot_ResetsShootingFlag_Unconditionally`
  </acceptance_criteria>
  <done>RocketAmmoData расширен полями стрельбы, EcsRocketAmmoSystem генерирует RocketShootEvent при Shooting+ammo>0, 5+ тестов стрельбы проходят</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input -> ECS | Пользовательский ввод (кнопка R) устанавливает данные в ECS через Game.cs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | RocketAmmoData.Shooting | accept | Локальная игра, нет сетевого взаимодействия. Игрок может модифицировать только свой процесс |
| T-13-02 | Denial of Service | EcsRocketAmmoSystem | accept | Shooting flag сбрасывается безусловно каждый кадр -- невозможно "застрять" в стрельбе |
</threat_model>

<verification>
- `player_actions.inputactions` содержит action "Rocket" с биндингом "<Keyboard>/r"
- `PlayerInput.cs` содержит event OnRocketAction и подписку на Rocket.performed
- `RocketAmmoData` содержит Shooting, Direction, ShootPosition
- `EcsRocketAmmoSystem` генерирует RocketShootEvent и безусловно сбрасывает Shooting
- Все существующие тесты перезарядки проходят (регрессия)
- Новые тесты стрельбы проходят
</verification>

<success_criteria>
Input action Rocket зарегистрирован в Unity Input System, PlayerInput пробрасывает OnRocketAction, EcsRocketAmmoSystem обрабатывает Shooting flag и генерирует RocketShootEvent с декрементом боезапаса, все тесты зеленые
</success_criteria>

<output>
After completion, create `.planning/phases/13-input-game-integration/13-01-SUMMARY.md`
</output>
