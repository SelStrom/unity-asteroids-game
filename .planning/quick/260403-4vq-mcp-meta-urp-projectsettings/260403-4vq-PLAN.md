---
phase: quick
plan: 260403-4vq
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp.json
  - .claude/skills/**
  - Assets/Scripts/Bridge/*.meta
  - Assets/Scripts/ECS/**/*.meta
  - Assets/Tests/EditMode/ECS/*.meta
  - Assets/Tests/PlayMode/GameplayCycleTests.cs.meta
  - Assets/DefaultVolumeProfile.asset
  - Assets/DefaultVolumeProfile.asset.meta
  - Assets/UniversalRenderPipelineGlobalSettings.asset
  - Assets/UniversalRenderPipelineGlobalSettings.asset.meta
  - Assets/Media/materials/Particle-URP.mat
  - Packages/packages-lock.json
  - ProjectSettings/EditorSettings.asset
  - ProjectSettings/EntitiesClientSettings.asset
  - ProjectSettings/PackageManagerSettings.asset
  - ProjectSettings/SceneTemplateSettings.json
  - ProjectSettings/ShaderGraphSettings.asset
  - ProjectSettings/URPProjectSettings.asset
autonomous: true
requirements: []
must_haves:
  truths:
    - "Все .meta файлы ECS/Bridge/Tests отслеживаются в git"
    - "MCP конфигурация и скиллы закоммичены"
    - "URP ассеты и ProjectSettings закоммичены"
    - ".claude/worktrees/ НЕ закоммичены"
    - "Assets/TestWithTempAssets НЕ закоммичены"
  artifacts: []
  key_links: []
---

<objective>
Закоммитить все незакоммиченные рабочие файлы проекта в 3 логических коммита.

Purpose: Привести git в чистое состояние, сгруппировав изменения по смыслу.
Output: 3 коммита на ветке feature/dots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Коммит infra — MCP конфиг и скиллы Unity-MCP</name>
  <files>.mcp.json, .claude/skills/</files>
  <action>
1. Сначала УДАЛИТЬ из staging area временные worktrees (они НЕ должны попасть в коммит):
   ```
   git reset HEAD .claude/worktrees/
   ```
2. Добавить .mcp.json и все скиллы:
   ```
   git add .mcp.json .claude/skills/
   ```
3. Закоммитить:
   ```
   git commit -m "infra: add Unity-MCP config and skills

   Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
   ```

ВАЖНО: НЕ добавлять .claude/worktrees/ — это временные рабочие деревья агентов.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "infra: add Unity-MCP" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>Коммит содержит .mcp.json и .claude/skills/*, не содержит worktrees</done>
</task>

<task type="auto">
  <name>Task 2: Коммит feat — .meta файлы ECS, Bridge и тестов</name>
  <files>Assets/Scripts/Bridge/*.meta, Assets/Scripts/ECS/**/*.meta, Assets/Tests/EditMode/ECS/*.meta, Assets/Tests/PlayMode/GameplayCycleTests.cs.meta</files>
  <action>
1. Добавить все .meta файлы ECS-компонентов, систем, Bridge и тестов:
   ```
   git add Assets/Scripts/Bridge.meta Assets/Scripts/Bridge/*.meta \
           Assets/Scripts/ECS.meta Assets/Scripts/ECS/**/*.meta \
           Assets/Tests/EditMode/ECS.meta Assets/Tests/EditMode/ECS/*.meta \
           Assets/Tests/PlayMode/GameplayCycleTests.cs.meta
   ```
2. Закоммитить:
   ```
   git commit -m "feat(ecs): track .meta files for ECS components, systems, bridge and tests

   Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
   ```

ВАЖНО: НЕ добавлять Assets/TestWithTempAssets* — временные артефакты Unity Test Runner.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "feat(ecs)" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>Все .meta файлы ECS/Bridge/Tests закоммичены, TestWithTempAssets не включены</done>
</task>

<task type="auto">
  <name>Task 3: Коммит feat — URP ассеты и ProjectSettings</name>
  <files>Assets/DefaultVolumeProfile.asset, Assets/DefaultVolumeProfile.asset.meta, Assets/UniversalRenderPipelineGlobalSettings.asset, Assets/UniversalRenderPipelineGlobalSettings.asset.meta, Assets/Media/materials/Particle-URP.mat, Packages/packages-lock.json, ProjectSettings/EditorSettings.asset, ProjectSettings/EntitiesClientSettings.asset, ProjectSettings/PackageManagerSettings.asset, ProjectSettings/SceneTemplateSettings.json, ProjectSettings/ShaderGraphSettings.asset, ProjectSettings/URPProjectSettings.asset</files>
  <action>
1. Добавить URP ассеты, обновленные ProjectSettings и packages-lock:
   ```
   git add Assets/DefaultVolumeProfile.asset Assets/DefaultVolumeProfile.asset.meta \
           Assets/UniversalRenderPipelineGlobalSettings.asset Assets/UniversalRenderPipelineGlobalSettings.asset.meta \
           Assets/Media/materials/Particle-URP.mat \
           Packages/packages-lock.json \
           ProjectSettings/EditorSettings.asset ProjectSettings/EntitiesClientSettings.asset \
           ProjectSettings/PackageManagerSettings.asset ProjectSettings/SceneTemplateSettings.json \
           ProjectSettings/ShaderGraphSettings.asset ProjectSettings/URPProjectSettings.asset
   ```
2. Закоммитить:
   ```
   git commit -m "feat(urp): track URP assets and updated ProjectSettings

   Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
   ```
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -q "feat(urp)" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>URP ассеты и ProjectSettings закоммичены, рабочее дерево чистое (кроме исключенных worktrees и TestWithTempAssets)</done>
</task>

</tasks>

<verification>
После всех 3 коммитов:
- `git log --oneline -3` показывает 3 новых коммита в правильном порядке
- `git status` показывает только .claude/worktrees/ и Assets/TestWithTempAssets* как untracked
- `git diff --cached` пуст (ничего не осталось в staging area)
</verification>

<success_criteria>
- 3 логических коммита созданы на ветке feature/dots
- .claude/worktrees/ и Assets/TestWithTempAssets* НЕ закоммичены
- Рабочее дерево чистое кроме явно исключенных файлов
</success_criteria>

<output>
After completion, create `.planning/quick/260403-4vq-mcp-meta-urp-projectsettings/260403-4vq-SUMMARY.md`
</output>
